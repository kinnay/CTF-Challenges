pkcs5.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
pkcs5.h:#define MBEDTLS_ERR_PKCS5_BAD_INPUT_DATA                  -0x2f80
pkcs5.h:#define MBEDTLS_ERR_PKCS5_INVALID_FORMAT                  -0x2f00
pkcs5.h:#define MBEDTLS_ERR_PKCS5_FEATURE_UNAVAILABLE             -0x2e80
pkcs5.h:#define MBEDTLS_ERR_PKCS5_PASSWORD_MISMATCH               -0x2e00
pkcs5.h: *                 - if #MBEDTLS_CIPHER_PADDING_PKCS7 is enabled at compile
pkcs5.h: *                   attempting to brute-forcing the password. Note also that
pkcs5.h: *                 - if #MBEDTLS_CIPHER_PADDING_PKCS7 is disabled at compile
pkcs5.h: *                 - This function validates the CBC padding and returns
pkcs5.h: *                   attempting to brute-forcing the password. Note also that
version.h: * \brief Run-time version information
version.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
version.h: * This set of run-time variables can be used to determine the version number of
version.h: * the Mbed TLS library used. Compile-time version defines for the same can be
version.h: *                  Multi-threading support.
version.h: *                  -1 if the feature is not present and
version.h: *                  -2 if support for feature checking as a whole was not
block_cipher.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
block_cipher.h:        unsigned dummy; /* Make the union non-empty even with no supported algorithms. */
ssl.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
ssl.h:#define MBEDTLS_ERR_SSL_CRYPTO_IN_PROGRESS                -0x7000
ssl.h:#define MBEDTLS_ERR_SSL_FEATURE_UNAVAILABLE               -0x7080
ssl.h:#define MBEDTLS_ERR_SSL_BAD_INPUT_DATA                    -0x7100
ssl.h:#define MBEDTLS_ERR_SSL_INVALID_MAC                       -0x7180
ssl.h:#define MBEDTLS_ERR_SSL_INVALID_RECORD                    -0x7200
ssl.h:#define MBEDTLS_ERR_SSL_CONN_EOF                          -0x7280
ssl.h:#define MBEDTLS_ERR_SSL_DECODE_ERROR                      -0x7300
ssl.h:#define MBEDTLS_ERR_SSL_NO_RNG                            -0x7400
ssl.h:#define MBEDTLS_ERR_SSL_NO_CLIENT_CERTIFICATE             -0x7480
ssl.h:#define MBEDTLS_ERR_SSL_UNSUPPORTED_EXTENSION             -0x7500
ssl.h:#define MBEDTLS_ERR_SSL_NO_APPLICATION_PROTOCOL           -0x7580
ssl.h:/** The own private key or pre-shared key is not set, but needed. */
ssl.h:#define MBEDTLS_ERR_SSL_PRIVATE_KEY_REQUIRED              -0x7600
ssl.h:#define MBEDTLS_ERR_SSL_CA_CHAIN_REQUIRED                 -0x7680
ssl.h:#define MBEDTLS_ERR_SSL_UNEXPECTED_MESSAGE                -0x7700
ssl.h:#define MBEDTLS_ERR_SSL_FATAL_ALERT_MESSAGE               -0x7780
ssl.h:#define MBEDTLS_ERR_SSL_UNRECOGNIZED_NAME                 -0x7800
ssl.h:#define MBEDTLS_ERR_SSL_PEER_CLOSE_NOTIFY                 -0x7880
ssl.h:#define MBEDTLS_ERR_SSL_BAD_CERTIFICATE                   -0x7A00
ssl.h:#define MBEDTLS_ERR_SSL_RECEIVED_NEW_SESSION_TICKET       -0x7B00
ssl.h:#define MBEDTLS_ERR_SSL_CANNOT_READ_EARLY_DATA            -0x7B80
ssl.h: * Early data has been received as part of an on-going handshake.
ssl.h:#define MBEDTLS_ERR_SSL_RECEIVED_EARLY_DATA               -0x7C00
ssl.h:#define MBEDTLS_ERR_SSL_CANNOT_WRITE_EARLY_DATA           -0x7C80
ssl.h:#define MBEDTLS_ERR_SSL_CACHE_ENTRY_NOT_FOUND             -0x7E80
ssl.h:#define MBEDTLS_ERR_SSL_ALLOC_FAILED                      -0x7F00
ssl.h:#define MBEDTLS_ERR_SSL_HW_ACCEL_FAILED                   -0x7F80
ssl.h:#define MBEDTLS_ERR_SSL_HW_ACCEL_FALLTHROUGH              -0x6F80
ssl.h:#define MBEDTLS_ERR_SSL_BAD_PROTOCOL_VERSION              -0x6E80
ssl.h:#define MBEDTLS_ERR_SSL_HANDSHAKE_FAILURE                 -0x6E00
ssl.h:#define MBEDTLS_ERR_SSL_SESSION_TICKET_EXPIRED            -0x6D80
ssl.h:#define MBEDTLS_ERR_SSL_PK_TYPE_MISMATCH                  -0x6D00
ssl.h:#define MBEDTLS_ERR_SSL_UNKNOWN_IDENTITY                  -0x6C80
ssl.h:/** Internal error (eg, unexpected failure in lower-level module) */
ssl.h:#define MBEDTLS_ERR_SSL_INTERNAL_ERROR                    -0x6C00
ssl.h:#define MBEDTLS_ERR_SSL_COUNTER_WRAPPING                  -0x6B80
ssl.h:#define MBEDTLS_ERR_SSL_WAITING_SERVER_HELLO_RENEGO       -0x6B00
ssl.h:#define MBEDTLS_ERR_SSL_HELLO_VERIFY_REQUIRED             -0x6A80
ssl.h:#define MBEDTLS_ERR_SSL_BUFFER_TOO_SMALL                  -0x6A00
ssl.h:#define MBEDTLS_ERR_SSL_WANT_READ                         -0x6900
ssl.h:#define MBEDTLS_ERR_SSL_WANT_WRITE                        -0x6880
ssl.h:#define MBEDTLS_ERR_SSL_TIMEOUT                           -0x6800
ssl.h:#define MBEDTLS_ERR_SSL_CLIENT_RECONNECT                  -0x6780
ssl.h:#define MBEDTLS_ERR_SSL_UNEXPECTED_RECORD                 -0x6700
ssl.h:/** The alert message received indicates a non-fatal error. */
ssl.h:#define MBEDTLS_ERR_SSL_NON_FATAL                         -0x6680
ssl.h:#define MBEDTLS_ERR_SSL_ILLEGAL_PARAMETER                 -0x6600
ssl.h:/** Internal-only message signaling that further message-processing should be done */
ssl.h:#define MBEDTLS_ERR_SSL_CONTINUE_PROCESSING               -0x6580
ssl.h:#define MBEDTLS_ERR_SSL_ASYNC_IN_PROGRESS                 -0x6500
ssl.h:/** Internal-only message signaling that a message arrived early. */
ssl.h:#define MBEDTLS_ERR_SSL_EARLY_MESSAGE                     -0x6480
ssl.h:/** An encrypted DTLS-frame with an unexpected CID was received. */
ssl.h:#define MBEDTLS_ERR_SSL_UNEXPECTED_CID                    -0x6000
ssl.h:#define MBEDTLS_ERR_SSL_VERSION_MISMATCH                  -0x5F00
ssl.h:#define MBEDTLS_ERR_SSL_BAD_CONFIG                        -0x5E80
ssl.h: * - the certificate chain must be valid;
ssl.h: * - the chain must start from a trusted CA;
ssl.h: * - the certificate must cover the server name that is expected by the client.
ssl.h: * set up to only use PSK-based authentication, which does not rely on the
ssl.h: * - A TLS client is configured with the authentication mode
ssl.h: * - Certificate authentication is enabled.
ssl.h: * - The client does not call mbedtls_ssl_set_hostname().
ssl.h: * - The configuration option
ssl.h:#define MBEDTLS_ERR_SSL_CERTIFICATE_VERIFICATION_WITHOUT_HOSTNAME  -0x5D80
ssl.h: * Those are used in the Pre-Shared Key Exchange Modes extension.
ssl.h:#define MBEDTLS_SSL_TLS1_3_PSK_MODE_PURE  0 /* Pure PSK-based exchange  */
ssl.h:#define MBEDTLS_SSL_TLS1_3_PSK_MODE_ECDHE 1 /* PSK+ECDHE-based exchange */
ssl.h:#define MBEDTLS_SSL_TLS1_3_KEY_EXCHANGE_MODE_PSK            (1u << 0)   /*!< Pure-PSK TLS 1.3 key exchange,
ssl.h:#define MBEDTLS_SSL_TLS1_3_KEY_EXCHANGE_MODE_EPHEMERAL      (1u << 1)   /*!< Pure-Ephemeral TLS 1.3 key exchanges,
ssl.h:#define MBEDTLS_SSL_TLS1_3_KEY_EXCHANGE_MODE_PSK_EPHEMERAL  (1u << 2)   /*!< PSK-Ephemeral TLS 1.3 key exchanges,
ssl.h:     MBEDTLS_SSL_TLS1_3_KEY_EXCHANGE_MODE_PSK_EPHEMERAL)      /*!< All PSK-based TLS 1.3 key exchanges */
ssl.h: * - RFC 5246: ProtocolVersion version = { 3, 3 };     // TLS v1.2
ssl.h: * - RFC 8446: see section 4.2.1
ssl.h:#define MBEDTLS_SSL_RENEGOTIATION_NOT_ENFORCED  -1
ssl.h: * - attempting to deprotect received records using the handshake traffic
ssl.h: *   server proceeds as with an ordinary 1-RTT handshake.
ssl.h: * - skipping all records with an external content type of
ssl.h: * Maximum number of heap-allocated bytes for the purpose of
ssl.h:/* RSASSA-PKCS1-v1_5 algorithms */
ssl.h:/* RSASSA-PSS algorithms with public key OID rsaEncryption */
ssl.h:/* RSASSA-PSS algorithms with public key OID RSASSA-PSS  */
ssl.h:#define MBEDTLS_SSL_ALERT_MSG_MISSING_EXTENSION    109  /* 0x6d -- new in TLS 1.3 */
ssl.h:#define MBEDTLS_TLS_EXT_CID                        254 /* Pre-RFC 9146 DTLS 1.2 CID */
ssl.h: * TLS1-3-AES-256-GCM-SHA384, set the PSK maximum length to 48 instead of 32.
ssl.h: * the cipher suite is TLS1-3-AES-256-GCM-SHA384 (pre-shared keys are 48
ssl.h:    unsigned char dummy; /* Make the union non-empty even with SSL disabled */
ssl.h:    unsigned char _pms_ecjpake[32];     /* Thread spec: SHA-256 output */
ssl.h: * \note           That callback may be either blocking or non-blocking.
ssl.h: *                 or a non-zero error code.
ssl.h: *                 If performing non-blocking I/O, \c MBEDTLS_ERR_SSL_WANT_WRITE
ssl.h: * \note           That callback may be either blocking or non-blocking.
ssl.h: * \returns        If performing non-blocking I/O, \c MBEDTLS_ERR_SSL_WANT_READ
ssl.h: *                 or a non-zero error code:
ssl.h: * \note           If using an event-driven style of programming, an event must
ssl.h: *                 -1 if cancelled (fin_ms == 0),
ssl.h: * \brief          Callback type: server-side session cache getter
ssl.h: *                 This callback retrieves an entry in this key-value store.
ssl.h: * \return                A non-zero return value on failure.
ssl.h: * \brief          Callback type: server-side session cache setter
ssl.h: *                 This callback sets an entry in this key-value store.
ssl.h: * \return                A non-zero return value on failure.
ssl.h: *                  the handshake step to be non-blocking.
ssl.h: *                  "EMSA-PKCS1-v1_5" as follows.
ssl.h: *                  - If \p md_alg is #MBEDTLS_MD_NONE, apply the PKCS#1 v1.5
ssl.h: *                    padded. In other words, apply EMSA-PKCS1-v1_5 starting
ssl.h: *                  - If `md_alg != MBEDTLS_MD_NONE`, apply the PKCS#1 v1.5
ssl.h: *                    padded. In other words, apply EMSA-PKCS1-v1_5 starting
ssl.h: *                  `Ecdsa-Sig-Value` defined in
ssl.h: *                  [RFC 4492 section 5.4](https://tools.ietf.org/html/rfc4492#section-5.4).
ssl.h: *                  the handshake step to be non-blocking.
ssl.h: *                  the handshake step to be non-blocking.
ssl.h:#error "Bad configuration - need SHA-1, SHA-256 or SHA-512 enabled to compute digest of peer CRT."
ssl.h: * For code readability use a typedef for DTLS-SRTP profiles
ssl.h: * http://www.iana.org/assignments/srtp-protection/srtp-protection.xhtml
ssl.h:/** Human-friendly representation of the (D)TLS protocol version. */
ssl.h: *  - in tests/suites/test_suite_ssl.function:
ssl.h: *  - in library/ssl_tls.c:
ssl.h:    /*! The digest of the peer's end-CRT. This must be kept to detect CRT
ssl.h:     * so that elements tend to be in the 128-element direct access window
ssl.h:     * Flags (could be bit-fields to save RAM, but separate bytes make
ssl.h:    uint8_t MBEDTLS_PRIVATE(encrypt_then_mac); /*!< negotiate encrypt-then-mac?    */
ssl.h:    /** Allowed ciphersuites for (D)TLS 1.2 (0-terminated)                  */
ssl.h:    /** Callback for getting (pseudo-)random numbers                        */
ssl.h:    unsigned char *MBEDTLS_PRIVATE(psk);      /*!< The raw pre-shared key. This field should
ssl.h:    size_t         MBEDTLS_PRIVATE(psk_len);  /*!< The length of the raw pre-shared key.
ssl.h:                                               *   Its value is non-zero if and only if
ssl.h:                                                      *   Its value is non-zero if and only if
ssl.h:                                                  *   - MBEDTLS_SSL_EARLY_DATA_DISABLED,
ssl.h:                                                  *   - MBEDTLS_SSL_EARLY_DATA_ENABLED */
ssl.h:    /* The maximum amount of 0-RTT data. RFC 8446 section 4.6.1 */
ssl.h:     * - DTLS: records with a bad MAC received.
ssl.h:     * - TLS: accumulated length of handshake fragments (up to \c in_hslen).
ssl.h:    unsigned char *MBEDTLS_PRIVATE(in_ctr);      /*!< 64-bit incoming message counter
ssl.h:    unsigned char *MBEDTLS_PRIVATE(in_len);      /*!< two-bytes message length field   */
ssl.h:    unsigned char *MBEDTLS_PRIVATE(in_iv);       /*!< ivlen-byte IV                    */
ssl.h:    int MBEDTLS_PRIVATE(nb_zero);                /*!< # of 0-length encrypted messages */
ssl.h:                                                    - \c 0 , no alert is to be sent.
ssl.h:                                                    - \c 1 , alert is to be sent. */
ssl.h:    unsigned char *MBEDTLS_PRIVATE(out_ctr);     /*!< 64-bit outgoing message counter  */
ssl.h:    unsigned char *MBEDTLS_PRIVATE(out_len);     /*!< two-bytes message length field   */
ssl.h:    unsigned char *MBEDTLS_PRIVATE(out_iv);      /*!< ivlen-byte IV                    */
ssl.h:     * - \p NULL in a newly initialized or reset context.
ssl.h:     * - A heap-allocated copy of the last value passed to
ssl.h:     *   mbedtls_ssl_set_hostname(), if the last call had a non-null
ssl.h:     * - A special value to indicate that mbedtls_ssl_set_hostname()
ssl.h:    unsigned char  *MBEDTLS_PRIVATE(cli_id);         /*!<  transport-level ID of the client  */
ssl.h:     *  CID currently used in case the user has re-configured the CID
ssl.h: * \warning        After setting up a client context, if certificate-based
ssl.h: * \brief          Reset an already initialized SSL context for re-use
ssl.h: *                 while retaining application-set variables, function
ssl.h:    return conf->MBEDTLS_PRIVATE(endpoint);
ssl.h: *  security properties for early data (also refered to as 0-RTT data) are
ssl.h: * \brief Set the maximum amount of 0-RTT data in bytes
ssl.h: *        The value defines the maximum amount of 0-RTT data
ssl.h: *       The maximum amount of 0-RTT data should thus be large enough
ssl.h: * \param[in] max_early_data_size   The maximum amount of 0-RTT data.
ssl.h:    return ssl->MBEDTLS_PRIVATE(conf);
ssl.h: *                 read-with-timeout.
ssl.h: *                 the other is used. If both are non-NULL, f_recv_timeout is
ssl.h: *                 - non-blocking I/O, f_recv != NULL, f_recv_timeout == NULL
ssl.h: *                 - blocking I/O, f_recv == NULL, f_recv_timeout != NULL
ssl.h: * \note           For DTLS, you need to provide either a non-NULL
ssl.h: *                    Reference: RFC 9146 (or draft-ietf-tls-dtls-connection-id-05
ssl.h: *                    https://tools.ietf.org/html/draft-ietf-tls-dtls-connection-id-05
ssl.h: *                    - MBEDTLS_SSL_CID_ENABLED to enable the use of the CID.
ssl.h: *                    - MBEDTLS_SSL_CID_DISABLED (default) to disable the use
ssl.h: *                    - On the Client, enabling the use of the CID through
ssl.h: *                    - On the Server, enabling the use of the CID through
ssl.h: *                    of CID-based demultiplexing before handing datagrams to
ssl.h: *                 willing to receive. Client-side, you can request the
ssl.h: * \brief          Set a connection-specific verification callback (optional).
ssl.h: * \note           With blocking I/O, this will only work if a non-NULL
ssl.h: *                 With non-blocking I/O, this will only work if timer
ssl.h: * \note           With non-blocking I/O, you may also skip this function
ssl.h: *                 This function does not change the user-visible state
ssl.h: *                 here, except if using an event-driven style.
ssl.h: *                 https://mbed-tls.readthedocs.io/en/latest/kb/how-to/dtls-tutorial
ssl.h: * \brief           Set the certificate selection callback (server-side only).
ssl.h:    conf->MBEDTLS_PRIVATE(f_cert_cb) = f_cert_cb;
ssl.h: *                  any other non-zero code for other failures.
ssl.h: *                  non-NULL callbacks.
ssl.h:        session->MBEDTLS_PRIVATE(endpoint) != MBEDTLS_SSL_IS_SERVER) {
ssl.h:    *ticket_creation_time = session->MBEDTLS_PRIVATE(ticket_creation_time);
ssl.h: * \brief          Get the session-id buffer.
ssl.h: * \return         The address of the session-id buffer.
ssl.h:    return &session->MBEDTLS_PRIVATE(id);
ssl.h: * \brief          Get the size of the session-id.
ssl.h: * \return         size_t size of session-id buffer.
ssl.h:    return session->MBEDTLS_PRIVATE(id_len);
ssl.h: * \brief          Get the ciphersuite-id.
ssl.h:    return session->MBEDTLS_PRIVATE(ciphersuite);
ssl.h: *          - Debugging: Use this API to e.g. generate an NSSKeylog
ssl.h: *          - Application-specific export: Use this API to implement
ssl.h: *            key exporters, e.g. for EAP-TLS or DTLS-SRTP.
ssl.h:    conf->MBEDTLS_PRIVATE(user_data).p = p;
ssl.h:    conf->MBEDTLS_PRIVATE(user_data).n = n;
ssl.h:    return conf->MBEDTLS_PRIVATE(user_data).p;
ssl.h:    return conf->MBEDTLS_PRIVATE(user_data).n;
ssl.h:    ssl->MBEDTLS_PRIVATE(user_data).p = p;
ssl.h:    ssl->MBEDTLS_PRIVATE(user_data).n = n;
ssl.h:    return ssl->MBEDTLS_PRIVATE(user_data).p;
ssl.h:    return ssl->MBEDTLS_PRIVATE(user_data).n;
ssl.h: * \brief          Set client's transport-level identification info.
ssl.h: * \param info     Transport-level info identifying the client (eg IP + port)
ssl.h: * \brief          Enable or disable anti-replay protection for DTLS.
ssl.h: *                 the Nth non-authentic record is seen.
ssl.h: *                 ones going through the authentication-decryption phase.
ssl.h: * \note           This is a security trade-off related to the fact that it's
ssl.h: *                 expected round-trip time to your peer, plus whatever time
ssl.h: *                 goes: send ... 1s -> resend ... 2s -> resend ... 4s ->
ssl.h: *                 resend ... 5s -> give up and return a timeout error.
ssl.h: * \brief          Set the session cache callbacks (server-side only)
ssl.h: *                 resumption: For TLS 1.2, both session ID-based resumption
ssl.h: *                 and ticket-based resumption will be considered. For TLS 1.3,
ssl.h: *                 loaded. This error is non-fatal, and has no observable
ssl.h: *                 end-entity certificate and the result of the chain
ssl.h: *                 potentially in non-volatile storage, for resuming later.
ssl.h: * \param ciphersuites  A 0-terminated list of IANA identifiers of supported
ssl.h: *                   - MBEDTLS_SSL_TLS1_3_KEY_EXCHANGE_MODE_PSK
ssl.h: *                     This flag enables pure-PSK key exchanges.
ssl.h: *                   - MBEDTLS_SSL_TLS1_3_KEY_EXCHANGE_MODE_PSK_EPHEMERAL
ssl.h: *                     This flag enables combined PSK-ephemeral key exchanges.
ssl.h: *                   - MBEDTLS_SSL_TLS1_3_KEY_EXCHANGE_MODE_EPHEMERAL
ssl.h: *                     This flag enables pure-ephemeral key exchanges.
ssl.h: *                   For convenience, the following pre-defined macros are
ssl.h: *                   - MBEDTLS_SSL_TLS1_3_KEY_EXCHANGE_MODE_ALL
ssl.h: *                     Includes all of pure-PSK, PSK-ephemeral and pure-ephemeral.
ssl.h: *                   - MBEDTLS_SSL_TLS1_3_KEY_EXCHANGE_MODE_PSK_ALL
ssl.h: *                     Includes both pure-PSK and combined PSK-ephemeral
ssl.h: *                     key exchanges, but excludes pure-ephemeral key exchanges.
ssl.h: *                   - MBEDTLS_SSL_TLS1_3_KEY_EXCHANGE_MODE_EPHEMERAL_ALL
ssl.h: *                     Includes both pure-ephemeral and combined PSK-ephemeral
ssl.h: * \note  If a PSK-based key exchange mode shall be supported, applications
ssl.h: * \note  If a pure-ephemeral key exchange mode shall be supported,
ssl.h: *        server-side applications must also provide a certificate via
ssl.h: *                          - #MBEDTLS_SSL_UNEXPECTED_CID_IGNORE
ssl.h: *                          - #MBEDTLS_SSL_UNEXPECTED_CID_FAIL
ssl.h: *                      allow variable-length incoming IDs. Mbed TLS currently
ssl.h: * \param ca_chain trusted CA chain (meaning all fully trusted top-level CAs)
ssl.h:    conf->MBEDTLS_PRIVATE(dn_hints) = crt;
ssl.h: *                 CertificateRequest messages: A server-side SSL context which
ssl.h: * \warning        In multi-threaded environments, the callback \p f_ca_cb
ssl.h: *                 must be thread-safe, and it is the user's responsibility
ssl.h: *                 certificate chain. The top certificate (self-signed)
ssl.h: *                 RSA with SHA-256, one RSA with SHA-1). An adequate
ssl.h: *                 whether it matches those preferences - the server can then
ssl.h: * \brief          Configure pre-shared keys (PSKs) and their
ssl.h: *                 identities to be used in PSK-based ciphersuites.
ssl.h: * \param psk      The pointer to the pre-shared key to use.
ssl.h: * \param psk_len  The length of the pre-shared key in bytes.
ssl.h: * \param psk_identity      The pointer to the pre-shared key identity.
ssl.h: * \param psk_identity_len  The length of the pre-shared key identity
ssl.h: * \brief          Configure one or more opaque pre-shared keys (PSKs) and
ssl.h: *                 their identities to be used in PSK-based ciphersuites.
ssl.h: * \param psk_identity      The pointer to the pre-shared key identity.
ssl.h: * \param psk_identity_len  The length of the pre-shared key identity
ssl.h: * \brief          Set the pre-shared Key (PSK) for the current handshake.
ssl.h: * \param psk      The pointer to the pre-shared key.
ssl.h: * \param psk_len  The length of the pre-shared key in bytes.
ssl.h: * \brief          Set an opaque pre-shared Key (PSK) for the current handshake.
ssl.h: * \brief          Set the PSK callback (server-side only).
ssl.h: *                 handshake where a PSK-based ciphersuite was negotiated.
ssl.h: *                 - \c void*: The opaque pointer \p p_psk.
ssl.h: *                 - \c mbedtls_ssl_context*: The SSL context to which
ssl.h: *                 - \c const unsigned char*: The PSK identity
ssl.h: *                 - \c size_t: The length of the PSK identity
ssl.h: * \brief          Set the Diffie-Hellman public P and G values
ssl.h: *                 from big-endian binary presentations.
ssl.h: * \param dhm_P    Diffie-Hellman-Merkle modulus in big-endian binary form
ssl.h: * \param dhm_G    Diffie-Hellman-Merkle generator in big-endian binary form
ssl.h: * \brief          Set the Diffie-Hellman public P and G values,
ssl.h: *                 read from existing context (server-side only)
ssl.h: * \param dhm_ctx  Diffie-Hellman-Merkle context
ssl.h: * \brief          Set the minimum length for Diffie-Hellman parameters.
ssl.h: *                 (Client-side only.)
ssl.h: *                 ECDHE and in the peer's end-entity certificate.
ssl.h: *                 for that. For the end-entity certificate however, the key
ssl.h: *                 ECDHE-only curves selected according to the same criteria.
ssl.h: *                 - For TLS 1.3, values of \c MBEDTLS_TLS1_3_SIG_XXXX should be
ssl.h: *                 - For TLS 1.2, values should be given as
ssl.h: *                 pre-shared keys, or in scenarios where server
ssl.h: *                 (client-side only)
ssl.h:    return ssl->MBEDTLS_PRIVATE(hostname);
ssl.h: *                 - value is valid only when called in \c f_cert_cb
ssl.h: *                 - value is NULL if SNI extension is not present.
ssl.h: *                 - value is not '\0'-terminated. Use \c name_len for len.
ssl.h: *                 - value must not be freed.
ssl.h: * \param ca_chain trusted CA chain (meaning all fully trusted top-level CAs)
ssl.h: *                 (optional, server-side only).
ssl.h: *                 callback may return non-zero to abort the handshake.
ssl.h: * \brief          Set the EC J-PAKE password for current handshake.
ssl.h: * \param pw       EC J-PAKE password (pre-shared secret). It cannot be empty
ssl.h: * \brief          Set the EC J-PAKE opaque password for current handshake.
ssl.h: * \param pwd      EC J-PAKE opaque password
ssl.h: * \param protos   Pointer to a NULL-terminated list of supported protocols,
ssl.h: *                          and re-keying. See RFC3711 section 3.1 for details.
ssl.h: * \brief                   Set the supported DTLS-SRTP protection profiles.
ssl.h: * \brief                  Set the mki_value for the current DTLS-SRTP session.
ssl.h: * \brief                  Get the negotiated DTLS-SRTP information:
ssl.h: * \param dtls_srtp_info   The negotiated DTLS-SRTP information:
ssl.h: *                         - Protection profile in use.
ssl.h:                   http://www.iana.org/assignments/srtp-protection/srtp-protection.xhtml
ssl.h: *                         - mki size and value( if size is > 0 ).
ssl.h:    conf->MBEDTLS_PRIVATE(max_tls_version) = tls_version;
ssl.h: *                 - (D)TLS 1.2: \p major = #MBEDTLS_SSL_MAJOR_VERSION_3,
ssl.h: *                 - TLS 1.3: \p major = #MBEDTLS_SSL_MAJOR_VERSION_3,
ssl.h: *                 differ by one from the human-readable versions!
ssl.h:    conf->MBEDTLS_PRIVATE(min_tls_version) = tls_version;
ssl.h: * \brief           Enable or disable Encrypt-then-MAC
ssl.h: * \note           Server-side, enabling renegotiation also makes the server
ssl.h: *                 with non-upgraded peers. Allowing legacy renegotiation
ssl.h: *                 in interoperability issues with non-upgraded peers
ssl.h: * \note           With DTLS and server-initiated renegotiation, the
ssl.h: *                 - max_records records have beens seen, if it is >= 0, or
ssl.h: *                 - the number of retransmits that would happen during an
ssl.h: *                 enforce renegotiation, or a non-negative value to enforce
ssl.h: *                 (Default: 2^48 - 1)
ssl.h: *                 conf->disable_renegotiation to
ssl.h: *                 period is 2^48 - 1, and for MBEDTLS_SSL_TRANSPORT_STREAM,
ssl.h: *                 the maximum renegotiation period is 2^64 - 1.
ssl.h: * \param period   The threshold value: a big-endian 64-bit number.
ssl.h: *                 returns a non-zero value, this function will
ssl.h: *                 operation - e.g. a handshake or an application record
ssl.h: *                 read - and you're awaiting incoming data next, you
ssl.h: * \return         \c -1u if the result is not available. This may happen
ssl.h:    return ssl->MBEDTLS_PRIVATE(tls_version);
ssl.h: *                 version-specific. It takes into account various factors, such as
ssl.h: *                 version-specific. It takes into account various factors, such as
ssl.h: *                 (PSK-based ciphersuites, for example), or because
ssl.h: * \note           For one-time inspection of the peer's certificate during
ssl.h: *                 resumption: For TLS 1.2, both session ID-based resumption and
ssl.h: *                 ticket-based resumption will be considered. For TLS 1.3,
ssl.h: *                 transport - in this case you must call this function again
ssl.h: *                 mbedtls_ssl_conf_async_private_cb()) - in this case you
ssl.h: *                 operation is in progress (see mbedtls_ecp_set_max_ops()) -
ssl.h: *                 and the client did not demonstrate reachability yet - in
ssl.h: * \return         Another SSL error code - in this case you must stop using
ssl.h: *                 on it before re-using it for a new connection; the current
ssl.h: * \note           Remarks regarding event-driven DTLS:
ssl.h: *                 - On an Mbed TLS server that only accepts TLS 1.2,
ssl.h: *                 - The first fragment of a handshake message must be
ssl.h: *                 - Non-handshake records must not be interleaved between
ssl.h:    return ssl->MBEDTLS_PRIVATE(state) >= MBEDTLS_SSL_HANDSHAKE_OVER;
ssl.h: * \note           The state of the context (ssl->state) will be at
ssl.h: *                 context state (ssl->state) in order to ascertain when to
ssl.h: *                 so with something like ssl->MBEDTLS_PRIVATE(state) or by
ssl.h: *                 re-using it for a new connection; the current connection
ssl.h: *                 re-using it for a new connection; the current connection
ssl.h: *                 stack, non-conformant peer not sending a CloseNotify and
ssl.h: *                 such) - in this case you must stop using the context
ssl.h: *                 transport - in this case you must call this function again
ssl.h: *                 mbedtls_ssl_conf_async_private_cb()) - in this case you
ssl.h: *                 operation is in progress (see mbedtls_ecp_set_max_ops()) -
ssl.h: * \return         Another SSL error code - in this case you must stop using
ssl.h: *                 on it before re-using it for a new connection; the current
ssl.h: *                 (which can only happen server-side), it means that a client
ssl.h: * \note           Remarks regarding event-driven DTLS:
ssl.h: *                 - If the function returns #MBEDTLS_ERR_SSL_WANT_READ, no datagram
ssl.h: *                 - This function may return MBEDTLS_ERR_SSL_WANT_READ even if data was
ssl.h: *                   when reacting to an incoming-data event from the underlying transport.
ssl.h: *                 - On success, the datagram of the underlying transport that is currently
ssl.h: *                 return value is non-negative but less than length, the
ssl.h: *                 buf + ret, len - ret (if ret is the return value) until
ssl.h: * \return         The (non-negative) number of bytes actually written if
ssl.h: *                 transport - in this case you must call this function again
ssl.h: *                 mbedtls_ssl_conf_async_private_cb()) - in this case you
ssl.h: *                 operation is in progress (see mbedtls_ecp_set_max_ops()) -
ssl.h: * \return         Another SSL error code - in this case you must stop using
ssl.h: *                 a non-negative value,
ssl.h: *                 on it before re-using it for a new connection; the current
ssl.h: *                 fragment length (either the built-in limit or the one set
ssl.h: *                 - with TLS, less bytes than requested are written.
ssl.h: *                 - with DTLS, MBEDTLS_ERR_SSL_BAD_INPUT_DATA is returned.
ssl.h: *                 call \c mbedtls_ssl_session_reset() on it before re-using it
ssl.h: *                 call \c mbedtls_ssl_session_reset() on it before re-using it
ssl.h: *                 The security properties for 0-RTT data are weaker than
ssl.h: *                 - This data is not forward secret, as it is encrypted
ssl.h: *                 - There are no guarantees of non-replay between connections.
ssl.h: *                   Protection against replay for ordinary TLS 1.3 1-RTT data
ssl.h: *                   is provided via the server's Random value, but 0-RTT data
ssl.h: * \warning        Mbed TLS does not implement any of the anti-replay defenses
ssl.h: *                 single-use of tickets or ClientHello recording within a
ssl.h: *                 The security properties for 0-RTT data are weaker than
ssl.h: *                 - This data is not forward secret, as it is encrypted
ssl.h: *                 - There are no guarantees of non-replay between connections.
ssl.h: *                   Protection against replay for ordinary TLS 1.3 1-RTT data
ssl.h: *                   is provided via the server's Random value, but 0-RTT data
ssl.h: * \return         The (non-negative) number of bytes actually written if
ssl.h: *                 from the server-side.
ssl.h: *                 it. In this situation, the client may want to re-send the
ssl.h: *                 mbedtls_ssl_write_early_data() as ordinary post-handshake
ssl.h: *                 This allows the freeing or re-using of the SSL context
ssl.h: *                 state. You're then free to either re-use the context
ssl.h: *                 also need to configure some connection-specific callbacks
ssl.h: *                 connection and the case of loading an already-established
ssl.h: *                 newly-configured value with the value that was active when
ssl.h: * \brief          TLS-PRF function for key derivation.
config_adjust_legacy_crypto.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
config_adjust_legacy_crypto.h:/* Auto-enable CIPHER_C when any of the unauthenticated ciphers is builtin
config_adjust_legacy_crypto.h:/* Auto-enable MBEDTLS_MD_LIGHT based on MBEDTLS_MD_C.
config_adjust_legacy_crypto.h:/* Auto-enable MBEDTLS_MD_LIGHT if needed by a module that didn't require it
config_adjust_legacy_crypto.h: * - MBEDTLS_MD_CAN_xxx is defined if the md module can perform xxx.
config_adjust_legacy_crypto.h: * - MBEDTLS_MD_xxx_VIA_PSA is defined if the md module may perform xxx via PSA
config_adjust_legacy_crypto.h: * - MBEDTLS_MD_SOME_PSA is defined if at least one algorithm may be performed
config_adjust_legacy_crypto.h: * - MBEDTLS_MD_SOME_LEGACY is defined if at least one algorithm may be performed
config_adjust_legacy_crypto.h:/* Built-in implementations */
config_adjust_legacy_crypto.h: * - PSA is enabled and drivers have been initialized
config_adjust_legacy_crypto.h: * - desired key type is supported on the PSA side
config_adjust_legacy_crypto.h: * - MBEDTLS_BLOCK_CIPHER_SOME_PSA: there is at least 1 key type between AES,
config_adjust_legacy_crypto.h: * - MBEDTLS_BLOCK_CIPHER_xxx_VIA_PSA: xxx key type is supported through a
config_adjust_legacy_crypto.h: * - MBEDTLS_BLOCK_CIPHER_xxx_VIA_LEGACY: xxx key type is supported through
config_adjust_legacy_crypto.h: * or CIPHER_C. The former is auto-enabled when:
config_adjust_legacy_crypto.h: * - CIPHER_C is not defined, which is also the legacy solution;
config_adjust_legacy_crypto.h: * - BLOCK_CIPHER_SOME_PSA because in this case BLOCK_CIPHER can take advantage
config_adjust_legacy_crypto.h:/* MBEDTLS_ECP_LIGHT is auto-enabled by the following symbols:
config_adjust_legacy_crypto.h: * - MBEDTLS_ECP_C because now it consists of MBEDTLS_ECP_LIGHT plus functions
config_adjust_legacy_crypto.h: * - MBEDTLS_PK_PARSE_EC_EXTENDED and MBEDTLS_PK_PARSE_EC_COMPRESSED because
config_adjust_legacy_crypto.h: *   to enable the built-in solution.
config_adjust_legacy_crypto.h: *   - PK_PARSE_EC_EXTENDED will be removed after #7779 and #7789
config_adjust_legacy_crypto.h: *   - support for compressed points should also be added to PSA, but in this
config_adjust_legacy_crypto.h: * - PSA_WANT_KEY_TYPE_ECC_KEY_PAIR_DERIVE because Weierstrass key derivation
config_adjust_legacy_crypto.h: * - PK_C + USE_PSA + PSA_WANT_ALG_ECDSA is a temporary dependency which will
config_adjust_legacy_crypto.h: * compatibility, we auto-enable PK_PARSE_EC_COMPRESSED when these conditions
error.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
error.h: * bits signed integers to support all platforms (-0x0001 - -0x7FFF). In
error.h: * 16 bit error code bit-segmentation
error.h: * 1 bit  - Unused (sign bit)
error.h: * 3 bits - High level module ID
error.h: * 5 bits - Module-dependent error code
error.h: * 7 bits - Low level module errors
error.h: * For historical reasons, low-level error codes are divided in even and odd,
error.h: * even codes were assigned first, and -1 is reserved for other errors.
error.h: * Low-level module errors (0x0002-0x007E, 0x0001-0x007F)
error.h: * MPI       7  0x0002-0x0010
error.h: * GCM       3  0x0012-0x0016   0x0013-0x0013
error.h: * THREADING 3  0x001A-0x001E
error.h: * AES       5  0x0020-0x0022   0x0021-0x0025
error.h: * CAMELLIA  3  0x0024-0x0026   0x0027-0x0027
error.h: * BASE64    2  0x002A-0x002C
error.h: * OID       1  0x002E-0x002E   0x000B-0x000B
error.h: * PADLOCK   1  0x0030-0x0030
error.h: * DES       2  0x0032-0x0032   0x0033-0x0033
error.h: * CTR_DBRG  4  0x0034-0x003A
error.h: * ENTROPY   3  0x003C-0x0040   0x003D-0x003F
error.h: * NET      13  0x0042-0x0052   0x0043-0x0049
error.h: * ARIA      4  0x0058-0x005E
error.h: * ASN1      7  0x0060-0x006C
error.h: * CMAC      1  0x007A-0x007A
error.h: * PBKDF2    1  0x007C-0x007C
error.h: * HMAC_DRBG 4                  0x0003-0x0009
error.h: * CCM       3                  0x000D-0x0011
error.h: * MD5       1                  0x002F-0x002F
error.h: * RIPEMD160 1                  0x0031-0x0031
error.h: * SHA1      1                  0x0035-0x0035 0x0073-0x0073
error.h: * SHA256    1                  0x0037-0x0037 0x0074-0x0074
error.h: * SHA512    1                  0x0039-0x0039 0x0075-0x0075
error.h: * SHA-3     1                  0x0076-0x0076
error.h: * CHACHA20  3                  0x0051-0x0055
error.h: * POLY1305  3                  0x0057-0x005B
error.h: * CHACHAPOLY 2 0x0054-0x0056
error.h: * PLATFORM  2  0x0070-0x0072
error.h: * LMS       5  0x0011-0x0019
error.h: * High-level module nr (3 bits - 0x0...-0x7...)
error.h: *                   0x7380, 0x7900-0x7980, 0x7A80-0x7E80)
error.h: * Module dependent error code (5 bits 0x.00.-0x.F8.)
error.h:#define MBEDTLS_ERR_ERROR_GENERIC_ERROR       -0x0001
error.h:#define MBEDTLS_ERR_ERROR_CORRUPTION_DETECTED -0x006E
error.h:#define MBEDTLS_ERR_PLATFORM_HW_ACCEL_FAILED     -0x0070
error.h:#define MBEDTLS_ERR_PLATFORM_FEATURE_UNSUPPORTED -0x0072
error.h: * \brief Combines a high-level and low-level error code together.
error.h: * \brief Combines a high-level and low-level error code together.
error.h: * \param high      high-level error code. See error.h for more details.
error.h: * \param low       low-level error code. See error.h for more details.
error.h: * \brief Translate the high-level part of an Mbed TLS error code into a string
error.h: * This function returns a const pointer to an un-modifiable string. The caller
error.h: * \brief Translate the low-level part of an Mbed TLS error code into a string
error.h: * This function returns a const pointer to an un-modifiable string. The caller
chachapoly.h: * \brief   This file contains the AEAD-ChaCha20-Poly1305 definitions and
chachapoly.h: *          ChaCha20-Poly1305 is an algorithm for Authenticated Encryption
chachapoly.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
chachapoly.h:#define MBEDTLS_ERR_CHACHAPOLY_BAD_STATE            -0x0054
chachapoly.h:#define MBEDTLS_ERR_CHACHAPOLY_AUTH_FAILED          -0x0056
chachapoly.h: * \brief           This function initializes the specified ChaCha20-Poly1305 context.
chachapoly.h: *                  - call \c mbedtls_chachapoly_starts() and (if needed)
chachapoly.h: *                  - call \c mbedtls_chachapoly_update() multiple times and
chachapoly.h: *                  - call \c mbedtls_chachapoly_finish() to compute the
chachapoly.h: *                  ChaCha20-Poly1305 context.
chachapoly.h: *                  case this function is a no-op.
chachapoly.h: * \brief           This function sets the ChaCha20-Poly1305
chachapoly.h: * \param ctx       The ChaCha20-Poly1305 context to which the key should be
chachapoly.h: * \brief           This function starts a ChaCha20-Poly1305 encryption or
chachapoly.h: * \param ctx       The ChaCha20-Poly1305 context. This must be initialized
chachapoly.h: *                  into an ongoing ChaCha20-Poly1305 operation.
chachapoly.h: * \param ctx       The ChaCha20-Poly1305 context. This must be initialized
chachapoly.h: *                  into an on-going ChaCha20-Poly1305
chachapoly.h: * \param ctx       The ChaCha20-Poly1305 context to use. This must be initialized.
chachapoly.h: * \brief           This function finished the ChaCha20-Poly1305 operation and
chachapoly.h: * \param ctx       The ChaCha20-Poly1305 context to use. This must be initialized.
chachapoly.h: * \param mac       The buffer to where the 128-bit (16 bytes) MAC is written.
chachapoly.h: * \brief           This function performs a complete ChaCha20-Poly1305
chachapoly.h: *                  authenticated encryption with the previously-set key.
chachapoly.h: * \param ctx       The ChaCha20-Poly1305 context to use (holds the key).
chachapoly.h: * \param nonce     The 96-bit (12 bytes) nonce/IV to use.
chachapoly.h: * \param tag       The buffer to where the computed 128-bit (16 bytes) MAC
chachapoly.h: * \brief           This function performs a complete ChaCha20-Poly1305
chachapoly.h: *                  authenticated decryption with the previously-set key.
chachapoly.h: * \param ctx       The ChaCha20-Poly1305 context to use (holds the key).
chachapoly.h: * \brief           The ChaCha20-Poly1305 checkup routine.
x509_crl.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
x509_crl.h: * Contains the CA-specific serial numbers and revocation dates.
x509_crl.h:    void *MBEDTLS_PRIVATE(sig_opts);             /**< Signature options to be passed to mbedtls_pk_verify_ext(), e.g. for RSASSA-PSS */
x509_crl.h: * \brief          Parse a DER-encoded CRL and append it to the chained list
ssl_ciphersuites.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
ssl_ciphersuites.h: * above, except RSA-PSK uses a server certificate but no client cert.
ssl_ciphersuites.h: * (Ephemeral mode) or both disallowed (PSK and PKS-Ephemeral modes).
ssl_ciphersuites.h:/* Helper to state that certificate-based client authentication through ECDSA
ssl_ciphersuites.h: * - by TLS 1.2 with `USE_PSA` to do ECDH or ECDHE;
ssl_ciphersuites.h: * - by TLS 1.3 to do ECDHE or FFDHE.
ssl_ciphersuites.h:    return info->MBEDTLS_PRIVATE(name);
ssl_ciphersuites.h:    return info->MBEDTLS_PRIVATE(id);
cmac.h: * The Cipher-based Message Authentication Code (CMAC) Mode for
cmac.h: * Authentication is defined in <em>RFC-4493: The AES-CMAC Algorithm</em>.
cmac.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
cmac.h:    /** Unprocessed data - either data that was not block aligned and is still
cmac.h: *                      AES-128 and AES-256, and may support AES-192 and 3DES.
cmac.h: * \return              A cipher-specific error code on failure.
cmac.h: *                      AES-128 and AES-256, and may support AES-192 and 3DES.
cmac.h: * \brief           This function implements the AES-CMAC-PRF-128 pseudorandom
cmac.h: *                  <em>RFC-4615: The Advanced Encryption Standard-Cipher-based
cmac.h: *                  Message Authentication Code-Pseudo-Random Function-128
cmac.h: *                  (AES-CMAC-PRF-128) Algorithm for the Internet Key
cmac.h: *                 not support the less widely used AES-192 or 3DES primitives.
cmac.h: *                 The self-test requires at least AES-128 and AES-256 to be
constant_time.h: *  Constant-time functions
constant_time.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
constant_time.h:/** Constant-time buffer comparison without branches.
constant_time.h: * and w.r.t. whether \p a and \p b are equal or not. It is not constant-time
constant_time.h: * This function can be used to write constant-time code by replacing branches
constant_time.h: *              otherwise non-zero.
threading.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
threading.h:#define MBEDTLS_ERR_THREADING_BAD_INPUT_DATA              -0x001C
threading.h:#define MBEDTLS_ERR_THREADING_MUTEX_ERROR                 -0x001E
threading.h:    /* WARNING - state should only be accessed when holding the mutex lock in
threading.h: * A mutex used to make the non-rng PSA global_data struct members thread safe.
hmac_drbg.h: * in <em>NIST SP 800-90A: Recommendation for Random Number Generation Using
hmac_drbg.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
hmac_drbg.h:#define MBEDTLS_ERR_HMAC_DRBG_REQUEST_TOO_BIG              -0x0003
hmac_drbg.h:#define MBEDTLS_ERR_HMAC_DRBG_INPUT_TOO_BIG                -0x0005
hmac_drbg.h:#define MBEDTLS_ERR_HMAC_DRBG_FILE_IO_ERROR                -0x0007
hmac_drbg.h:#define MBEDTLS_ERR_HMAC_DRBG_ENTROPY_SOURCE_FAILED        -0x0009
hmac_drbg.h:     * md_ctx->md_info != NULL. This means that the mutex is initialized
hmac_drbg.h: * - \p f_entropy is mbedtls_entropy_func();
hmac_drbg.h: * - \p p_entropy is an instance of ::mbedtls_entropy_context initialized
hmac_drbg.h: *                      - 128 bits if \p md_info is SHA-1;
hmac_drbg.h: *                      - 192 bits if \p md_info is SHA-224;
hmac_drbg.h: *                      - 256 bits if \p md_info is SHA-256, SHA-384 or SHA-512.
hmac_drbg.h: *                      Note that SHA-256 is just as efficient as SHA-224.
hmac_drbg.h: *                      reseeding, are not thread-safe.
hmac_drbg.h: *                      #MBEDTLS_HMAC_DRBG_MAX_SEED_INPUT - \c entropy_len * 3 / 2
hmac_drbg.h: *                      reseeding, are not thread-safe.
hmac_drbg.h: * \note                This function is not thread-safe. It is not safe
hmac_drbg.h: * \note                This function is not thread-safe. It is not safe
hmac_drbg.h: *                      #MBEDTLS_HMAC_DRBG_MAX_SEED_INPUT - \c entropy_len
hmac_drbg.h: * \note                This function is not thread-safe. It is not safe
hmac_drbg.h: *                      reseeding, are not thread-safe.
platform_time.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
platform_time.h: * \return Monotonically-increasing current time in milliseconds.
platform_time.h: * \warning This function returns a monotonically-increasing time value from a
mbedtls_config.h: *  This set of compile-time options may be used to enable
mbedtls_config.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
mbedtls_config.h: * The platform lacks support for double-width integer division (64-bit
mbedtls_config.h: * division on a 32-bit platform, 128-bit division on a 64-bit platform).
mbedtls_config.h: * The bignum code uses double-width division to speed up some operations.
mbedtls_config.h: * Double-width division is often implemented in software that needs to
mbedtls_config.h: * be linked with the program. The presence of a double-width integer
mbedtls_config.h: * Uncomment this option to prevent the use of double-width division.
mbedtls_config.h: * Furthermore, a 64-bit type is always required even on a 32-bit
mbedtls_config.h: * cases it is also desirable to disable some double-width operations. For
mbedtls_config.h: * example, if double-width division is implemented in software, disabling
mbedtls_config.h: * The platform lacks support for 32x32 -> 64-bit multiplication.
mbedtls_config.h: * Some parts of the library may use multiplication of two unsigned 32-bit
mbedtls_config.h: * operands with a 64-bit result in order to speed up computations. On some
mbedtls_config.h: * 64-bit multiplier by embedding a software implementation in Mbed TLS.
mbedtls_config.h: * Uncomment if the CPU supports SSE2 (IA-32 specific).
mbedtls_config.h: * \note If MBEDTLS_TIMING_C is set - to enable the semi-portable timing
mbedtls_config.h: *       interface - timing.c will include time.h on suitable platforms
mbedtls_config.h: * mbedtls_platform_gmtime_r() at compile-time by using the macro
mbedtls_config.h: * By default Mbed TLS uses the system-provided calloc() and free().
mbedtls_config.h: * This allows different allocators (self-implemented or provided) to be
mbedtls_config.h: * - if !MBEDTLS_PLATFORM_MEMORY
mbedtls_config.h: *     - mbedtls_calloc = calloc
mbedtls_config.h: * - if MBEDTLS_PLATFORM_MEMORY
mbedtls_config.h: *     - if (MBEDTLS_PLATFORM_CALLOC_MACRO && MBEDTLS_PLATFORM_FREE_MACRO):
mbedtls_config.h: *         - mbedtls_calloc = MBEDTLS_PLATFORM_CALLOC_MACRO
mbedtls_config.h: *     - if !(MBEDTLS_PLATFORM_CALLOC_MACRO && MBEDTLS_PLATFORM_FREE_MACRO):
mbedtls_config.h: *         - Dynamic setup via mbedtls_platform_set_calloc_free is now possible with a default value MBEDTLS_PLATFORM_STD_CALLOC.
mbedtls_config.h: *         - How is MBEDTLS_PLATFORM_STD_CALLOC handled?
mbedtls_config.h: *         - if MBEDTLS_PLATFORM_NO_STD_FUNCTIONS:
mbedtls_config.h: *             - MBEDTLS_PLATFORM_STD_CALLOC is not set to anything;
mbedtls_config.h: *             - MBEDTLS_PLATFORM_STD_MEM_HDR can be included if present;
mbedtls_config.h: *         - if !MBEDTLS_PLATFORM_NO_STD_FUNCTIONS:
mbedtls_config.h: *             - if MBEDTLS_PLATFORM_STD_CALLOC is present:
mbedtls_config.h: *                 - User-defined MBEDTLS_PLATFORM_STD_CALLOC is respected;
mbedtls_config.h: *             - if !MBEDTLS_PLATFORM_STD_CALLOC:
mbedtls_config.h: *                 - MBEDTLS_PLATFORM_STD_CALLOC = calloc
mbedtls_config.h: *         - At this point the presence of MBEDTLS_PLATFORM_STD_CALLOC is checked.
mbedtls_config.h: *         - if !MBEDTLS_PLATFORM_STD_CALLOC
mbedtls_config.h: *             - MBEDTLS_PLATFORM_STD_CALLOC = uninitialized_calloc
mbedtls_config.h: *         - mbedtls_calloc = MBEDTLS_PLATFORM_STD_CALLOC.
mbedtls_config.h: * gmtime() is not a thread-safe function as defined in the C standard. The
mbedtls_config.h: * \warning   MD5, DES and SHA-1 are considered weak and their
mbedtls_config.h: *      - ecp.c
mbedtls_config.h: *      - ecp_curves.c
mbedtls_config.h: *       MBEDTLS_AES_ROM_TABLES in order to help the linker garbage-collect the AES
mbedtls_config.h: * \warning   MD5, DES and SHA-1 are considered weak and their use
mbedtls_config.h: *            the static HMAC-DRBG as RNG to mbedtls_ecdsa_sign(). Therefore
mbedtls_config.h: * (the tables are entry-wise rotations of one another).
mbedtls_config.h: * Use only 128-bit keys in AES operations to save ROM.
mbedtls_config.h: * Uncomment this macro to remove support for AES operations that use 192-
mbedtls_config.h: * or 256-bit keys.
mbedtls_config.h: * on v8-M/Thumb2.
mbedtls_config.h: * If this macro is defined, emit a compile-time warning if application code
mbedtls_config.h: * Enable Xor-encrypt-xor with ciphertext stealing mode (XTS) for AES.
mbedtls_config.h: * Uncomment this macro to use a 128-bit key in the CTR_DRBG module.
mbedtls_config.h: * Without this, CTR_DRBG uses a 256-bit key
mbedtls_config.h: * option is not compatible with taking the library under the GPL v2.0-or-later license.
mbedtls_config.h: * Enable "non-blocking" ECC operations that can return early and be resumed.
mbedtls_config.h: * This is useful in non-threaded environments if you want to avoid blocking
mbedtls_config.h: * - Adds xxx_restartable() variants of existing operations in the
mbedtls_config.h: *   - ECP (for Short Weierstrass curves only): scalar multiplication (mul),
mbedtls_config.h: *   - ECDSA: signature generation & verification;
mbedtls_config.h: *   - PK: signature generation & verification;
mbedtls_config.h: *   - X509: certificate chain verification.
mbedtls_config.h: * - Adds mbedtls_ecdh_enable_restart() in the ECDH module.
mbedtls_config.h: * - Changes the behaviour of TLS 1.2 clients (not servers) when using the
mbedtls_config.h: *   ECDHE-ECDSA key exchange (not other key exchanges) to make all ECC
mbedtls_config.h: *   - ECDH operations from the key exchange, only for Short Weierstrass
mbedtls_config.h: *   - verification of the server's key exchange signature;
mbedtls_config.h: *   - verification of the server's certificate chain;
mbedtls_config.h: *   - generation of the client's signature if client authentication is used,
mbedtls_config.h: * may result in a compromise of the long-term signing key. This is avoided by
mbedtls_config.h: * Enable the DHE-PSK based ciphersuite modes in SSL / TLS.
mbedtls_config.h: * Enable the ECDHE-PSK based ciphersuite modes in SSL / TLS.
mbedtls_config.h: * Enable the RSA-PSK based ciphersuite modes in SSL / TLS.
mbedtls_config.h: * Enable the RSA-only based ciphersuite modes in SSL / TLS.
mbedtls_config.h: * Enable the DHE-RSA based ciphersuite modes in SSL / TLS.
mbedtls_config.h: * Enable the ECDHE-RSA based ciphersuite modes in SSL / TLS.
mbedtls_config.h: * Enable the ECDHE-ECDSA based ciphersuite modes in SSL / TLS.
mbedtls_config.h: * Enable the ECDH-ECDSA based ciphersuite modes in SSL / TLS.
mbedtls_config.h: * Enable the ECDH-RSA based ciphersuite modes in SSL / TLS.
mbedtls_config.h: * \warning This is currently experimental. EC J-PAKE support is based on the
mbedtls_config.h: *           SHA-256 (via MBEDTLS_SHA256_C or a PSA driver)
mbedtls_config.h: * \warning If SHA-256 is provided only by a PSA driver, you must call
mbedtls_config.h: * Enable the prime-number generation code.
mbedtls_config.h: * Do not use built-in platform entropy functions.
mbedtls_config.h: * Uncomment this macro to disable the built-in platform entropy functions.
mbedtls_config.h: * Force the entropy accumulator to use a SHA-256 accumulator instead of the
mbedtls_config.h: * default SHA-512 based one (if both are available).
mbedtls_config.h: * On 32-bit systems SHA-256 can be much faster than SHA-512. Use this option
mbedtls_config.h: * Enable the non-volatile (NV) seed file-based entropy source.
mbedtls_config.h: *       compile-time depending on the flags (MBEDTLS_PLATFORM_NV_SEED_*) used.
mbedtls_config.h: * which is currently hard-coded to be int32_t.
mbedtls_config.h: *           GLIBC-compatible backtrace() and backtrace_symbols() support
mbedtls_config.h: * This enables support for RSAES-OAEP and RSASSA-PSS operations.
mbedtls_config.h: * Enable support for platform built-in keys. If you enable this feature,
mbedtls_config.h: * Built-in keys are typically derived from a hardware unique key or
mbedtls_config.h: *       or delivers non-uniform output, declare it as an entropy source
mbedtls_config.h: * NSPE (Non-Secure Process Environment) and an SPE (Secure Process
mbedtls_config.h: * Uncomment to enable p256-m. This is an alternative implementation of
mbedtls_config.h: * - p256-m has a much smaller code size and RAM footprint.
mbedtls_config.h: * - p256-m is only available via the PSA API. This includes the pk module
mbedtls_config.h: * - p256-m does not support deterministic ECDSA, EC-JPAKE, custom protocols
mbedtls_config.h: * If you enable this option, you do not need to enable any ECC-related
mbedtls_config.h: * - #MBEDTLS_PSA_CRYPTO_C and #MBEDTLS_PSA_CRYPTO_CONFIG for PSA-based
mbedtls_config.h: * - #MBEDTLS_USE_PSA_CRYPTO if you want to use p256-m from PK, X.509 or TLS;
mbedtls_config.h: * - #PSA_WANT_ECC_SECP_R1_256;
mbedtls_config.h: * - #PSA_WANT_ALG_ECDH and/or #PSA_WANT_ALG_ECDSA as needed;
mbedtls_config.h: * - #PSA_WANT_KEY_TYPE_ECC_PUBLIC_KEY, #PSA_WANT_KEY_TYPE_ECC_KEY_PAIR_BASIC,
mbedtls_config.h: * \note To benefit from the smaller code size of p256-m, make sure that you
mbedtls_config.h: *       do not enable any ECC-related option not supported by p256-m: this
mbedtls_config.h: *       would cause the built-in ECC implementation to be built as well, in
mbedtls_config.h: *       the built-in ECC implementation, see docs/driver-only-builds.md.
mbedtls_config.h: * required on systems that do not have a built-in entropy source (TRNG).
mbedtls_config.h: * This feature is currently not supported on systems that have a built-in
mbedtls_config.h: * Enable an implementation of SHA-256 that has lower ROM footprint but also
mbedtls_config.h: * the expense of performance. Eg on Cortex-M4 it reduces the size of
mbedtls_config.h: * Enable an implementation of SHA-512 that has lower ROM footprint but also
mbedtls_config.h: * - MBEDTLS_SSL_CID_OUT_LEN_MAX
mbedtls_config.h: * - MBEDTLS_SSL_CID_IN_LEN_MAX.
mbedtls_config.h: * (version draft-ietf-tls-dtls-connection-id-05,
mbedtls_config.h: * https://tools.ietf.org/html/draft-ietf-tls-dtls-connection-id-05)
mbedtls_config.h: * - the certificate chain must be valid;
mbedtls_config.h: * - the chain must start from a trusted CA;
mbedtls_config.h: * - the certificate must cover the server name that is expected by the client.
mbedtls_config.h: * set up to only use PSK-based authentication, which does not rely on the
mbedtls_config.h: * - If this option is unset (default), the connection attempt is aborted
mbedtls_config.h: * - If this option is set, the TLS library does not check the server name
mbedtls_config.h: * context associated with the connection, then free or re-use that context
mbedtls_config.h: * handling a single, potentially long-lived connection.
mbedtls_config.h: * a timing side-channel.
mbedtls_config.h: * Enable support for Encrypt-then-MAC, RFC 7366.
mbedtls_config.h: * Comment this macro to disable support for Encrypt-then-MAC
mbedtls_config.h: * The two main uses of renegotiation are (1) refresh keys on long-lived
mbedtls_config.h: *               SHA-256 or SHA-512 provided by a PSA driver)
mbedtls_config.h: * \note See docs/architecture/tls13-support.md for a description of the TLS
mbedtls_config.h: * Enable support for the anti-replay mechanism in DTLS.
mbedtls_config.h: * Comment this to disable anti-replay in DTLS.
mbedtls_config.h: * Enable support for negotiation of DTLS-SRTP (RFC 5764)
mbedtls_config.h: * to negotiate the use of DTLS-SRTP and to allow the derivation of
mbedtls_config.h: * the use of DTLS-SRTP, the extended key exporter API
mbedtls_config.h: * through which the supported DTLS-SRTP protection
mbedtls_config.h: * runtime if you wish to negotiate the use of DTLS-SRTP.
mbedtls_config.h: * Enable server-side support for clients that reconnect from the same port.
mbedtls_config.h: * Client-side, provides full support for session tickets (maintenance of a
mbedtls_config.h: * Server-side, you also need to provide callbacks for writing and parsing
mbedtls_config.h: * Enable testing of the constant-flow nature of some sensitive functions with
mbedtls_config.h: * this non-functional property of the code under test.
mbedtls_config.h: * This setting requires compiling with clang -fsanitize=memory. The test
mbedtls_config.h: * Uncomment to enable testing of the constant-flow nature of selected code.
mbedtls_config.h: * Enable testing of the constant-flow nature of some sensitive functions with
mbedtls_config.h: * this non-functional property of the code under test.
mbedtls_config.h: * Uncomment to enable testing of the constant-flow nature of selected code.
mbedtls_config.h: * See `docs/architecture/testing/mbed-crypto-invasive-testing.md` for more
mbedtls_config.h: * this option is what allows use of drivers for ECDSA, ECDH and EC J-PAKE in
mbedtls_config.h: * \note See docs/use-psa-crypto.md for a complete description this option.
mbedtls_config.h: * Allow run-time checking of compile-time enabled features. Thus allowing users
mbedtls_config.h: * to check at run-time if the library is for instance compiled with threading
mbedtls_config.h: * Comment this to disable run-time checking and save ROM space
mbedtls_config.h: * signed with RSASSA-PSS (aka PKCS#1 v2.1).
mbedtls_config.h: * Comment this macro to disallow using RSASSA-PSS in certificates.
mbedtls_config.h: * Enable AES-NI support on x86-64 or x86-32.
mbedtls_config.h: * - Visual Studio: supported
mbedtls_config.h: * - GCC, x86-64, target not explicitly supporting AESNI:
mbedtls_config.h: * - GCC, x86-32, target not explicitly supporting AESNI:
mbedtls_config.h: * - GCC, x86-64 or x86-32, target supporting AESNI: supported.
mbedtls_config.h: *   For this assembly-less implementation, you must currently compile
mbedtls_config.h: *   SSE2 and AESNI instructions: `gcc -msse2 -maes -mpclmul` or
mbedtls_config.h: *   `clang -maes -mpclmul`.
mbedtls_config.h: * - Non-x86 targets: this option is silently ignored.
mbedtls_config.h: * - Other compilers: this option is silently ignored.
mbedtls_config.h: * This modules adds support for the AES-NI instructions on x86.
mbedtls_config.h: * \warning Runtime detection only works on Linux. For non-Linux operating
mbedtls_config.h: *          system, Armv8-A Cryptographic Extensions must be supported by
mbedtls_config.h: *          Minimum compiler versions for this feature when targeting 32-bit
mbedtls_config.h: * \note \c CFLAGS must be set to a minimum of \c -march=armv8-a+crypto for
mbedtls_config.h: * This module adds support for the AES Armv8-A Cryptographic Extensions on Armv8 systems.
mbedtls_config.h: * Enable the multi-precision integer library.
mbedtls_config.h: * Enable the Counter with CBC-MAC (CCM) mode for 128-bit block cipher.
mbedtls_config.h: * This module enables the AES-CCM ciphersuites, if other requisites are
mbedtls_config.h: * Enable the ChaCha20-Poly1305 AEAD algorithm.
mbedtls_config.h: * Auto-enabled by: MBEDTLS_PSA_CRYPTO_C depending on which ciphers are enabled
mbedtls_config.h: * Enable the CMAC (Cipher-based Message Authentication Code) mode for block
mbedtls_config.h: *       AES-192 or 3DES as underlying block ciphers for the CMAC operation.
mbedtls_config.h: * Enable the CTR_DRBG AES-based random generator.
mbedtls_config.h: * The CTR_DRBG generator uses AES-256 by default.
mbedtls_config.h: * To use AES-128 instead, enable \c MBEDTLS_CTR_DRBG_USE_128_BIT_KEY above.
mbedtls_config.h: * \note AES-128 will be used if \c MBEDTLS_AES_ONLY_128_BIT_KEY_LENGTH is set.
mbedtls_config.h: * \note To achieve a 256-bit security strength with CTR_DRBG,
mbedtls_config.h: *       you must use AES-256 *and* use sufficient entropy.
mbedtls_config.h: * Enable the Diffie-Hellman-Merkle module.
mbedtls_config.h: *      DHE-RSA, DHE-PSK
mbedtls_config.h: * Enable the elliptic curve Diffie-Hellman library.
mbedtls_config.h: *      ECDHE-ECDSA, ECDHE-RSA, DHE-PSK
mbedtls_config.h: *      ECDHE-ECDSA
mbedtls_config.h: * Enable the elliptic curve J-PAKE library.
mbedtls_config.h: * \note EC J-PAKE support is based on the Thread v1.0.0 specification.
mbedtls_config.h: * call psa_crypto_init() before doing any EC J-PAKE operations.
mbedtls_config.h: * Enable the platform-specific entropy code.
mbedtls_config.h: * This module enables the AES-GCM and CAMELLIA-GCM ciphersuites, if other
mbedtls_config.h: * Enable large pre-computed tables for  Galois/Counter Mode (GCM).
mbedtls_config.h: * (HMAC)-based key derivation function (HKDF).
mbedtls_config.h: * Enable the LMS stateful-hash asymmetric signature algorithm.
mbedtls_config.h: * Enable LMS private-key operations and signing code. Functions enabled by this
mbedtls_config.h: * Enable the Key Wrapping mode for 128-bit block ciphers,
mbedtls_config.h: * as defined in NIST SP 800-38F. Only KW and KWP modes
mbedtls_config.h: * Further, it is used for checking MD5-signed certificates, and for PBKDF1
mbedtls_config.h: * when decrypting PEM-encoded encrypted keys.
mbedtls_config.h: * https://mbed-tls.readthedocs.io/en/latest/kb/how-to/how-do-i-port-mbed-tls-to-a-new-environment-OS
mbedtls_config.h: * \warning When parsing password-protected files, if MD5 is provided only by
mbedtls_config.h: * Auto-enables: MBEDTLS_MD_C
mbedtls_config.h: * Enable PKCS #7 core for using PKCS #7-formatted signatures.
mbedtls_config.h: * RFC Link - https://tools.ietf.org/html/rfc2315
mbedtls_config.h: * Enable the platform abstraction layer that allows you to re-assign
mbedtls_config.h: *       - performing a TLS handshake if support for TLS 1.3 is enabled;
mbedtls_config.h: *       - using a TLS 1.3 connection;
mbedtls_config.h: *       - indirect calls from PK, X.509 or SSL functions when
mbedtls_config.h: *       - indirect calls to calculate a hash when #MBEDTLS_MD_C is disabled;
mbedtls_config.h: *       - any other call to a function that requires calling psa_crypto_init()
mbedtls_config.h: * Auto-enables: MBEDTLS_CIPHER_C if any unauthenticated (ie, non-AEAD) cipher
mbedtls_config.h: *               docs/driver-only-builds.md about that).
mbedtls_config.h: * \warning    This feature is not thread-safe, and should not be used in a
mbedtls_config.h: *             multi-threaded environment.
mbedtls_config.h: *       - All the key slots are allocated no matter if they are used or not.
mbedtls_config.h: *       - Each key buffer's length is #MBEDTLS_PSA_STATIC_KEY_SLOT_BUFFER_SIZE bytes.
mbedtls_config.h: * Enable the RIPEMD-160 hash algorithm.
mbedtls_config.h: * Enable the RSA public-key cryptosystem.
mbedtls_config.h: *      RSA, DHE-RSA, ECDHE-RSA, RSA-PSK
mbedtls_config.h: * and for SHA1-signed certificates.
mbedtls_config.h: * \warning   SHA-1 is considered a weak message digest and its use constitutes
mbedtls_config.h: * Enable the SHA-224 cryptographic hash algorithm.
mbedtls_config.h: * This module adds support for SHA-224.
mbedtls_config.h: * Enable the SHA-256 cryptographic hash algorithm.
mbedtls_config.h: * This module adds support for SHA-256.
mbedtls_config.h: * Enable acceleration of the SHA-256 and SHA-224 cryptographic hash algorithms
mbedtls_config.h: * for a non-Armv8-A build it will be silently ignored.
mbedtls_config.h: * \note \c CFLAGS must be set to a minimum of \c -march=armv8-a+crypto for
mbedtls_config.h: * Uncomment to have the library check for the Armv8-A SHA-256 crypto extensions
mbedtls_config.h: * Enable acceleration of the SHA-256 and SHA-224 cryptographic hash algorithms
mbedtls_config.h: * \note \c CFLAGS must be set to a minimum of \c -march=armv8-a+crypto for
mbedtls_config.h: * Uncomment to have the library use the Armv8-A SHA-256 crypto extensions
mbedtls_config.h: * Enable the SHA-384 cryptographic hash algorithm.
mbedtls_config.h: * Comment to disable SHA-384
mbedtls_config.h: * Enable SHA-512 cryptographic hash algorithms.
mbedtls_config.h: * This module adds support for SHA-512.
mbedtls_config.h: * Enable acceleration of the SHA-512 and SHA-384 cryptographic hash algorithms
mbedtls_config.h: * for a non-Aarch64 build it will be silently ignored.
mbedtls_config.h: * \note \c CFLAGS must be set to a minimum of \c -march=armv8.2-a+sha3 for
mbedtls_config.h: * Uncomment to have the library check for the A64 SHA-512 crypto extensions
mbedtls_config.h: * Enable acceleration of the SHA-512 and SHA-384 cryptographic hash algorithms
mbedtls_config.h: * \note \c CFLAGS must be set to a minimum of \c -march=armv8.2-a+sha3 for
mbedtls_config.h: * Uncomment to have the library use the A64 SHA-512 crypto extensions
mbedtls_config.h: * Enable an implementation of TLS server-side callbacks for session tickets.
mbedtls_config.h: * Traditionally, Mbed TLS assumes it is used in a non-threaded environment or
mbedtls_config.h: * - Any call to a PSA function (`psa_xxx()`).
mbedtls_config.h: * - Any call to a TLS, X.509 or PK function (`mbedtls_ssl_xxx()`,
mbedtls_config.h: * - A TLS 1.3 connection, regardless of the compile-time configuration.
mbedtls_config.h: * - Any library feature that calculates a hash, if `MBEDTLS_MD_C` is disabled.
mbedtls_config.h: *   As an exception, algorithm-specific low-level modules do not require
mbedtls_config.h: * - Any library feature that performs symmetric encryption or decryption,
mbedtls_config.h: *   As an exception, algorithm-specific low-level modules do not require
mbedtls_config.h: * - Any use of a cryptographic context if the same context is used in
mbedtls_config.h: * - Any call to a function where the documentation specifies that
mbedtls_config.h: * https://mbed-tls.readthedocs.io/en/latest/kb/development/thread-safety-and-multi-threading
mbedtls_config.h: * This allows different threading implementations (self-implemented or
mbedtls_config.h: * Enable the semi-portable timing interface.
mbedtls_config.h: * https://mbed-tls.readthedocs.io/en/latest/kb/how-to/how-do-i-port-mbed-tls-to-a-new-environment-OS
mbedtls_config.h: * Enable run-time version information.
mbedtls_config.h: * This module provides run-time version information.
mbedtls_config.h: * This header file specifies the compile-time configuration of Mbed TLS.
mbedtls_config.h: * non-standard feature of the C language, so this feature is only available
mbedtls_config.h: * non-standard feature of the C language, so this feature is only available
mbedtls_config.h: * non-standard feature of the C language, so this feature is only available
mbedtls_config.h: * non-standard feature of the C language, so this feature is only available
mbedtls_config.h: * non-standard feature of the C language, so this feature is only available
mbedtls_config.h: * client-server integrations of PSA crypto, where operation structures
mbedtls_config.h: * non-standard feature of the C language, so this feature is only available
mbedtls_config.h: * plus optionally a same-line Doxygen comment here if there is a Doxygen
mbedtls_config.h://#define MBEDTLS_CTR_DRBG_ENTROPY_LEN               48 /**< Amount of entropy used per seed by default (48 with SHA-512, 32 with SHA-256) */
mbedtls_config.h://#define MBEDTLS_ECP_FIXED_POINT_OPTIM      1 /**< Enable fixed-point speed-up */
mbedtls_config.h: * A non-null parameter will always be a pointer previously returned by #MBEDTLS_PLATFORM_STD_CALLOC and not yet freed.
mbedtls_config.h:/* Note: your snprintf must correctly zero-terminate the buffer! */
mbedtls_config.h:/* Note: your snprintf must correctly zero-terminate the buffer! */
mbedtls_config.h: *       - #MBEDTLS_PSA_CRYPTO_EXTERNAL_RNG if enabled;
mbedtls_config.h: *       - Entropy from #MBEDTLS_ENTROPY_C plus CTR_DRBG with AES
mbedtls_config.h: *       - Entropy from #MBEDTLS_ENTROPY_C plus HMAC_DRBG.
mbedtls_config.h: * Maximum number of heap-allocated bytes for the purpose of
mbedtls_config.h://#define MBEDTLS_PSK_MAX_LEN               32 /**< Max size of TLS pre-shared keys, in bytes (default 256 or 384 bits) */
mbedtls_config.h: * The default maximum amount of 0-RTT data. See the documentation of
mbedtls_config.h: * - The client ticket age is the time difference between the time when the
mbedtls_config.h: * - The server ticket age is the time difference between the time when the
pkcs7.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
pkcs7.h: *  - The RFC specifies 6 different content types. The only type currently
pkcs7.h: *    supported in Mbed TLS is the signed-data content type.
pkcs7.h: *  - The only supported PKCS #7 Signed Data syntax version is version 1
pkcs7.h: *  - The RFC specifies support for BER. This implementation is limited to
pkcs7.h: *  - The RFC specifies that multiple digest algorithms can be specified
pkcs7.h: *  - The RFC specifies the Signed Data type can contain multiple X.509 or PKCS #6 extended
pkcs7.h: *  - The RFC specifies the Signed Data type can contain
pkcs7.h: *    certificate-revocation lists (CRLs). This implementation has no support
pkcs7.h: *  - The RFC allows for SignerInfo structure to optionally contain
pkcs7.h: *  - The RFC allows for the signed Data type to contain contentInfo. This
pkcs7.h:#define MBEDTLS_ERR_PKCS7_INVALID_FORMAT                   -0x5300  /**< The format is invalid, e.g. different type expected. */
pkcs7.h:#define MBEDTLS_ERR_PKCS7_FEATURE_UNAVAILABLE              -0x5380  /**< Unavailable feature, e.g. anything other than signed data. */
pkcs7.h:#define MBEDTLS_ERR_PKCS7_INVALID_VERSION                  -0x5400  /**< The PKCS #7 version element is invalid or cannot be parsed. */
pkcs7.h:#define MBEDTLS_ERR_PKCS7_INVALID_CONTENT_INFO             -0x5480  /**< The PKCS #7 content info is invalid or cannot be parsed. */
pkcs7.h:#define MBEDTLS_ERR_PKCS7_INVALID_ALG                      -0x5500  /**< The algorithm tag or value is invalid or cannot be parsed. */
pkcs7.h:#define MBEDTLS_ERR_PKCS7_INVALID_CERT                     -0x5580  /**< The certificate tag or value is invalid or cannot be parsed. */
pkcs7.h:#define MBEDTLS_ERR_PKCS7_INVALID_SIGNATURE                -0x5600  /**< Error parsing the signature */
pkcs7.h:#define MBEDTLS_ERR_PKCS7_INVALID_SIGNER_INFO              -0x5680  /**< Error parsing the signer's info */
pkcs7.h:#define MBEDTLS_ERR_PKCS7_BAD_INPUT_DATA                   -0x5700  /**< Input invalid. */
pkcs7.h:#define MBEDTLS_ERR_PKCS7_ALLOC_FAILED                     -0x5780  /**< Allocation of memory failed. */
pkcs7.h:#define MBEDTLS_ERR_PKCS7_VERIFY_FAIL                      -0x5800  /**< Verification Failed */
pkcs7.h:#define MBEDTLS_ERR_PKCS7_CERT_DATE_INVALID                -0x5880  /**< The PKCS #7 date issued/expired dates are invalid */
pkcs7.h: * Type-length-value structure that allows for ASN.1 using DER.
pkcs7.h: * \brief          Verification of PKCS #7 signature against a caller-supplied
pkcs7.h: * \brief          Verification of PKCS #7 signature against a caller-supplied
ssl_cache.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
ssl_cache.h: *                 (Thread-safe if MBEDTLS_THREADING_C is enabled)
ssl_cache.h: *                 (Thread-safe if MBEDTLS_THREADING_C is enabled)
ssl_cache.h: *                 (Thread-safe if MBEDTLS_THREADING_C is enabled)
ssl_cache.h:    return cache->MBEDTLS_PRIVATE(timeout);
lms.h: * \brief This file provides an API for the LMS post-quantum-safe stateful-hash
lms.h:          public-key signature scheme as defined in RFC8554 and NIST.SP.200-208.
lms.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
lms.h:#define MBEDTLS_ERR_LMS_BAD_INPUT_DATA   -0x0011 /**< Bad data has been input to an LMS function */
lms.h:#define MBEDTLS_ERR_LMS_OUT_OF_PRIVATE_KEYS -0x0013 /**< Specified LMS key has utilised all of its private keys */
lms.h:#define MBEDTLS_ERR_LMS_VERIFY_FAILED    -0x0015 /**< LMS signature verification failed */
lms.h:#define MBEDTLS_ERR_LMS_ALLOC_FAILED     -0x0017 /**< LMS failed to allocate space for a private key */
lms.h:#define MBEDTLS_ERR_LMS_BUFFER_TOO_SMALL -0x0019 /**< Input/output buffer is too small to contain requited data */
lms.h: * https://www.iana.org/assignments/leighton-micali-signatures/leighton-micali-signatures.xhtml
lms.h: *  https://www.iana.org/assignments/leighton-micali-signatures/leighton-micali-signatures.xhtml.
lms.h:    mbedtls_lmots_algorithm_type_t MBEDTLS_PRIVATE(type); /*!< The LM-OTS key type identifier as
lms.h: *   UNINITIALIZED -> INIT [label="init"];
lms.h: *   HAVE_PUBLIC_KEY -> INIT [label="free"];
lms.h: *   INIT -> HAVE_PUBLIC_KEY [label="import_public_key"];
lms.h: *   INIT -> HAVE_PUBLIC_KEY [label="calculate_public_key from private key"];
lms.h: *   HAVE_PUBLIC_KEY -> HAVE_PUBLIC_KEY [label="export_public_key"];
lms.h: *   UNINITIALIZED -> INIT [label="init"];
lms.h: *   HAVE_PRIVATE_KEY -> INIT [label="free"];
lms.h: *   INIT -> HAVE_PRIVATE_KEY [label="generate_private_key"];
lms.h: *   HAVE_PRIVATE_KEY -> INIT [label="sign"];
lms.h:    mbedtls_lmots_algorithm_type_t MBEDTLS_PRIVATE(otstype); /*!< The LM-OTS key type identifier as
lms.h: *   UNINITIALIZED -> INIT [label="init"];
lms.h: *   HAVE_PUBLIC_KEY -> INIT [label="free"];
lms.h: *   INIT -> HAVE_PUBLIC_KEY [label="import_public_key"];
lms.h: *   INIT -> HAVE_PUBLIC_KEY [label="calculate_public_key from private key"];
lms.h: *   HAVE_PUBLIC_KEY -> HAVE_PUBLIC_KEY [label="export_public_key"];
lms.h: *   UNINITIALIZED -> INIT [label="init"];
lms.h: *   HAVE_PRIVATE_KEY -> INIT [label="free"];
lms.h: *   INIT -> HAVE_PRIVATE_KEY [label="generate_private_key"];
lms.h:                                                                   when have_private_key is 0 and non-NULL otherwise.
lms.h:                                                                   non-NULL otherwise.
lms.h: * \return         A non-zero error code on failure.
lms.h: * \return         A non-zero error code on failure.
lms.h: * \return         A non-zero error code on failure.
lms.h: *                           production**, due to as-yet unsolved problems with
lms.h: * \return         A non-zero error code on failure.
lms.h: * \return         A non-zero error code on failure.
lms.h: *                           production**, due to as-yet unsolved problems with
lms.h: * \return         A non-zero error code on failure.
chacha20.h: *          its Salsa cipher https://cr.yp.to/chacha/chacha-20080128.pdf
chacha20.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
chacha20.h:#define MBEDTLS_ERR_CHACHA20_BAD_INPUT_DATA         -0x0051
chacha20.h: *                  in which case this function is a no-op. If it is not
chacha20.h: * \note            A ChaCha20 context can be re-used with the same key by
chacha20.h: *                  point to non-overlapping buffers.
chacha20.h: *                  point to non-overlapping buffers.
sha256.h: * \brief This file contains SHA-224 and SHA-256 definitions and functions.
sha256.h: * The Secure Hash Algorithms 224 and 256 (SHA-224 and SHA-256) cryptographic
sha256.h: * hash functions are defined in <em>FIPS 180-4: Secure Hash Standard (SHS)</em>.
sha256.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
sha256.h:/** SHA-256 input data was malformed. */
sha256.h:#define MBEDTLS_ERR_SHA256_BAD_INPUT_DATA                 -0x0074
sha256.h: * \brief          The SHA-256 context structure.
sha256.h: *                 The structure is used both for SHA-256 and for SHA-224
sha256.h:                                                    0: Use SHA-256, or 1: Use SHA-224. */
sha256.h: * \brief          This function initializes a SHA-256 context.
sha256.h: * \param ctx      The SHA-256 context to initialize. This must not be \c NULL.
sha256.h: * \brief          This function clears a SHA-256 context.
sha256.h: * \param ctx      The SHA-256 context to clear. This may be \c NULL, in which
sha256.h: *                 it must point to an initialized SHA-256 context.
sha256.h: * \brief          This function clones the state of a SHA-256 context.
sha256.h: * \brief          This function starts a SHA-224 or SHA-256 checksum
sha256.h: *                 either \c 0 for SHA-256, or \c 1 for SHA-224.
sha256.h: *                 SHA-256 checksum calculation.
sha256.h: * \param ctx      The SHA-256 context. This must be initialized
sha256.h: * \brief          This function finishes the SHA-256 operation, and writes
sha256.h: * \param ctx      The SHA-256 context. This must be initialized
sha256.h: * \param output   The SHA-224 or SHA-256 checksum result.
sha256.h: *                 for SHA-256, \c 28 bytes for SHA-224.
sha256.h: *                 the ongoing SHA-256 computation. This function is for
sha256.h: * \param ctx      The SHA-256 context. This must be initialized.
sha256.h: * \brief          This function calculates the SHA-224 or SHA-256
sha256.h: *                 The SHA-256 result is calculated as
sha256.h: *                 output = SHA-256(input buffer).
sha256.h: * \param output   The SHA-224 or SHA-256 checksum result.
sha256.h: *                 for SHA-256, \c 28 bytes for SHA-224.
sha256.h: *                 either \c 0 for SHA-256, or \c 1 for SHA-224.
sha256.h: * \brief          The SHA-224 checkup routine.
sha256.h: * \brief          The SHA-256 checkup routine.
config_adjust_x509.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
ecdsa.h: * The use of ECDSA for TLS is defined in <em>RFC-4492: Elliptic Curve
ecdsa.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
ecdsa.h: * \note            This macro returns a compile-time constant if its argument
ecdsa.h: *     Ecdsa-Sig-Value ::= SEQUENCE {
ecdsa.h:    mbedtls_ecdsa_restart_ver_ctx *MBEDTLS_PRIVATE(ver); /*!<  ecdsa_verify() sub-context    */
ecdsa.h:    mbedtls_ecdsa_restart_sig_ctx *MBEDTLS_PRIVATE(sig); /*!<  ecdsa_sign() sub-context      */
ecdsa.h:    mbedtls_ecdsa_restart_det_ctx *MBEDTLS_PRIVATE(det); /*!<  ecdsa_sign_det() sub-context  */
ecdsa.h: *                  previously-hashed message.
ecdsa.h: *                  previously-hashed message, deterministic version.
ecdsa.h: *                  For more information, see <em>RFC-6979: Deterministic
ecdsa.h: *                      previously-hashed message, in a restartable way.
ecdsa.h: *                      previously-hashed message, in a restartable way.
ecdsa.h: *                  previously-hashed message.
ecdsa.h: *                  previously-hashed message, in a restartable manner
ecdsa.h: *                  to a buffer, serialized as defined in <em>RFC-4492:
ecdsa.h: * \warning         It is not thread-safe to use the same context in
ecdsa.h: *                  information, see <em>RFC-6979: Deterministic Usage
ecdsa.h: *                  a 256-bit curve is used. A buffer length of
ecdsa.h: *                  a 256-bit curve is used. A buffer length of
ecdsa.h: *                  a private-public key pair or a public key. In the former
poly1305.h: *          Poly1305 is a one-time message authenticator that can be used to
poly1305.h: *          authenticate messages. Poly1305-AES was created by Daniel
poly1305.h: *          Bernstein https://cr.yp.to/mac/poly1305-20050329.pdf The generic
poly1305.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
poly1305.h:#define MBEDTLS_ERR_POLY1305_BAD_INPUT_DATA         -0x0057
poly1305.h: *                  case this function is a no-op. If it is not \c NULL, it must
poly1305.h: * \brief           This function sets the one-time authentication key.
ecdh.h: * The Elliptic Curve Diffie-Hellman (ECDH) protocol is an anonymous
ecdh.h: * elliptic-curve public–private key pair.
ecdh.h: * For more information, see <em>NIST SP 800-56A Rev. 2: Recommendation for
ecdh.h: * Pair-Wise Key Establishment Schemes Using Discrete Logarithm
ecdh.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
ecdh.h: * (56 bytes on a 32-bit platform). In future versions of the library, it
ecdh.h:    } MBEDTLS_PRIVATE(ctx);                      /*!< Implementation-specific context. The
ecdh.h: *                  countermeasures against side-channel attacks.
ecdh.h: *                  - #MBEDTLS_ECDH_OURS: The key is ours.
ecdh.h: *                  - #MBEDTLS_ECDH_THEIRS: The key is that of the peer.
ecdh.h: *                  countermeasures against side-channel attacks.
ecdh.h: *                  computations once enabled, except by free-ing the context,
ecdh.h: *                  which cancels possible in-progress operations.
asn1.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
asn1.h:#define MBEDTLS_ERR_ASN1_OUT_OF_DATA                      -0x0060
asn1.h:#define MBEDTLS_ERR_ASN1_UNEXPECTED_TAG                   -0x0062
asn1.h:#define MBEDTLS_ERR_ASN1_INVALID_LENGTH                   -0x0064
asn1.h:#define MBEDTLS_ERR_ASN1_LENGTH_MISMATCH                  -0x0066
asn1.h:#define MBEDTLS_ERR_ASN1_INVALID_DATA                     -0x0068
asn1.h:#define MBEDTLS_ERR_ASN1_ALLOC_FAILED                     -0x006A
asn1.h:#define MBEDTLS_ERR_ASN1_BUF_TOO_SMALL                    -0x006C
asn1.h: * - 0x02 -- tag indicating INTEGER
asn1.h: * - 0x01 -- length in octets
asn1.h: * - 0x05 -- value
asn1.h: *     +-------+-----+------------+
asn1.h: *     +-------+-----+------------+
asn1.h:#define MBEDTLS_OID_SIZE(x) (sizeof(x) - 1)
asn1.h:    ((MBEDTLS_OID_SIZE(oid_str) != (oid_buf)->len) ||                \
asn1.h:     memcmp((oid_str), (oid_buf)->p, (oid_buf)->len) != 0)
asn1.h: * Type-length-value structure that allows for ASN1 using DER.
asn1.h: *              You must set `cur->next = NULL` before calling this function!
asn1.h: *              Otherwise it is impossible to distinguish a previously non-null
asn1.h: *              \c *cur. If the sequence is valid and non-empty, this
asn1.h: *              function sets `cur->buf.tag` to \p tag. This allows
asn1.h: *              a one-element sequence.
asn1.h: *              first element, `*cur->next` describes the second element, etc.
asn1.h: * \brief          Free a heap-allocated linked list presentation of
asn1.h: * - Allocate a head node `mbedtls_asn1_sequence *head` with mbedtls_calloc().
asn1.h: * - Allocate a head node `mbedtls_asn1_sequence *head` in any manner,
asn1.h: *   for example on the stack. Make sure that `head->next == NULL`.
asn1.h: *   call mbedtls_asn1_sequence_free() on `head->cur`,
asn1.h: *                       - \p ctx.
asn1.h: *                       - The tag of the current element.
asn1.h: *                       - A pointer to the start of the current element's
asn1.h: *                       - The length of the content of the current element.
asn1.h: *                       If the callback returns a non-zero value,
asn1.h: * \return               A non-zero error code forwarded from the callback
asn1.h: *                       \p cb in case the latter returns a non-zero value.
asn1.h: *              `entry->oid.p` and `entry->val.p`.
asn1.h: *              `entry->oid.p` and `entry->val.p` and then on `entry`
psa_util.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
psa_util.h: * \param bits          The bit-length of a private key on \p curve.
pem.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
pem.h:#define MBEDTLS_ERR_PEM_NO_HEADER_FOOTER_PRESENT          -0x1080
pem.h:#define MBEDTLS_ERR_PEM_INVALID_DATA                      -0x1100
pem.h:#define MBEDTLS_ERR_PEM_ALLOC_FAILED                      -0x1180
pem.h:/** RSA IV is not in hex-format. */
pem.h:#define MBEDTLS_ERR_PEM_INVALID_ENC_IV                    -0x1200
pem.h:#define MBEDTLS_ERR_PEM_UNKNOWN_ENC_ALG                   -0x1280
pem.h:#define MBEDTLS_ERR_PEM_PASSWORD_REQUIRED                 -0x1300
pem.h:#define MBEDTLS_ERR_PEM_PASSWORD_MISMATCH                 -0x1380
pem.h:#define MBEDTLS_ERR_PEM_FEATURE_UNAVAILABLE               -0x1400
pem.h:#define MBEDTLS_ERR_PEM_BAD_INPUT_DATA                    -0x1480
pem.h: * \param data      source data to look in (must be nul-terminated)
pem.h: *                  This must be a valid (non-null) pointer.
pem.h:    *buflen = ctx->MBEDTLS_PRIVATE(buflen);
pem.h:    return ctx->MBEDTLS_PRIVATE(buf);
rsa.h: * \brief This file provides an API for the RSA public-key cryptosystem.
rsa.h: * The RSA public-key cryptosystem is defined in <em>Public-Key
rsa.h: * and <em>Public-Key Cryptography Standards (PKCS) #1 v2.1:
rsa.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
rsa.h:#define MBEDTLS_ERR_RSA_BAD_INPUT_DATA                    -0x4080
rsa.h:#define MBEDTLS_ERR_RSA_INVALID_PADDING                   -0x4100
rsa.h:#define MBEDTLS_ERR_RSA_KEY_GEN_FAILED                    -0x4180
rsa.h:#define MBEDTLS_ERR_RSA_KEY_CHECK_FAILED                  -0x4200
rsa.h:#define MBEDTLS_ERR_RSA_PUBLIC_FAILED                     -0x4280
rsa.h:#define MBEDTLS_ERR_RSA_PRIVATE_FAILED                    -0x4300
rsa.h:#define MBEDTLS_ERR_RSA_VERIFY_FAILED                     -0x4380
rsa.h:#define MBEDTLS_ERR_RSA_OUTPUT_TOO_LARGE                  -0x4400
rsa.h:/** The random generator failed to generate non-zeros. */
rsa.h:#define MBEDTLS_ERR_RSA_RNG_FAILED                        -0x4480
rsa.h:#define MBEDTLS_RSA_SALT_LEN_ANY    -1
rsa.h:    mbedtls_mpi MBEDTLS_PRIVATE(DP);             /*!<  <code>D % (P - 1)</code>. */
rsa.h:    mbedtls_mpi MBEDTLS_PRIVATE(DQ);             /*!<  <code>D % (Q - 1)</code>. */
rsa.h:    mbedtls_mpi MBEDTLS_PRIVATE(Vf);             /*!<  The cached un-blinding value. */
rsa.h:                                                    EME-OAEP and EMSA-PSS encodings. */
rsa.h:    mbedtls_threading_mutex_t MBEDTLS_PRIVATE(mutex);    /*!<  Thread-safety mutex. */
rsa.h: * \note           Set padding to #MBEDTLS_RSA_PKCS_V21 for the RSAES-OAEP
rsa.h: *                 encryption scheme and the RSASSA-PSS signature scheme.
rsa.h: *                 completes the provided information to a ready-for-use
rsa.h: * \return         A non-zero error code on failure.
rsa.h: * \brief          This function imports core RSA parameters, in raw big-endian
rsa.h: *                 completes the provided information to a ready-for-use
rsa.h: * \return         A non-zero error code on failure.
rsa.h: *                 If this function runs successfully, the non-NULL buffers
rsa.h: * \return         A non-zero return code on any other failure.
rsa.h: *                 in raw big-endian binary format.
rsa.h: *                 If this function runs successfully, the non-NULL buffers
rsa.h: * \return         A non-zero return code on any other failure.
rsa.h: * \note           Alternative RSA implementations not using CRT-parameters
rsa.h: * \param DP       The MPI to hold \c D modulo `P-1`,
rsa.h: * \param DQ       The MPI to hold \c D modulo `Q-1`,
rsa.h: * \return         A non-zero error code on failure.
rsa.h: *             content of a PKCS1-encoded RSA private key, for example, you
rsa.h: * \brief          This function checks a public-private RSA key pair.
rsa.h: *                 of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus.
rsa.h: *                 of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus.
rsa.h: *                 against some side-channel attacks.
rsa.h: *                 of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus.
rsa.h: *                 of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus.
rsa.h: *                 of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus.
rsa.h: *                 (RSAES-PKCS1-v1_5-ENCRYPT).
rsa.h: *                 of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus.
rsa.h: *                   operation (RSAES-OAEP-ENCRYPT).
rsa.h: *                   of ctx->N. For example, 128 Bytes if RSA-1024 is used.
rsa.h: *                   of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                   for an 2048-bit RSA modulus.
rsa.h: * \warning        When \p ctx->padding is set to #MBEDTLS_RSA_PKCS_V15,
rsa.h: *                 inherently dangerous function (CWE-242).
rsa.h: *                 as large as the size \p ctx->len of \p ctx->N (for example,
rsa.h: *                 128 Bytes if RSA-1024 is used) to be able to hold an
rsa.h: *                 of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus.
rsa.h: *                 operation (RSAES-PKCS1-v1_5-DECRYPT).
rsa.h: * \warning        This is an inherently dangerous function (CWE-242). Unless
rsa.h: *                 as large as the size \p ctx->len of \p ctx->N, for example,
rsa.h: *                 128 Bytes if RSA-1024 is used, to be able to hold an
rsa.h: *                 of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus.
rsa.h: *                   operation (RSAES-OAEP-DECRYPT).
rsa.h: *                   as large as the size \p ctx->len of \p ctx->N, for
rsa.h: *                   example, 128 Bytes if RSA-1024 is used, to be able to
rsa.h: *                   of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                   for an 2048-bit RSA modulus.
rsa.h: *                 of \p ctx->N. For example, 128 Bytes if RSA-1024 is used.
rsa.h: * \param md_alg   The message-digest algorithm used to hash the original data.
rsa.h: *                 buffer of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus. A buffer length of
rsa.h: *                 operation (RSASSA-PKCS1-v1_5-SIGN).
rsa.h: * \param md_alg   The message-digest algorithm used to hash the original data.
rsa.h: *                 buffer of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus. A buffer length of
rsa.h: *                 operation (RSASSA-PSS-SIGN).
rsa.h: *                 mask generation function, consult <em>RFC-3447: Public-Key
rsa.h: *                 with FIPS 186-4 §5.5 (e) and RFC 8017 (PKCS#1 v2.2) §9.1.1
rsa.h: * \param md_alg   The message-digest algorithm used to hash the original data.
rsa.h: *                 FIPS 186-4 §5.5.
rsa.h: *                 buffer of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus. A buffer length of
rsa.h: *                 operation (RSASSA-PSS-SIGN).
rsa.h: *                 mask generation function, consult <em>RFC-3447: Public-Key
rsa.h: *                 size complies with FIPS 186-4 §5.5 (e) and RFC 8017 (PKCS#1
rsa.h: * \param md_alg   The message-digest algorithm used to hash the original data.
rsa.h: *                 buffer of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus. A buffer length of
rsa.h: * \param md_alg   The message-digest algorithm used to hash the original data.
rsa.h: *                 buffer of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus.
rsa.h: *                 operation (RSASSA-PKCS1-v1_5-VERIFY).
rsa.h: * \param md_alg   The message-digest algorithm used to hash the original data.
rsa.h: *                 buffer of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus.
rsa.h: *                 operation (RSASSA-PSS-VERIFY).
rsa.h: *                 mask generation function, consult <em>RFC-3447: Public-Key
rsa.h: * \param md_alg   The message-digest algorithm used to hash the original data.
rsa.h: *                 buffer of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus.
rsa.h: *                 operation (RSASSA-PSS-VERIFY).
rsa.h: *                 of \p ctx->N. For example, 128 Bytes if RSA-1024 is used.
rsa.h: * \param md_alg   The message-digest algorithm used to hash the original data.
rsa.h: *                          <em>RFC-3447: Public-Key Cryptography Standards
rsa.h: *                 buffer of length \c ctx->len Bytes. For example, \c 256 Bytes
rsa.h: *                 for an 2048-bit RSA modulus.
rsa.h: *                 this function is a no-op. If it is not \c NULL, it must
des.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
des.h:#define MBEDTLS_ERR_DES_INVALID_INPUT_LENGTH              -0x0032
des.h: * \brief          Triple-DES context structure
des.h: * \brief          Initialize Triple-DES context
des.h: * \brief          Clear Triple-DES context
des.h: * \param key      8-byte secret key
des.h: * \param key      8-byte secret key
des.h: * \brief          Check that key is not a weak or semi-weak DES key
des.h: * \param key      8-byte secret key
des.h: * \brief          DES key schedule (56-bit, encryption)
des.h: * \param key      8-byte secret key
des.h: * \brief          DES key schedule (56-bit, decryption)
des.h: * \param key      8-byte secret key
des.h: * \brief          Triple-DES key schedule (112-bit, encryption)
des.h: * \param key      16-byte secret key
des.h: * \brief          Triple-DES key schedule (112-bit, decryption)
des.h: * \param key      16-byte secret key
des.h: * \brief          Triple-DES key schedule (168-bit, encryption)
des.h: * \param key      24-byte secret key
des.h: * \brief          Triple-DES key schedule (168-bit, decryption)
des.h: * \param key      24-byte secret key
des.h: * \brief          DES-ECB block encryption/decryption
des.h: * \param input    64-bit input block
des.h: * \param output   64-bit output block
des.h: * \brief          DES-CBC buffer encryption/decryption
des.h: * \brief          3DES-ECB block encryption/decryption
des.h: * \param input    64-bit input block
des.h: * \param output   64-bit output block
des.h: * \brief          3DES-CBC buffer encryption/decryption
ssl_ticket.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
ssl_ticket.h:    /** Callback for getting (pseudo-)random numbers                        */
ssl_ticket.h: *                  supported. Usually that means a 256-bit key.
ssl_ticket.h: * \note            \c nlength must match sizeof( ctx->name )
private_access.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
cipher.h: * \author Adriaan de Jong <dejong@fox-it.com>
cipher.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
cipher.h:#define MBEDTLS_ERR_CIPHER_FEATURE_UNAVAILABLE  -0x6080
cipher.h:#define MBEDTLS_ERR_CIPHER_BAD_INPUT_DATA       -0x6100
cipher.h:#define MBEDTLS_ERR_CIPHER_ALLOC_FAILED         -0x6180
cipher.h:#define MBEDTLS_ERR_CIPHER_INVALID_PADDING      -0x6200
cipher.h:#define MBEDTLS_ERR_CIPHER_FULL_BLOCK_EXPECTED  -0x6280
cipher.h:#define MBEDTLS_ERR_CIPHER_AUTH_FAILED          -0x6300
cipher.h:#define MBEDTLS_ERR_CIPHER_INVALID_CONTEXT      -0x6380
cipher.h:    MBEDTLS_CIPHER_NONE = 0,             /**< Placeholder to mark the end of cipher-pair lists. */
cipher.h:    MBEDTLS_CIPHER_AES_128_ECB,          /**< AES cipher with 128-bit ECB mode. */
cipher.h:    MBEDTLS_CIPHER_AES_192_ECB,          /**< AES cipher with 192-bit ECB mode. */
cipher.h:    MBEDTLS_CIPHER_AES_256_ECB,          /**< AES cipher with 256-bit ECB mode. */
cipher.h:    MBEDTLS_CIPHER_AES_128_CBC,          /**< AES cipher with 128-bit CBC mode. */
cipher.h:    MBEDTLS_CIPHER_AES_192_CBC,          /**< AES cipher with 192-bit CBC mode. */
cipher.h:    MBEDTLS_CIPHER_AES_256_CBC,          /**< AES cipher with 256-bit CBC mode. */
cipher.h:    MBEDTLS_CIPHER_AES_128_CFB128,       /**< AES cipher with 128-bit CFB128 mode. */
cipher.h:    MBEDTLS_CIPHER_AES_192_CFB128,       /**< AES cipher with 192-bit CFB128 mode. */
cipher.h:    MBEDTLS_CIPHER_AES_256_CFB128,       /**< AES cipher with 256-bit CFB128 mode. */
cipher.h:    MBEDTLS_CIPHER_AES_128_CTR,          /**< AES cipher with 128-bit CTR mode. */
cipher.h:    MBEDTLS_CIPHER_AES_192_CTR,          /**< AES cipher with 192-bit CTR mode. */
cipher.h:    MBEDTLS_CIPHER_AES_256_CTR,          /**< AES cipher with 256-bit CTR mode. */
cipher.h:    MBEDTLS_CIPHER_AES_128_GCM,          /**< AES cipher with 128-bit GCM mode. */
cipher.h:    MBEDTLS_CIPHER_AES_192_GCM,          /**< AES cipher with 192-bit GCM mode. */
cipher.h:    MBEDTLS_CIPHER_AES_256_GCM,          /**< AES cipher with 256-bit GCM mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_128_ECB,     /**< Camellia cipher with 128-bit ECB mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_192_ECB,     /**< Camellia cipher with 192-bit ECB mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_256_ECB,     /**< Camellia cipher with 256-bit ECB mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_128_CBC,     /**< Camellia cipher with 128-bit CBC mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_192_CBC,     /**< Camellia cipher with 192-bit CBC mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_256_CBC,     /**< Camellia cipher with 256-bit CBC mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_128_CFB128,  /**< Camellia cipher with 128-bit CFB128 mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_192_CFB128,  /**< Camellia cipher with 192-bit CFB128 mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_256_CFB128,  /**< Camellia cipher with 256-bit CFB128 mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_128_CTR,     /**< Camellia cipher with 128-bit CTR mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_192_CTR,     /**< Camellia cipher with 192-bit CTR mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_256_CTR,     /**< Camellia cipher with 256-bit CTR mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_128_GCM,     /**< Camellia cipher with 128-bit GCM mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_192_GCM,     /**< Camellia cipher with 192-bit GCM mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_256_GCM,     /**< Camellia cipher with 256-bit GCM mode. */
cipher.h:    MBEDTLS_CIPHER_AES_128_CCM,          /**< AES cipher with 128-bit CCM mode. */
cipher.h:    MBEDTLS_CIPHER_AES_192_CCM,          /**< AES cipher with 192-bit CCM mode. */
cipher.h:    MBEDTLS_CIPHER_AES_256_CCM,          /**< AES cipher with 256-bit CCM mode. */
cipher.h:    MBEDTLS_CIPHER_AES_128_CCM_STAR_NO_TAG, /**< AES cipher with 128-bit CCM_STAR_NO_TAG mode. */
cipher.h:    MBEDTLS_CIPHER_AES_192_CCM_STAR_NO_TAG, /**< AES cipher with 192-bit CCM_STAR_NO_TAG mode. */
cipher.h:    MBEDTLS_CIPHER_AES_256_CCM_STAR_NO_TAG, /**< AES cipher with 256-bit CCM_STAR_NO_TAG mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_128_CCM,     /**< Camellia cipher with 128-bit CCM mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_192_CCM,     /**< Camellia cipher with 192-bit CCM mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_256_CCM,     /**< Camellia cipher with 256-bit CCM mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_128_CCM_STAR_NO_TAG, /**< Camellia cipher with 128-bit CCM_STAR_NO_TAG mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_192_CCM_STAR_NO_TAG, /**< Camellia cipher with 192-bit CCM_STAR_NO_TAG mode. */
cipher.h:    MBEDTLS_CIPHER_CAMELLIA_256_CCM_STAR_NO_TAG, /**< Camellia cipher with 256-bit CCM_STAR_NO_TAG mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_128_ECB,         /**< Aria cipher with 128-bit key and ECB mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_192_ECB,         /**< Aria cipher with 192-bit key and ECB mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_256_ECB,         /**< Aria cipher with 256-bit key and ECB mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_128_CBC,         /**< Aria cipher with 128-bit key and CBC mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_192_CBC,         /**< Aria cipher with 192-bit key and CBC mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_256_CBC,         /**< Aria cipher with 256-bit key and CBC mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_128_CFB128,      /**< Aria cipher with 128-bit key and CFB-128 mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_192_CFB128,      /**< Aria cipher with 192-bit key and CFB-128 mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_256_CFB128,      /**< Aria cipher with 256-bit key and CFB-128 mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_128_CTR,         /**< Aria cipher with 128-bit key and CTR mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_192_CTR,         /**< Aria cipher with 192-bit key and CTR mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_256_CTR,         /**< Aria cipher with 256-bit key and CTR mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_128_GCM,         /**< Aria cipher with 128-bit key and GCM mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_192_GCM,         /**< Aria cipher with 192-bit key and GCM mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_256_GCM,         /**< Aria cipher with 256-bit key and GCM mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_128_CCM,         /**< Aria cipher with 128-bit key and CCM mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_192_CCM,         /**< Aria cipher with 192-bit key and CCM mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_256_CCM,         /**< Aria cipher with 256-bit key and CCM mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_128_CCM_STAR_NO_TAG, /**< Aria cipher with 128-bit key and CCM_STAR_NO_TAG mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_192_CCM_STAR_NO_TAG, /**< Aria cipher with 192-bit key and CCM_STAR_NO_TAG mode. */
cipher.h:    MBEDTLS_CIPHER_ARIA_256_CCM_STAR_NO_TAG, /**< Aria cipher with 256-bit key and CCM_STAR_NO_TAG mode. */
cipher.h:    MBEDTLS_CIPHER_AES_128_OFB,          /**< AES 128-bit cipher in OFB mode. */
cipher.h:    MBEDTLS_CIPHER_AES_192_OFB,          /**< AES 192-bit cipher in OFB mode. */
cipher.h:    MBEDTLS_CIPHER_AES_256_OFB,          /**< AES 256-bit cipher in OFB mode. */
cipher.h:    MBEDTLS_CIPHER_AES_128_XTS,          /**< AES 128-bit cipher in XTS block mode. */
cipher.h:    MBEDTLS_CIPHER_AES_256_XTS,          /**< AES 256-bit cipher in XTS block mode. */
cipher.h:    MBEDTLS_CIPHER_CHACHA20_POLY1305,    /**< ChaCha20-Poly1305 AEAD cipher. */
cipher.h:    MBEDTLS_CIPHER_AES_128_KW,           /**< AES cipher with 128-bit NIST KW mode. */
cipher.h:    MBEDTLS_CIPHER_AES_192_KW,           /**< AES cipher with 192-bit NIST KW mode. */
cipher.h:    MBEDTLS_CIPHER_AES_256_KW,           /**< AES cipher with 256-bit NIST KW mode. */
cipher.h:    MBEDTLS_CIPHER_AES_128_KWP,          /**< AES cipher with 128-bit NIST KWP mode. */
cipher.h:    MBEDTLS_CIPHER_AES_192_KWP,          /**< AES cipher with 192-bit NIST KWP mode. */
cipher.h:    MBEDTLS_CIPHER_AES_256_KWP,          /**< AES cipher with 256-bit NIST KWP mode. */
cipher.h:    MBEDTLS_MODE_CCM_STAR_NO_TAG,        /**< The CCM*-no-tag cipher mode. */
cipher.h:    MBEDTLS_MODE_CHACHAPOLY,             /**< The ChaCha-Poly cipher mode. */
cipher.h:    MBEDTLS_MODE_KW,                     /**< The SP800-38F KW mode */
cipher.h:    MBEDTLS_MODE_KWP,                    /**< The SP800-38F KWP mode */
cipher.h:    MBEDTLS_PADDING_ONE_AND_ZEROS, /**< ISO/IEC 7816-4 padding.         */
cipher.h:    MBEDTLS_OPERATION_NONE = -1,
cipher.h:    /** Key length in bits, including parity, for DES in two-key EDE. \warning 3DES is considered weak. */
cipher.h:    /** Key length in bits, including parity, for DES in three-key EDE. \warning 3DES is considered weak. */
cipher.h: *              only built-in structures returned by the functions
cipher.h: * \note        Some fields store a value that has been right-shifted to save
cipher.h: *              code-size, so should not be used directly. The accessor
cipher.h:    /** Current IV or NONCE_COUNTER for CTR-mode, data unit (or sector) number
cipher.h:     * for XTS-mode. */
cipher.h:    /** IV size in Bytes, for ciphers with variable-length IVs. */
cipher.h:    /** The cipher-specific context. */
cipher.h:    /** CMAC-specific context. */
cipher.h: * \return      A statically-allocated array of cipher identifiers
cipher.h: * \brief               This function retrieves the cipher-information
cipher.h: * \brief               This function retrieves the cipher-information
cipher.h: * \brief               This function retrieves the cipher-information
cipher.h:        return (mbedtls_cipher_type_t) info->MBEDTLS_PRIVATE(type);
cipher.h:        return (mbedtls_cipher_mode_t) info->MBEDTLS_PRIVATE(mode);
cipher.h: *                      For variable-sized ciphers, this is the default length.
cipher.h:        return ((size_t) info->MBEDTLS_PRIVATE(key_bitlen)) << MBEDTLS_KEY_BITLEN_SHIFT;
cipher.h: * \brief               Retrieve the human-readable name for a
cipher.h:        return info->MBEDTLS_PRIVATE(name);
cipher.h:    return ((size_t) info->MBEDTLS_PRIVATE(iv_size)) << MBEDTLS_IV_SIZE_SHIFT;
cipher.h:    return (size_t) (info->MBEDTLS_PRIVATE(block_size));
cipher.h: * \brief        This function returns a non-zero value if the key length for
cipher.h: * \return       Non-zero if the key length is variable, \c 0 otherwise.
cipher.h:    return info->MBEDTLS_PRIVATE(flags) & MBEDTLS_CIPHER_VARIABLE_KEY_LEN;
cipher.h: * \brief        This function returns a non-zero value if the IV size for
cipher.h: * \return       Non-zero if the IV size is variable, \c 0 otherwise.
cipher.h:    return info->MBEDTLS_PRIVATE(flags) & MBEDTLS_CIPHER_VARIABLE_IV_LEN;
cipher.h: * \brief               This function frees and clears the cipher-specific
cipher.h: *                      - mbedtls_cipher_crypt() for one-shot processing with
cipher.h: *                      non-AEAD modes;
cipher.h: *                      - mbedtls_cipher_auth_encrypt_ext() or
cipher.h: *                      mbedtls_cipher_auth_decrypt_ext() for one-shot
cipher.h: *                      - for multi-part processing, see the documentation of
cipher.h: *                      parameter-verification failure.
cipher.h: *                      cipher-specific context fails.
cipher.h: *                      PSA-based use with the given cipher primitive.
cipher.h: *                      For non-AEAD ciphers, the value must be \c 0.
cipher.h: *                      parameter-verification failure.
cipher.h: *                      cipher-specific context fails.
cipher.h:    if (ctx->MBEDTLS_PRIVATE(cipher_info) == NULL) {
cipher.h:    return (unsigned int) ctx->MBEDTLS_PRIVATE(cipher_info)->MBEDTLS_PRIVATE(block_size);
cipher.h:    if (ctx->MBEDTLS_PRIVATE(cipher_info) == NULL) {
cipher.h:    return (mbedtls_cipher_mode_t) ctx->MBEDTLS_PRIVATE(cipher_info)->MBEDTLS_PRIVATE(mode);
cipher.h:    if (ctx->MBEDTLS_PRIVATE(cipher_info) == NULL) {
cipher.h:    if (ctx->MBEDTLS_PRIVATE(iv_size) != 0) {
cipher.h:        return (int) ctx->MBEDTLS_PRIVATE(iv_size);
cipher.h:    return (int) (((int) ctx->MBEDTLS_PRIVATE(cipher_info)->MBEDTLS_PRIVATE(iv_size)) <<
cipher.h:    if (ctx->MBEDTLS_PRIVATE(cipher_info) == NULL) {
cipher.h:    return (mbedtls_cipher_type_t) ctx->MBEDTLS_PRIVATE(cipher_info)->MBEDTLS_PRIVATE(type);
cipher.h:    if (ctx->MBEDTLS_PRIVATE(cipher_info) == NULL) {
cipher.h:    return ctx->MBEDTLS_PRIVATE(cipher_info)->MBEDTLS_PRIVATE(name);
cipher.h:    if (ctx->MBEDTLS_PRIVATE(cipher_info) == NULL) {
cipher.h:    return (int) ctx->MBEDTLS_PRIVATE(cipher_info)->MBEDTLS_PRIVATE(key_bitlen) <<
cipher.h:    if (ctx->MBEDTLS_PRIVATE(cipher_info) == NULL) {
cipher.h:    return ctx->MBEDTLS_PRIVATE(operation);
cipher.h: *                      parameter-verification failure.
cipher.h: * \return              A cipher-specific error code on failure.
cipher.h: * \param iv        The IV to use, or NONCE_COUNTER for CTR-mode ciphers. This
cipher.h: * \param iv_len    The IV length for ciphers with variable-size IV.
cipher.h: *                  This parameter is discarded by ciphers with fixed-size IV.
cipher.h: *                  parameter-verification failure.
cipher.h: * \note          With non-AEAD ciphers, the order of calls for each message
cipher.h: *                parameter-verification failure.
cipher.h: *                      many block-sized blocks of data as possible to output.
cipher.h: *                      parameter-verification failure.
cipher.h: * \return              A cipher-specific error code on failure.
cipher.h: *                      parameter-verification failure.
cipher.h: * \return              A cipher-specific error code on failure.
cipher.h: * \brief               The generic all-in-one encryption/decryption function,
cipher.h: * \param iv            The IV to use, or NONCE_COUNTER for CTR-mode ciphers.
cipher.h: * \param iv_len        The IV length for ciphers with variable-size IV.
cipher.h: *                      This parameter is discarded by ciphers with fixed-size
cipher.h: *                      parameter-verification failure.
cipher.h: * \return              A cipher-specific error code on failure.
cipher.h: *                      parameter-verification failure.
cipher.h: * \return              A cipher-specific error code on failure.
cipher.h: *                      ciphers, this must be at least \p ilen - \p tag_len.
cipher.h: *                      For NIST_KW, this must be at least \p ilen - 8.
cipher.h: *                      parameter-verification failure.
cipher.h: * \return              A cipher-specific error code on failure.
pk.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
pk.h:#define MBEDTLS_ERR_PK_ALLOC_FAILED        -0x3F80
pk.h:#define MBEDTLS_ERR_PK_TYPE_MISMATCH       -0x3F00
pk.h:#define MBEDTLS_ERR_PK_BAD_INPUT_DATA      -0x3E80
pk.h:#define MBEDTLS_ERR_PK_FILE_IO_ERROR       -0x3E00
pk.h:#define MBEDTLS_ERR_PK_KEY_INVALID_VERSION -0x3D80
pk.h:#define MBEDTLS_ERR_PK_KEY_INVALID_FORMAT  -0x3D00
pk.h:#define MBEDTLS_ERR_PK_UNKNOWN_PK_ALG      -0x3C80
pk.h:#define MBEDTLS_ERR_PK_PASSWORD_REQUIRED   -0x3C00
pk.h:#define MBEDTLS_ERR_PK_PASSWORD_MISMATCH   -0x3B80
pk.h:#define MBEDTLS_ERR_PK_INVALID_PUBKEY      -0x3B00
pk.h:#define MBEDTLS_ERR_PK_INVALID_ALG         -0x3A80
pk.h:#define MBEDTLS_ERR_PK_UNKNOWN_NAMED_CURVE -0x3A00
pk.h:#define MBEDTLS_ERR_PK_FEATURE_UNAVAILABLE -0x3980
pk.h:#define MBEDTLS_ERR_PK_SIG_LEN_MISMATCH    -0x3900
pk.h:#define MBEDTLS_ERR_PK_BUFFER_TOO_SMALL    -0x3880
pk.h: * \brief           Options for RSASSA-PSS signature verification.
pk.h: * - if USE_PSA is not defined and ECP_C is defined then use ecp_keypair data
pk.h: * - if USE_PSA is defined and
pk.h: *     - if ECP_C then use ecp_keypair structure, convert data to a PSA friendly
pk.h: *     - if !ECP_C then use new raw data and PSA functions directly.
pk.h: *              only built-in structures returned by
pk.h:     * - opaque key when MBEDTLS_USE_PSA_CRYPTO is defined
pk.h:     * - normal key when MBEDTLS_PK_USE_PSA_EC_DATA is defined. In this case:
pk.h:     *    - the pk_ctx above is not not used to store the private key anymore.
pk.h:     *    - this ID is used for all private key operations (ex: sign, check
pk.h:     * - easily importing it into the PSA context
pk.h:     * - reducing the ECP module dependencies in the PK one.
pk.h:     * - the pk_ctx above is not used anymore for storing the public key
pk.h:     * - the following fields are used for all public key operations: signature
pk.h:     * - For a key pair, priv_id contains the private key. For a public key,
pk.h: * \brief           Types for RSA-alt abstraction
pk.h: *                  and allocates the type-specific PK subcontext.
pk.h: * \note            For contexts holding an RSA-alt key, use
pk.h: * \brief           Initialize an RSA-alt context
pk.h: * \note            This function replaces \c mbedtls_pk_setup() for RSA-alt.
pk.h: *                  be taken in account for non-allowed algorithms and usage
pk.h: *                  type, for non-allowed algorithms and usage flags, or
pk.h: * \note            This function does not support RSA-alt contexts
pk.h: *                  - #PSA_KEY_USAGE_DECRYPT: \p pk must contain a
pk.h: *                  - #PSA_KEY_USAGE_DERIVE: \p pk must contain a
pk.h: *                  - #PSA_KEY_USAGE_ENCRYPT: The output
pk.h: *                  - #PSA_KEY_USAGE_SIGN_HASH: \p pk must contain a
pk.h: *                  - #PSA_KEY_USAGE_SIGN_MESSAGE: \p pk must contain a
pk.h: *                  - #PSA_KEY_USAGE_VERIFY_HASH: The output
pk.h: *                  - #PSA_KEY_USAGE_VERIFY_MESSAGE: The output
pk.h: *                  - The lifetime and key identifier are unchanged. If the
pk.h: *                  - The key type and bit-size are determined by the contents
pk.h: *                  - The key's policy is determined by the key type and
pk.h: *                      - #MBEDTLS_PK_RSA whose underlying
pk.h: *                      - #MBEDTLS_PK_RSA whose underlying
pk.h: *                      - #MBEDTLS_PK_RSA_ALT: not supported.
pk.h: *                      - #MBEDTLS_PK_ECDSA or #MBEDTLS_PK_ECKEY
pk.h: *                      - #MBEDTLS_PK_ECKEY_DH or #MBEDTLS_PK_ECKEY
pk.h: *                      - #MBEDTLS_PK_OPAQUE: same as the primary algorithm
pk.h: * -# Call mbedtls_pk_get_psa_attributes() to obtain
pk.h: * -# If desired, modify the attributes, for example:
pk.h: *     - To create a persistent key, call
pk.h: *     - To import only the public part of a key pair:
pk.h: *     - Restrict the key usage if desired.
pk.h: * -# Call mbedtls_pk_import_into_psa().
pk.h: * \note            This function does not support RSA-alt contexts
pk.h: *                  - must be exportable and
pk.h: *                  - must be an RSA or EC key pair or public key (FFDH is not supported in PK).
pk.h: *                  - #MBEDTLS_PK_RSA for RSA keys or
pk.h: *                  - #MBEDTLS_PK_ECKEY for EC keys.
pk.h: *                    be based on the compile-time setting #MBEDTLS_ECDSA_DETERMINISTIC.
pk.h: *                  - #MBEDTLS_PK_RSA for RSA keys or
pk.h: *                  - #MBEDTLS_PK_ECKEY for EC keys.
pk.h: *                  does not rely on a hash algorithm (non-deterministic
pk.h: * \param options   Pointer to type-specific options, or NULL
pk.h: * \brief           Check if a public-private pair of keys matches.
pk.h: *                  be checked - in that case they may or may not match.
pk.h: * \return          Another non-zero value if the keys do not match.
pk.h: *                  contain a null-terminated string.
pk.h: *                  Pass \c NULL if expecting a non-encrypted key.
pk.h: *                  key; a non-encrypted key will also be accepted.
pk.h: *                  contain a null-terminated string.
pk.h: *                  Pass \c NULL if expecting a non-encrypted key.
pk.h: *                  Pass a null-terminated string if expecting an encrypted
pk.h: *                  key; a non-encrypted key will also be accepted.
pk.h: * WARNING: Low-level functions. You probably do not want to use these unless
pk.h: * \param start     start of the buffer (for bounds-checking)
memory_buffer_alloc.h: * \brief Buffer-based memory allocator
memory_buffer_alloc.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
memory_buffer_alloc.h: * \brief   Initialize use of stack-based memory allocator.
memory_buffer_alloc.h: *          The stack-based allocator does memory management inside the
memory_buffer_alloc.h: *          (Provided mbedtls_calloc() and mbedtls_free() are thread-safe if
memory_buffer_alloc.h: * \note    This code is not optimized and provides a straight-forward
memory_buffer_alloc.h: *          implementation of a stack-based memory allocator.
memory_buffer_alloc.h: * \brief   Free the mutex for thread-safety and clear remaining memory
memory_buffer_alloc.h: *          of the entire chain of headers / meta-data.
memory_buffer_alloc.h: *          after a program should have de-allocated all memory)
memory_buffer_alloc.h: *          and contain sane values. Helps debug buffer-overflow errors.
ctr_drbg.h: * CTR_DRBG is a standardized way of building a PRNG from a block-cipher
ctr_drbg.h: * in counter mode operation, as defined in <em>NIST SP 800-90A:
ctr_drbg.h: * The Mbed TLS implementation of CTR_DRBG uses AES-256 (default) or AES-128
ctr_drbg.h: * The security strength as defined in NIST SP 800-90A is
ctr_drbg.h: * 128 bits when AES-128 is used (\c MBEDTLS_CTR_DRBG_USE_128_BIT_KEY enabled)
ctr_drbg.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
ctr_drbg.h:/* The CTR_DRBG implementation can either directly call the low-level AES
ctr_drbg.h:#define MBEDTLS_ERR_CTR_DRBG_ENTROPY_SOURCE_FAILED        -0x0034
ctr_drbg.h:#define MBEDTLS_ERR_CTR_DRBG_REQUEST_TOO_BIG              -0x0036
ctr_drbg.h:#define MBEDTLS_ERR_CTR_DRBG_INPUT_TOO_BIG                -0x0038
ctr_drbg.h:#define MBEDTLS_ERR_CTR_DRBG_FILE_IO_ERROR                -0x003A
ctr_drbg.h: * Compile-time choice: 16 bytes (128 bits)
ctr_drbg.h: * Compile-time choice: 32 bytes (256 bits)
ctr_drbg.h:/** This is 48 bytes because the entropy module uses SHA-512.
ctr_drbg.h:/** This is 32 bytes because the entropy module uses SHA-256.
ctr_drbg.h:/** \warning To achieve a 256-bit security strength, you must pass a nonce
ctr_drbg.h:                                                  * or -1 if no nonce length has been explicitly
ctr_drbg.h: * - \p f_entropy is mbedtls_entropy_func();
ctr_drbg.h: * - \p p_entropy is an instance of ::mbedtls_entropy_context initialized
ctr_drbg.h: * - \c 0 if the entropy length is at least 3/2 times the entropy length,
ctr_drbg.h: *   by the key size and entropy length according to NIST SP 800-90A §10.2.1;
ctr_drbg.h: * - Half the entropy length otherwise.
ctr_drbg.h: * See SP 800-90A §8.6.7 for more details about nonces.
ctr_drbg.h: * the CTR_DRBG Instantiate Process described in NIST SP 800-90A §10.2.1.3.2
ctr_drbg.h: * - A string obtained by calling \p f_entropy function for the entropy
ctr_drbg.h: * - If mbedtls_ctr_drbg_set_nonce_len() has been called, a string
ctr_drbg.h: * - A string obtained by calling \p f_entropy function for the entropy nonce
ctr_drbg.h: *                      reseeding, are not thread-safe.
ctr_drbg.h: * - The \p custom string.
ctr_drbg.h: *                      - at least 16 bytes for a 128-bit strength
ctr_drbg.h: *                      (maximum achievable strength when using AES-128);
ctr_drbg.h: *                      - at least 32 bytes for a 256-bit strength
ctr_drbg.h: *                      (maximum achievable strength when using AES-256).
ctr_drbg.h: *                      - at least 24 bytes for a 128-bit strength
ctr_drbg.h: *                      (maximum achievable strength when using AES-128);
ctr_drbg.h: *                      - at least 48 bytes for a 256-bit strength
ctr_drbg.h: *                      (maximum achievable strength when using AES-256).
ctr_drbg.h: *                      - #MBEDTLS_CTR_DRBG_ENTROPY_LEN.
ctr_drbg.h: *                      - When using AES-256
ctr_drbg.h: *                        to achieve a 256-bit strength.
ctr_drbg.h: *                      - When using AES-128
ctr_drbg.h: *                        to achieve a 128-bit strength.
ctr_drbg.h: * \note                This function is not thread-safe. It is not safe
ctr_drbg.h: *                      #MBEDTLS_CTR_DRBG_MAX_SEED_INPUT - \c entropy_len
ctr_drbg.h: * \note                This function is not thread-safe. It is not safe
ctr_drbg.h: * \note                This function is not thread-safe. It is not safe
ctr_drbg.h: *                      #MBEDTLS_CTR_DRBG_MAX_SEED_INPUT - \c entropy_len
ctr_drbg.h: *                      reseeding, are not thread-safe.
x509_crt.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
x509_crt.h:    void *MBEDTLS_PRIVATE(sig_opts);             /**< Signature options to be passed to mbedtls_pk_verify_ext(), e.g. for RSASSA-PSS */
x509_crt.h:#define MBEDTLS_X509_ID_FLAG(id)   (1 << ((id) - 1))
x509_crt.h: * For example to allow SHA-224 in addition to the default:
x509_crt.h: * Or to allow only RSA-3072+ with SHA-256:
x509_crt.h: * Max size of verification chain: end-entity + intermediates + trusted root
x509_crt.h:    mbedtls_x509_crt *MBEDTLS_PRIVATE(parent); /* non-null iff parent_in in progress */
x509_crt.h:    int MBEDTLS_PRIVATE(parent_is_trusted); /* -1 if find_parent is not in progress */
x509_crt.h: * - SHA2 hashes with at least 256 bits: SHA-256, SHA-384, SHA-512.
x509_crt.h: * - Elliptic curves with 255 bits and above except secp256k1.
x509_crt.h: * - RSA with 2048 bits and above.
x509_crt.h: * Currently targets a 128-bit security level, except for allowing RSA-2048.
x509_crt.h: * \brief          Parse one DER-encoded or one or more concatenated PEM-encoded
x509_crt.h: *                 '-----{BEGIN/END} CERTIFICATE-----' delimiters.
x509_crt.h: *                 The verify callback is a user-supplied callback that
x509_crt.h: *                 certificate in the chain (from the trust-ca down to the
x509_crt.h: *                 other than fatal error, as a non-zero return code
x509_crt.h: * \note           It is your responsibility to provide up-to-date CRLs for
x509_crt.h: *                 self-signed end-entity certificates to be trusted (for
x509_crt.h: *                 specific peers you know) - in that case, the self-signed
x509_crt.h: *                 set to (uint32_t) -1.
x509_crt.h: *                 set to (uint32_t) -1.
x509_crt.h: *                 set to (uint32_t) -1.
x509_crt.h: * \note                The callback must only return a non-zero value on a
x509_crt.h: *                      to a heap-allocated linked list of instances of
x509_crt.h: *                 set to (uint32_t) -1.
x509_crt.h: *                      non-zero otherwise
x509_crt.h:    return ctx->MBEDTLS_PRIVATE(ext_types) & ext_type;
x509_crt.h: *                  Issuer names should contain a comma-separated list
x509_crt.h: *                  Subject names should contain a comma-separated list
x509_crt.h: *                      certificate (only for CA certificates, -1 is
build_info.h: * \brief Build-time configuration info
build_info.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
build_info.h: * This set of compile-time defines can be used to determine the version number
build_info.h: * of the Mbed TLS library used. Run-time variables for the same can be found in
build_info.h:/* Macros for build-time platform detection */
build_info.h:/* This is defined if the architecture is Armv8-A, or higher */
build_info.h:/* MSVC ARM64 is at least Armv8.0-A */
build_info.h:/* Define `inline` on some non-C99-compliant compilers. */
build_info.h:/* X.509, TLS and non-PSA crypto configuration */
build_info.h:/* Auto-enable MBEDTLS_CTR_DRBG_USE_128_BIT_KEY if
build_info.h: * to ensure a 128-bit key size in CTR_DRBG.
build_info.h:/* Auto-enable MBEDTLS_MD_C if needed by a module that didn't require it
build_info.h: * - If config_psa.h reads a configuration option in preprocessor directive,
build_info.h: * - If config_psa.h writes a configuration option in conditional directive,
bignum.h: * \brief Multi-precision integer library
bignum.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
bignum.h:#define MBEDTLS_ERR_MPI_FILE_IO_ERROR                     -0x0002
bignum.h:#define MBEDTLS_ERR_MPI_BAD_INPUT_DATA                    -0x0004
bignum.h:#define MBEDTLS_ERR_MPI_INVALID_CHARACTER                 -0x0006
bignum.h:#define MBEDTLS_ERR_MPI_BUFFER_TOO_SMALL                  -0x0008
bignum.h:#define MBEDTLS_ERR_MPI_NEGATIVE_VALUE                    -0x000A
bignum.h:#define MBEDTLS_ERR_MPI_DIVISION_BY_ZERO                  -0x000C
bignum.h:#define MBEDTLS_ERR_MPI_NOT_ACCEPTABLE                    -0x000E
bignum.h:#define MBEDTLS_ERR_MPI_ALLOC_FAILED                      -0x0010
bignum.h: * Maximum size of MPIs allowed in bits and bytes for user-MPIs.
bignum.h:                                                  MBEDTLS_LN_2_DIV_LN_10_SCALE100 - 1) / \
bignum.h: * Define the base integer type, architecture-wise.
bignum.h: * 32 or 64-bit integer types can be forced regardless of the underlying
bignum.h: * Double-width integers (e.g. 128-bit in 64-bit architectures) can be
bignum.h:/* Always choose 64-bit when using MSC */
bignum.h:/* mbedtls_t_udbl defined as 128-bit unsigned int */
bignum.h: * __aarch64__ is only defined by armclang when compiling 64-bit code
bignum.h:/* mbedtls_t_udbl defined as 128-bit unsigned int */
bignum.h:/* Force 64-bit integers with unknown compiler */
bignum.h:/* Default to 32-bit compilation */
bignum.h:#error "Only 32-bit or 64-bit limbs are supported in bignum"
bignum.h: * is platform-dependent.
bignum.h: * is platform-dependent.
bignum.h:    /** Sign: -1 if the mpi is negative, 1 otherwise.
bignum.h:     * functions treat all-limbs-zero as equivalent to a valid representation
bignum.h: *                 in which case this function is a no-op. If it is
bignum.h: * \note           The limb-buffer in the destination MPI is enlarged
bignum.h: * \param pos      Zero-based index of the bit to query.
bignum.h: * \param pos      Zero-based index of the bit to modify.
bignum.h: * \note           This is the same as the zero-based index of
bignum.h: * * \note         This is same as the one-based index of the most
bignum.h: * \param s        Null-terminated string buffer.
bignum.h: * \brief          Perform a left-shift on an MPI: X <<= count
bignum.h: * \brief          Perform a right-shift on an MPI: X >>= count
bignum.h: * \param X        The left-hand MPI. This must point to an initialized MPI.
bignum.h: * \param Y        The right-hand MPI. This must point to an initialized MPI.
bignum.h: * \return         \c -1 if `|X|` is lesser than `|Y|`.
bignum.h: * \param X        The left-hand MPI. This must point to an initialized MPI.
bignum.h: * \param Y        The right-hand MPI. This must point to an initialized MPI.
bignum.h: * \return         \c -1 if \p X is lesser than \p Y.
bignum.h: * \param X        The left-hand MPI. This must point to an initialized MPI
bignum.h: * \param Y        The right-hand MPI. This must point to an initialized MPI
bignum.h: * \param X        The left-hand MPI. This must point to an initialized MPI.
bignum.h: * \return         \c -1 if \p X is lesser than \p z.
bignum.h: * \brief          Perform an unsigned subtraction of MPIs: X = |A| - |B|
bignum.h: * \brief          Perform a signed subtraction of MPIs: X = A - B
bignum.h: *                 X = A - b
bignum.h: *                 speed-up multiple modular exponentiations for the same value
bignum.h: *                 as a big-endian representation of an MPI; this can
bignum.h: * \note           There are `N - min` possible outputs. The lower bound
bignum.h: * \brief          Compute the modular inverse: X = A^-1 mod N
bignum.h: * \brief          Miller-Rabin primality test.
bignum.h: *                 if \p X is chosen uniformly or non-adversarially (as is the
bignum.h: * \param rounds   The number of bases to perform the Miller-Rabin primality
bignum.h: *                 at most 2<sup>-2*\p rounds </sup>.
bignum.h:    MBEDTLS_MPI_GEN_PRIME_FLAG_DH =      0x0001, /**< (X-1)/2 is prime too */
bignum.h:    MBEDTLS_MPI_GEN_PRIME_FLAG_LOW_ERR = 0x0002, /**< lower error rate from 2<sup>-80</sup> to 2<sup>-128</sup> */
config_adjust_ssl.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
aria.h: *        Korean, but see http://210.104.33.10/ARIA/index-e.html in English)
aria.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
aria.h:#define MBEDTLS_ERR_ARIA_BAD_INPUT_DATA -0x005C
aria.h:#define MBEDTLS_ERR_ARIA_INVALID_INPUT_LENGTH -0x005E
aria.h: * \brief The ARIA context-type definition.
aria.h: * \brief          This function performs an ARIA single-block encryption or
aria.h: * \param input    The 16-Byte buffer holding the input data.
aria.h: * \param output   The 16-Byte buffer holding the output data.
aria.h: * \brief  This function performs an ARIA-CBC encryption or decryption operation
aria.h: * \brief This function performs an ARIA-CFB128 encryption or decryption
aria.h: * \brief      This function performs an ARIA-CTR encryption or decryption
aria.h: *             per-message nonce, handled by yourself, and the second one
aria.h: *             per-message nonce, and the last 4 bytes for internal use. In that
aria.h: *             The per-message nonce (or information sufficient to reconstruct
aria.h: *             for example, with 96-bit random nonces, you should not encrypt
aria.h: * \param nonce_counter    The 128-bit nonce and counter. This must point to
x509.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
x509.h: * That is, maximum length of the chain, excluding the end-entity certificate
x509.h:#define MBEDTLS_ERR_X509_FEATURE_UNAVAILABLE              -0x2080
x509.h:#define MBEDTLS_ERR_X509_UNKNOWN_OID                      -0x2100
x509.h:#define MBEDTLS_ERR_X509_INVALID_FORMAT                   -0x2180
x509.h:#define MBEDTLS_ERR_X509_INVALID_VERSION                  -0x2200
x509.h:#define MBEDTLS_ERR_X509_INVALID_SERIAL                   -0x2280
x509.h:#define MBEDTLS_ERR_X509_INVALID_ALG                      -0x2300
x509.h:#define MBEDTLS_ERR_X509_INVALID_NAME                     -0x2380
x509.h:#define MBEDTLS_ERR_X509_INVALID_DATE                     -0x2400
x509.h:#define MBEDTLS_ERR_X509_INVALID_SIGNATURE                -0x2480
x509.h:#define MBEDTLS_ERR_X509_INVALID_EXTENSIONS               -0x2500
x509.h:#define MBEDTLS_ERR_X509_UNKNOWN_VERSION                  -0x2580
x509.h:#define MBEDTLS_ERR_X509_UNKNOWN_SIG_ALG                  -0x2600
x509.h:#define MBEDTLS_ERR_X509_SIG_MISMATCH                     -0x2680
x509.h:#define MBEDTLS_ERR_X509_CERT_VERIFY_FAILED               -0x2700
x509.h:#define MBEDTLS_ERR_X509_CERT_UNKNOWN_FORMAT              -0x2780
x509.h:#define MBEDTLS_ERR_X509_BAD_INPUT_DATA                   -0x2800
x509.h:#define MBEDTLS_ERR_X509_ALLOC_FAILED                     -0x2880
x509.h:#define MBEDTLS_ERR_X509_FILE_IO_ERROR                    -0x2900
x509.h:#define MBEDTLS_ERR_X509_BUFFER_TOO_SMALL                 -0x2980
x509.h:#define MBEDTLS_ERR_X509_FATAL_ERROR                      -0x3000
x509.h: * (http://www.mozilla.org/projects/security/pki/nss/tech-notes/tn3.html)
x509.h: * Type-length-value structure that allows for ASN1 using DER.
x509.h: *      type-id    OBJECT IDENTIFIER,
x509.h: *      value      [0] EXPLICIT ANY DEFINED BY type-id }
x509.h: * \note           Intended use is to compare function result to dn->next
x509.h: *                 in order to detect boundaries of multi-valued RDNs.
x509.h: * \return         Pointer to the first attribute-value pair of the
x509.h:    while (dn->MBEDTLS_PRIVATE(next_merged) && dn->next != NULL) {
x509.h:        dn = dn->next;
x509.h:    return dn->next;
x509.h: * \return         A non-zero return value on failure.
x509.h: *                 \c crt->subject_alt_names sequence, and pass it to
x509.h: * \param san      SAN structure - extra memory owned by this structure will be freed
x509.h: * \param cn       The CN string to parse. CN string MUST be null-terminated.
x509.h: *                 4-byte aligned if the result will be used as struct in_addr.
x509.h:        n -= (size_t) ret;                                  \
config_adjust_legacy_from_psa.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
config_adjust_legacy_from_psa.h:/* Define appropriate ACCEL macros for the p256-m driver.
config_adjust_legacy_from_psa.h: * 2. Then enable built-ins for each thing that's either not accelerated of
config_adjust_legacy_from_psa.h:/* ECC: curves: enable built-ins as needed.
config_adjust_legacy_from_psa.h: * We need the curve built-in:
config_adjust_legacy_from_psa.h: * - if it's not accelerated, or
config_adjust_legacy_from_psa.h: * - if there's a key type with missing acceleration, or
config_adjust_legacy_from_psa.h: * - if there's a alg with missing acceleration.
config_adjust_legacy_from_psa.h:/* ECC: algs: enable built-ins as needed.
config_adjust_legacy_from_psa.h: * We need the alg built-in:
config_adjust_legacy_from_psa.h: * - if it's not accelerated, or
config_adjust_legacy_from_psa.h: * - if there's a relevant curve (see below) with missing acceleration, or
config_adjust_legacy_from_psa.h: * - if there's a key type among (public, basic) with missing acceleration.
config_adjust_legacy_from_psa.h: * - all curves for ECDH
config_adjust_legacy_from_psa.h: * - Weierstrass curves for (deterministic) ECDSA
config_adjust_legacy_from_psa.h: * - secp256r1 for EC J-PAKE
config_adjust_legacy_from_psa.h:/* ECC: key types: enable built-ins as needed.
config_adjust_legacy_from_psa.h: * We need the key type built-in:
config_adjust_legacy_from_psa.h: * - if it's not accelerated, or
config_adjust_legacy_from_psa.h: * - if there's a curve with missing acceleration, or
config_adjust_legacy_from_psa.h: * - only for public/basic: if there's an alg with missing acceleration.
camellia.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
camellia.h:#define MBEDTLS_ERR_CAMELLIA_BAD_INPUT_DATA -0x0024
camellia.h:#define MBEDTLS_ERR_CAMELLIA_INVALID_INPUT_LENGTH -0x0026
camellia.h: * \brief          Perform a CAMELLIA-ECB block encryption/decryption operation.
camellia.h: * \brief          Perform a CAMELLIA-CBC buffer encryption/decryption operation.
camellia.h: * \brief          Perform a CAMELLIA-CFB128 buffer encryption/decryption
camellia.h: *                 to this function should be preceded by a key-schedule via
camellia.h: * \brief      Perform a CAMELLIA-CTR buffer encryption/decryption operation.
camellia.h: *             to this function should be preceded by a key-schedule via
camellia.h: *             per-message nonce, handled by yourself, and the second one
camellia.h: *             per-message nonce, and the last \c 4 Bytes for internal use.
camellia.h: *             The per-message nonce (or information sufficient to reconstruct
camellia.h: *             encrypted: for example, with 96-bit random nonces, you should
camellia.h: * \param nonce_counter The 128-bit nonce and counter. This must be a read/write
camellia.h: * \param stream_block  The saved stream-block for resuming. This must be a
net_sockets.h: *          BSD-style sockets API.
net_sockets.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
net_sockets.h:#define MBEDTLS_ERR_NET_SOCKET_FAILED                     -0x0042
net_sockets.h:#define MBEDTLS_ERR_NET_CONNECT_FAILED                    -0x0044
net_sockets.h:#define MBEDTLS_ERR_NET_BIND_FAILED                       -0x0046
net_sockets.h:#define MBEDTLS_ERR_NET_LISTEN_FAILED                     -0x0048
net_sockets.h:#define MBEDTLS_ERR_NET_ACCEPT_FAILED                     -0x004A
net_sockets.h:#define MBEDTLS_ERR_NET_RECV_FAILED                       -0x004C
net_sockets.h:#define MBEDTLS_ERR_NET_SEND_FAILED                       -0x004E
net_sockets.h:#define MBEDTLS_ERR_NET_CONN_RESET                        -0x0050
net_sockets.h:#define MBEDTLS_ERR_NET_UNKNOWN_HOST                      -0x0052
net_sockets.h:#define MBEDTLS_ERR_NET_BUFFER_TOO_SMALL                  -0x0043
net_sockets.h:#define MBEDTLS_ERR_NET_INVALID_CONTEXT                   -0x0045
net_sockets.h:#define MBEDTLS_ERR_NET_POLL_FAILED                       -0x0047
net_sockets.h:#define MBEDTLS_ERR_NET_BAD_INPUT_DATA                    -0x0049
net_sockets.h: * structures for hand-made UDP demultiplexing).
net_sockets.h:     * This field is only guaranteed to be present on POSIX/Unix-like platforms.
net_sockets.h: *                  non-blocking and accept() would block.
net_sockets.h: *                 - If MBEDTLS_NET_POLL_READ is set, the function
net_sockets.h: *                 - If MBEDTLS_NET_POLL_WRITE is set, the function
net_sockets.h: *                 -1u, the function blocks potentially indefinitely.
net_sockets.h: * \return         0 if successful, or a non-zero error code
net_sockets.h: * \brief          Set the socket non-blocking
net_sockets.h: * \return         0 if successful, or a non-zero error code
net_sockets.h: *                 or a non-zero error code; with a non-blocking socket,
net_sockets.h: *                 or a non-zero error code; with a non-blocking socket,
net_sockets.h: *                 non-blocking. Handling timeouts with non-blocking reads
sha3.h: * \brief This file contains SHA-3 definitions and functions.
sha3.h: * hash functions are defined in <em>FIPS 202: SHA-3 Standard:
sha3.h: * Permutation-Based Hash and Extendable-Output Functions </em>.
sha3.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
sha3.h:/** SHA-3 input data was malformed. */
sha3.h:#define MBEDTLS_ERR_SHA3_BAD_INPUT_DATA                 -0x0076
sha3.h: * SHA-3 family id.
sha3.h: * It identifies the family (SHA3-256, SHA3-512, etc.)
sha3.h:    MBEDTLS_SHA3_224, /*!< SHA3-224 */
sha3.h:    MBEDTLS_SHA3_256, /*!< SHA3-256 */
sha3.h:    MBEDTLS_SHA3_384, /*!< SHA3-384 */
sha3.h:    MBEDTLS_SHA3_512, /*!< SHA3-512 */
sha3.h: * \brief          The SHA-3 context structure.
sha3.h: *                 The structure is used SHA-3 checksum calculations.
sha3.h: * \brief          This function initializes a SHA-3 context.
sha3.h: * \param ctx      The SHA-3 context to initialize. This must not be \c NULL.
sha3.h: * \brief          This function clears a SHA-3 context.
sha3.h: * \param ctx      The SHA-3 context to clear. This may be \c NULL, in which
sha3.h: *                 it must point to an initialized SHA-3 context.
sha3.h: * \brief          This function clones the state of a SHA-3 context.
sha3.h: * \brief          This function starts a SHA-3 checksum
sha3.h: * \param id       The id of the SHA-3 family.
sha3.h: *                 SHA-3 checksum calculation.
sha3.h: * \param ctx      The SHA-3 context. This must be initialized
sha3.h: * \brief          This function finishes the SHA-3 operation, and writes
sha3.h: * \param ctx      The SHA-3 context. This must be initialized
sha3.h: * \param output   The SHA-3 checksum result.
sha3.h: * \param olen     Defines the length of output buffer (in bytes). For SHA-3 224, SHA-3 256,
sha3.h: *                 SHA-3 384 and SHA-3 512 \c olen must equal to 28, 32, 48 and 64,
sha3.h: * \brief          This function calculates the SHA-3
sha3.h: *                 The SHA-3 result is calculated as
sha3.h: *                 output = SHA-3(id, input buffer, d).
sha3.h: * \param id       The id of the SHA-3 family.
sha3.h: * \param output   The SHA-3 checksum result.
sha3.h: * \param olen     Defines the length of output buffer (in bytes). For SHA-3 224, SHA-3 256,
sha3.h: *                 SHA-3 384 and SHA-3 512 \c olen must equal to 28, 32, 48 and 64,
sha3.h: *                 by this module: SHA3-224, SHA3-256, SHA3-384, SHA3-512.
debug.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
debug.h: * via -wFormat and other flags. This does nothing on builds with compilers
debug.h: *                          - Debug levels
debug.h: *                              - 0 No debug
debug.h: *                              - 1 Error
debug.h: *                              - 2 State change
debug.h: *                              - 3 Informational
debug.h: *                              - 4 Verbose
ccm.h: * CCM combines Counter mode encryption with CBC-MAC authentication
ccm.h: * for 128-bit block ciphers.
ccm.h: * <ul><li>Payload - data that is both authenticated and encrypted.</li>
ccm.h: * <li>Associated data (Adata) - data that is authenticated but not
ccm.h: * <li>Nonce - A unique value that is assigned to the payload and the
ccm.h: * http://csrc.nist.gov/publications/nistpubs/800-38C/SP800-38C_updated-July20_2007.pdf
ccm.h: * RFC 3610 "Counter with CBC-MAC (CCM)"
ccm.h: * IEEE 802.15.4 - IEEE Standard for Local and metropolitan area networks
ccm.h: * Integer representation is fixed most-significant-octet-first order and
ccm.h: * the representation of octets is most-significant-bit-first order. This is
ccm.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
ccm.h:#define MBEDTLS_ERR_CCM_BAD_INPUT       -0x000D
ccm.h:#define MBEDTLS_ERR_CCM_AUTH_FAILED     -0x000F
ccm.h: * \brief    The CCM context-type definition. The CCM context is passed
ccm.h: * \param cipher    The 128-bit block cipher to use.
ccm.h: * \return          A CCM or cipher-specific error code on failure.
ccm.h: *          and underlying cipher sub-context.
ccm.h: *                  the \p tag with the \p output, as done in <em>RFC-3610:
ccm.h: *                  Counter with CBC-MAC (CCM)</em>, use
ccm.h: *                  15 - \p iv_len.
ccm.h: *                  This must be less than `2^16 - 2^8`.
ccm.h: * \return          A CCM or cipher-specific error code on failure.
ccm.h: *                  the \p tag with the \p output, as done in <em>RFC-3610:
ccm.h: *                  Counter with CBC-MAC (CCM)</em>, use
ccm.h: *                  15 - \p iv_len.
ccm.h: *                  This must be less than 2^16 - 2^8.
ccm.h: * \return          A CCM or cipher-specific error code on failure.
ccm.h: *                  15 - \p iv_len.
ccm.h: *                  This must be less than 2^16 - 2^8.
ccm.h: * \return          A cipher-specific error code on calculation failure.
ccm.h: *                  15 - \p iv_len.
ccm.h: *                  This must be less than 2^16 - 2^8.
ccm.h: * \return          A cipher-specific error code on calculation failure.
ccm.h: *                  15 - \p iv_len.
ccm.h: *                       This must be less than `2^16 - 2^8`.
ccm.h: *                  - Immediate output: the output length is always equal
ccm.h: *                  - Buffered output: except for the last part of input data,
ccm.h: *                    the output consists of a whole number of 16-byte blocks.
ccm.h: *                  - It is always correct to call this function with
ccm.h: *                  - If \p input_len is a multiple of 16 for all the calls
pkcs12.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
pkcs12.h:#define MBEDTLS_ERR_PKCS12_BAD_INPUT_DATA                 -0x1F80
pkcs12.h:#define MBEDTLS_ERR_PKCS12_FEATURE_UNAVAILABLE            -0x1F00
pkcs12.h:#define MBEDTLS_ERR_PKCS12_PBE_INVALID_FORMAT             -0x1E80
pkcs12.h:#define MBEDTLS_ERR_PKCS12_PASSWORD_MISMATCH              -0x1E00
pkcs12.h: *                   for cipher-based and mbedtls_md-based PBE's
pkcs12.h: *                   - if #MBEDTLS_CIPHER_PADDING_PKCS7 is enabled at compile
pkcs12.h: *                     attempting to brute-forcing the password. Note also that
pkcs12.h: *                   - if #MBEDTLS_CIPHER_PADDING_PKCS7 is disabled at compile
pkcs12.h: * \param pbe_params an ASN1 buffer containing the pkcs-12 PbeParams structure
pkcs12.h: * \param pwd        Latin1-encoded password used. This may only be \c NULL when
pkcs12.h: *                   for cipher-based and mbedtls_md-based PBE's
pkcs12.h: *                   - This function validates the CBC padding and returns
pkcs12.h: *                     attempting to brute-forcing the password. Note also that
pkcs12.h: * \param pbe_params an ASN1 buffer containing the pkcs-12 PbeParams structure
pkcs12.h: * \param pwd        Latin1-encoded password used. This may only be \c NULL when
pkcs12.h: *                   to produce pseudo-random bits for a particular "purpose".
pkcs12.h: *                   character is encoded as 2 bytes in big-endian order, with
sha512.h: * \brief This file contains SHA-384 and SHA-512 definitions and functions.
sha512.h: * The Secure Hash Algorithms 384 and 512 (SHA-384 and SHA-512) cryptographic
sha512.h: * hash functions are defined in <em>FIPS 180-4: Secure Hash Standard (SHS)</em>.
sha512.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
sha512.h:/** SHA-512 input data was malformed. */
sha512.h:#define MBEDTLS_ERR_SHA512_BAD_INPUT_DATA                 -0x0075
sha512.h: * \brief          The SHA-512 context structure.
sha512.h: *                 The structure is used both for SHA-384 and for SHA-512
sha512.h:                                                      0: Use SHA-512, or 1: Use SHA-384. */
sha512.h: * \brief          This function initializes a SHA-512 context.
sha512.h: * \param ctx      The SHA-512 context to initialize. This must
sha512.h: * \brief          This function clears a SHA-512 context.
sha512.h: * \param ctx      The SHA-512 context to clear. This may be \c NULL,
sha512.h: *                 SHA-512 context.
sha512.h: * \brief          This function clones the state of a SHA-512 context.
sha512.h: * \brief          This function starts a SHA-384 or SHA-512 checksum
sha512.h: * \param ctx      The SHA-512 context to use. This must be initialized.
sha512.h: *                 either \c 0 for SHA-512, or \c 1 for SHA-384.
sha512.h: *                 SHA-512 checksum calculation.
sha512.h: * \param ctx      The SHA-512 context. This must be initialized
sha512.h: * \brief          This function finishes the SHA-512 operation, and writes
sha512.h: * \param ctx      The SHA-512 context. This must be initialized
sha512.h: * \param output   The SHA-384 or SHA-512 checksum result.
sha512.h: *                 for SHA-512, \c 48 bytes for SHA-384.
sha512.h: *                 the ongoing SHA-512 computation.
sha512.h: * \param ctx      The SHA-512 context. This must be initialized.
sha512.h: * \brief          This function calculates the SHA-512 or SHA-384
sha512.h: *                 The SHA-512 result is calculated as
sha512.h: *                 output = SHA-512(input buffer).
sha512.h: * \param output   The SHA-384 or SHA-512 checksum result.
sha512.h: *                 for SHA-512, \c 48 bytes for SHA-384.
sha512.h: *                 \c 0 for SHA-512, or \c 1 for SHA-384.
sha512.h: *                 - is384 is 0, but \c MBEDTLS_SHA384_C is not defined, or
sha512.h: *                 - is384 is 1, but \c MBEDTLS_SHA512_C is not defined
sha512.h: * \brief          The SHA-384 checkup routine.
sha512.h: * \brief          The SHA-512 checkup routine.
gcm.h: * The Galois/Counter Mode (GCM) for 128-bit block ciphers is defined
gcm.h: * For more information on GCM, see <em>NIST SP 800-38D: Recommendation for
gcm.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
gcm.h:#define MBEDTLS_ERR_GCM_AUTH_FAILED                       -0x0012
gcm.h:#define MBEDTLS_ERR_GCM_BAD_INPUT                         -0x0014
gcm.h:#define MBEDTLS_ERR_GCM_BUFFER_TOO_SMALL                  -0x0016
gcm.h: * \param cipher    The 128-bit block cipher to use.
gcm.h: * \return          A cipher-specific error code on failure.
gcm.h: *                  - #MBEDTLS_GCM_ENCRYPT to perform authenticated encryption.
gcm.h: *                  - #MBEDTLS_GCM_DECRYPT to perform decryption.
gcm.h: *                  not valid or a cipher-specific error code if the encryption
gcm.h: *                  not valid or a cipher-specific error code if the decryption
gcm.h: *                  - Immediate output: the output length is always equal
gcm.h: *                  - Buffered output: the output consists of a whole number
gcm.h: *                    of 16-byte blocks. If the total input length so far
gcm.h: *                  - It is always correct to call this function with
gcm.h: *                  - If \p input_length is a multiple of 16 for all the calls
gcm.h: *                  - If mbedtls_gcm_update() produces immediate output,
gcm.h: *                  - \p output_size never needs to be more than \c 15.
gcm.h: *                  cipher sub-context.
x509_csr.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
x509_csr.h:    void *MBEDTLS_PRIVATE(sig_opts);         /**< Signature options to be passed to mbedtls_pk_verify_ext(), e.g. for RSASSA-PSS */
x509_csr.h: *                  Subject names should contain a comma-separated list
ripemd160.h: * \brief RIPE MD-160 message digest
ripemd160.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
ripemd160.h: * \brief          RIPEMD-160 context structure
ripemd160.h: * \brief          Initialize RIPEMD-160 context
ripemd160.h: * \param ctx      RIPEMD-160 context to be initialized
ripemd160.h: * \brief          Clear RIPEMD-160 context
ripemd160.h: * \param ctx      RIPEMD-160 context to be cleared
ripemd160.h: * \brief          Clone (the state of) a RIPEMD-160 context
ripemd160.h: * \brief          RIPEMD-160 context setup
ripemd160.h: * \brief          RIPEMD-160 process buffer
ripemd160.h: * \param ctx      RIPEMD-160 context
ripemd160.h: * \brief          RIPEMD-160 final digest
ripemd160.h: * \param ctx      RIPEMD-160 context
ripemd160.h: * \param output   RIPEMD-160 checksum result
ripemd160.h: * \brief          RIPEMD-160 process data block (internal use only)
ripemd160.h: * \param ctx      RIPEMD-160 context
ripemd160.h: * \brief          Output = RIPEMD-160( input buffer )
ripemd160.h: * \param output   RIPEMD-160 checksum result
platform_util.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
platform_util.h:/* Implementation of the check-return facility.
platform_util.h:/** Critical-failure function
platform_util.h: * and will result in a compile-time warning if #MBEDTLS_CHECK_RETURN
platform_util.h: *        there are near-unavoidable circumstances under which the function
platform_util.h: *        return-check-critical.
platform_util.h:/** Ordinary-failure function
platform_util.h: * applications. Omitting the check will result in a compile-time warning if
platform_util.h: * #MBEDTLS_CHECK_RETURN_WARNING is enabled in the compile-time configuration.
platform_util.h:/** Benign-failure function
platform_util.h: * has been analyzed for return-check usefulness, whereas the lack of
platform_util.h: * return-check usefulness is unknown.
platform_util.h: * https://gcc.gnu.org/onlinedocs/gcc-3.4.6/gcc/Non_002dbugs.html
platform_util.h: * https://stackoverflow.com/questions/40576003/ignoring-warning-wunused-result
platform_util.h:#if !defined(MBEDTLS_TEST_DEFINES_ZEROIZE) //no-check-names
platform_util.h: * \brief      Platform-specific implementation of gmtime_r()
platform_util.h: *             The function is a thread-safe abstraction that behaves
ssl_cookie.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
md.h: * \brief   This file contains the generic functions for message-digest
md.h: * \author Adriaan de Jong <dejong@fox-it.com>
md.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
md.h:#define MBEDTLS_ERR_MD_FEATURE_UNAVAILABLE                -0x5080
md.h:#define MBEDTLS_ERR_MD_BAD_INPUT_DATA                     -0x5100
md.h:#define MBEDTLS_ERR_MD_ALLOC_FAILED                       -0x5180
md.h:#define MBEDTLS_ERR_MD_FILE_IO_ERROR                      -0x5200
md.h: * \warning   MD5 and SHA-1 are considered weak message digests and
md.h:    MBEDTLS_MD_RIPEMD160=0x04, /**< The RIPEMD-160 message digest. */
md.h:    MBEDTLS_MD_SHA1=0x05,      /**< The SHA-1 message digest. */
md.h:    MBEDTLS_MD_SHA224=0x08,    /**< The SHA-224 message digest. */
md.h:    MBEDTLS_MD_SHA256=0x09,    /**< The SHA-256 message digest. */
md.h:    MBEDTLS_MD_SHA384=0x0a,    /**< The SHA-384 message digest. */
md.h:    MBEDTLS_MD_SHA512=0x0b,    /**< The SHA-512 message digest. */
md.h:    MBEDTLS_MD_SHA3_224=0x10,  /**< The SHA3-224 message digest. */
md.h:    MBEDTLS_MD_SHA3_256=0x11,  /**< The SHA3-256 message digest. */
md.h:    MBEDTLS_MD_SHA3_384=0x12,  /**< The SHA3-384 message digest. */
md.h:    MBEDTLS_MD_SHA3_512=0x13,  /**< The SHA3-512 message digest. */
md.h:#define MBEDTLS_MD_MAX_SIZE         20  /* longest known is SHA1 or RIPE MD-160
md.h:#define MBEDTLS_MD_MAX_BLOCK_SIZE         144 /* the longest known is SHA3-224 */
md.h: * The generic message-digest context.
md.h:    /** The digest-specific context (legacy) or the PSA operation. */
md.h: * \brief           This function returns the message-digest information
md.h: * \return          The message-digest information associated with \p md_type.
md.h: * \return          NULL if the associated message-digest information is not found.
md.h: * \brief           This function initializes a message-digest context without
md.h: *                  binding it to a particular message-digest algorithm.
md.h: *                  message-digest algorithm.
md.h: * \param md_info   The information structure of the message-digest algorithm
md.h: *                  or non-zero: HMAC is used with this context.
md.h: * \return          #MBEDTLS_ERR_MD_BAD_INPUT_DATA on parameter-verification
md.h: * \return          #MBEDTLS_ERR_MD_ALLOC_FAILED on memory-allocation failure.
md.h: * \brief           This function clones the state of a message-digest
md.h: *                  for example, both are SHA-256.
md.h: * \warning         This function clones the message-digest state, not the
md.h: * \return          #MBEDTLS_ERR_MD_BAD_INPUT_DATA on parameter-verification failure.
md.h: * \brief           This function extracts the message-digest size from the
md.h: *                  message-digest information structure.
md.h: * \param md_info   The information structure of the message-digest algorithm
md.h: * \return          The size of the message-digest output in Bytes.
md.h: * \brief           This function gives the message-digest size associated to
md.h: *                  message-digest type.
md.h: * \param md_type   The message-digest type.
md.h: * \return          The size of the message-digest output in Bytes,
md.h: *                  or 0 if the message-digest type is not known.
md.h: * \brief           This function extracts the message-digest type from the
md.h: *                  message-digest information structure.
md.h: * \param md_info   The information structure of the message-digest algorithm
md.h: * \brief           This function starts a message-digest computation.
md.h: * \param ctx       The generic message-digest context.
md.h: * \return          #MBEDTLS_ERR_MD_BAD_INPUT_DATA on parameter-verification
md.h: *                  message-digest computation.
md.h: * \param ctx       The generic message-digest context.
md.h: * \return          #MBEDTLS_ERR_MD_BAD_INPUT_DATA on parameter-verification
md.h: * \param ctx       The generic message-digest context.
md.h: * \param output    The buffer for the generic message-digest checksum result.
md.h: * \return          #MBEDTLS_ERR_MD_BAD_INPUT_DATA on parameter-verification
md.h: * \brief          This function calculates the message-digest of a buffer,
md.h: *                 with respect to a configurable message-digest algorithm
md.h: * \param md_info  The information structure of the message-digest algorithm
md.h: * \param output   The generic message-digest checksum result.
md.h: * \return         #MBEDTLS_ERR_MD_BAD_INPUT_DATA on parameter-verification
md.h: *                  message-digest enumeration #mbedtls_md_type_t.
md.h: * \brief           This function returns the message-digest information
md.h: * \return          The message-digest information associated with \p md_name.
md.h: * \return          NULL if the associated message-digest information is not found.
md.h: *                  the message-digest information structure given.
md.h: * \param md_info   The information structure of the message-digest algorithm
md.h: * \brief           This function returns the message-digest information
md.h: * \return          The message-digest information associated with \p ctx.
md.h: * \brief          This function calculates the message-digest checksum
md.h: * \param md_info  The information structure of the message-digest algorithm
md.h: * \param output   The generic message-digest checksum result.
md.h: * \return          #MBEDTLS_ERR_MD_BAD_INPUT_DATA on parameter-verification
md.h: * \return          #MBEDTLS_ERR_MD_BAD_INPUT_DATA on parameter-verification
md.h: * \return          #MBEDTLS_ERR_MD_BAD_INPUT_DATA on parameter-verification
md.h: * \return          #MBEDTLS_ERR_MD_BAD_INPUT_DATA on parameter-verification
md.h: * \param md_info  The information structure of the message-digest algorithm
md.h: * \return         #MBEDTLS_ERR_MD_BAD_INPUT_DATA on parameter-verification
oid.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
oid.h:#define MBEDTLS_ERR_OID_NOT_FOUND                         -0x002E
oid.h:#define MBEDTLS_ERR_OID_BUF_TOO_SMALL                     -0x000B
oid.h:#define MBEDTLS_OID_ISO_MEMBER_BODIES           "\x2a"          /* {iso(1) member-body(2)} */
oid.h:#define MBEDTLS_OID_ISO_IDENTIFIED_ORG          "\x2b"          /* {iso(1) identified-organization(3)} */
oid.h:#define MBEDTLS_OID_ISO_CCITT_DS                "\x55"          /* {joint-iso-ccitt(2) ds(5)} */
oid.h:#define MBEDTLS_OID_ISO_ITU_COUNTRY             "\x60"          /* {joint-iso-itu-t(2) country(16)} */
oid.h:        MBEDTLS_OID_ORG_RSA_DATA_SECURITY                                 /* {iso(1) member-body(2) us(840) rsadsi(113549)} */
oid.h:#define MBEDTLS_OID_ORG_ANSI_X9_62              "\xce\x3d" /* ansi-X9-62(10045) */
oid.h:        MBEDTLS_OID_ORGANIZATION                                                                                            /* {joint-iso-itu-t(2) country(16) us(840) organization(1)} */
oid.h:#define MBEDTLS_OID_GOV                         MBEDTLS_OID_ISO_ITU_US_ORG MBEDTLS_OID_ORG_GOV /* {joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101)} */
oid.h:#define MBEDTLS_OID_NETSCAPE                    MBEDTLS_OID_ISO_ITU_US_ORG MBEDTLS_OID_ORG_NETSCAPE /* Netscape OID {joint-iso-itu-t(2) country(16) us(840) organization(1) netscape(113730)} */
oid.h:#define MBEDTLS_OID_ID_CE                       MBEDTLS_OID_ISO_CCITT_DS "\x1D" /**< id-ce OBJECT IDENTIFIER  ::=  {joint-iso-ccitt(2) ds(5) 29} */
oid.h:#define MBEDTLS_OID_NIST_ALG                    MBEDTLS_OID_GOV "\x03\x04" /** { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithm(4) */
oid.h: * { iso(1) identified-organization(3) dod(6) internet(1)
oid.h:#define MBEDTLS_OID_AT                          MBEDTLS_OID_ISO_CCITT_DS "\x04" /**< id-at OBJECT IDENTIFIER ::= {joint-iso-ccitt(2) ds(5) 4} */
oid.h:#define MBEDTLS_OID_AT_CN                       MBEDTLS_OID_AT "\x03" /**< id-at-commonName AttributeType:= {id-at 3} */
oid.h:#define MBEDTLS_OID_AT_SUR_NAME                 MBEDTLS_OID_AT "\x04" /**< id-at-surName AttributeType:= {id-at 4} */
oid.h:#define MBEDTLS_OID_AT_SERIAL_NUMBER            MBEDTLS_OID_AT "\x05" /**< id-at-serialNumber AttributeType:= {id-at 5} */
oid.h:#define MBEDTLS_OID_AT_COUNTRY                  MBEDTLS_OID_AT "\x06" /**< id-at-countryName AttributeType:= {id-at 6} */
oid.h:#define MBEDTLS_OID_AT_LOCALITY                 MBEDTLS_OID_AT "\x07" /**< id-at-locality AttributeType:= {id-at 7} */
oid.h:#define MBEDTLS_OID_AT_STATE                    MBEDTLS_OID_AT "\x08" /**< id-at-state AttributeType:= {id-at 8} */
oid.h:#define MBEDTLS_OID_AT_ORGANIZATION             MBEDTLS_OID_AT "\x0A" /**< id-at-organizationName AttributeType:= {id-at 10} */
oid.h:#define MBEDTLS_OID_AT_ORG_UNIT                 MBEDTLS_OID_AT "\x0B" /**< id-at-organizationalUnitName AttributeType:= {id-at 11} */
oid.h:#define MBEDTLS_OID_AT_TITLE                    MBEDTLS_OID_AT "\x0C" /**< id-at-title AttributeType:= {id-at 12} */
oid.h:#define MBEDTLS_OID_AT_POSTAL_ADDRESS           MBEDTLS_OID_AT "\x10" /**< id-at-postalAddress AttributeType:= {id-at 16} */
oid.h:#define MBEDTLS_OID_AT_POSTAL_CODE              MBEDTLS_OID_AT "\x11" /**< id-at-postalCode AttributeType:= {id-at 17} */
oid.h:#define MBEDTLS_OID_AT_GIVEN_NAME               MBEDTLS_OID_AT "\x2A" /**< id-at-givenName AttributeType:= {id-at 42} */
oid.h:#define MBEDTLS_OID_AT_INITIALS                 MBEDTLS_OID_AT "\x2B" /**< id-at-initials AttributeType:= {id-at 43} */
oid.h:#define MBEDTLS_OID_AT_GENERATION_QUALIFIER     MBEDTLS_OID_AT "\x2C" /**< id-at-generationQualifier AttributeType:= {id-at 44} */
oid.h:#define MBEDTLS_OID_AT_UNIQUE_IDENTIFIER        MBEDTLS_OID_AT "\x2D" /**< id-at-uniqueIdentifier AttributeType:= {id-at 45} */
oid.h:#define MBEDTLS_OID_AT_DN_QUALIFIER             MBEDTLS_OID_AT "\x2E" /**< id-at-dnQualifier AttributeType:= {id-at 46} */
oid.h:#define MBEDTLS_OID_AT_PSEUDONYM                MBEDTLS_OID_AT "\x41" /**< id-at-pseudonym AttributeType:= {id-at 65} */
oid.h:#define MBEDTLS_OID_UID                         "\x09\x92\x26\x89\x93\xF2\x2C\x64\x01\x01" /** id-domainComponent AttributeType:= {itu-t(0) data(9) pss(2342) ucl(19200300) pilot(100) pilotAttributeType(1) uid(1)} */
oid.h:#define MBEDTLS_OID_DOMAIN_COMPONENT            "\x09\x92\x26\x89\x93\xF2\x2C\x64\x01\x19" /** id-domainComponent AttributeType:= {itu-t(0) data(9) pss(2342) ucl(19200300) pilot(100) pilotAttributeType(1) domainComponent(25)} */
oid.h:#define MBEDTLS_OID_AUTHORITY_KEY_IDENTIFIER    MBEDTLS_OID_ID_CE "\x23" /**< id-ce-authorityKeyIdentifier OBJECT IDENTIFIER ::=  { id-ce 35 } */
oid.h:#define MBEDTLS_OID_SUBJECT_KEY_IDENTIFIER      MBEDTLS_OID_ID_CE "\x0E" /**< id-ce-subjectKeyIdentifier OBJECT IDENTIFIER ::=  { id-ce 14 } */
oid.h:#define MBEDTLS_OID_KEY_USAGE                   MBEDTLS_OID_ID_CE "\x0F" /**< id-ce-keyUsage OBJECT IDENTIFIER ::=  { id-ce 15 } */
oid.h:#define MBEDTLS_OID_CERTIFICATE_POLICIES        MBEDTLS_OID_ID_CE "\x20" /**< id-ce-certificatePolicies OBJECT IDENTIFIER ::=  { id-ce 32 } */
oid.h:#define MBEDTLS_OID_POLICY_MAPPINGS             MBEDTLS_OID_ID_CE "\x21" /**< id-ce-policyMappings OBJECT IDENTIFIER ::=  { id-ce 33 } */
oid.h:#define MBEDTLS_OID_SUBJECT_ALT_NAME            MBEDTLS_OID_ID_CE "\x11" /**< id-ce-subjectAltName OBJECT IDENTIFIER ::=  { id-ce 17 } */
oid.h:#define MBEDTLS_OID_ISSUER_ALT_NAME             MBEDTLS_OID_ID_CE "\x12" /**< id-ce-issuerAltName OBJECT IDENTIFIER ::=  { id-ce 18 } */
oid.h:#define MBEDTLS_OID_SUBJECT_DIRECTORY_ATTRS     MBEDTLS_OID_ID_CE "\x09" /**< id-ce-subjectDirectoryAttributes OBJECT IDENTIFIER ::=  { id-ce 9 } */
oid.h:#define MBEDTLS_OID_BASIC_CONSTRAINTS           MBEDTLS_OID_ID_CE "\x13" /**< id-ce-basicConstraints OBJECT IDENTIFIER ::=  { id-ce 19 } */
oid.h:#define MBEDTLS_OID_NAME_CONSTRAINTS            MBEDTLS_OID_ID_CE "\x1E" /**< id-ce-nameConstraints OBJECT IDENTIFIER ::=  { id-ce 30 } */
oid.h:#define MBEDTLS_OID_POLICY_CONSTRAINTS          MBEDTLS_OID_ID_CE "\x24" /**< id-ce-policyConstraints OBJECT IDENTIFIER ::=  { id-ce 36 } */
oid.h:#define MBEDTLS_OID_EXTENDED_KEY_USAGE          MBEDTLS_OID_ID_CE "\x25" /**< id-ce-extKeyUsage OBJECT IDENTIFIER ::= { id-ce 37 } */
oid.h:#define MBEDTLS_OID_CRL_DISTRIBUTION_POINTS     MBEDTLS_OID_ID_CE "\x1F" /**< id-ce-cRLDistributionPoints OBJECT IDENTIFIER ::=  { id-ce 31 } */
oid.h:#define MBEDTLS_OID_INIHIBIT_ANYPOLICY          MBEDTLS_OID_ID_CE "\x36" /**< id-ce-inhibitAnyPolicy OBJECT IDENTIFIER ::=  { id-ce 54 } */
oid.h:#define MBEDTLS_OID_FRESHEST_CRL                MBEDTLS_OID_ID_CE "\x2E" /**< id-ce-freshestCRL OBJECT IDENTIFIER ::=  { id-ce 46 } */
oid.h:#define MBEDTLS_OID_ANY_POLICY              MBEDTLS_OID_CERTIFICATE_POLICIES "\x00" /**< anyPolicy OBJECT IDENTIFIER ::= { id-ce-certificatePolicies 0 } */
oid.h:#define MBEDTLS_OID_CRL_NUMBER                  MBEDTLS_OID_ID_CE "\x14" /**< id-ce-cRLNumber OBJECT IDENTIFIER ::= { id-ce 20 } */
oid.h:#define MBEDTLS_OID_ANY_EXTENDED_KEY_USAGE      MBEDTLS_OID_EXTENDED_KEY_USAGE "\x00" /**< anyExtendedKeyUsage OBJECT IDENTIFIER ::= { id-ce-extKeyUsage 0 } */
oid.h:#define MBEDTLS_OID_KP                          MBEDTLS_OID_PKIX "\x03" /**< id-kp OBJECT IDENTIFIER ::= { id-pkix 3 } */
oid.h:#define MBEDTLS_OID_SERVER_AUTH                 MBEDTLS_OID_KP "\x01" /**< id-kp-serverAuth OBJECT IDENTIFIER ::= { id-kp 1 } */
oid.h:#define MBEDTLS_OID_CLIENT_AUTH                 MBEDTLS_OID_KP "\x02" /**< id-kp-clientAuth OBJECT IDENTIFIER ::= { id-kp 2 } */
oid.h:#define MBEDTLS_OID_CODE_SIGNING                MBEDTLS_OID_KP "\x03" /**< id-kp-codeSigning OBJECT IDENTIFIER ::= { id-kp 3 } */
oid.h:#define MBEDTLS_OID_EMAIL_PROTECTION            MBEDTLS_OID_KP "\x04" /**< id-kp-emailProtection OBJECT IDENTIFIER ::= { id-kp 4 } */
oid.h:#define MBEDTLS_OID_TIME_STAMPING               MBEDTLS_OID_KP "\x08" /**< id-kp-timeStamping OBJECT IDENTIFIER ::= { id-kp 8 } */
oid.h:#define MBEDTLS_OID_OCSP_SIGNING                MBEDTLS_OID_KP "\x09" /**< id-kp-OCSPSigning OBJECT IDENTIFIER ::= { id-kp 9 } */
oid.h: * Wi-SUN Alliance Field Area Network
oid.h: * { iso(1) identified-organization(3) dod(6) internet(1)
oid.h:#define MBEDTLS_OID_ON                          MBEDTLS_OID_PKIX "\x08" /**< id-on OBJECT IDENTIFIER ::= { id-pkix 8 } */
oid.h:#define MBEDTLS_OID_ON_HW_MODULE_NAME           MBEDTLS_OID_ON "\x04" /**< id-on-hardwareModuleName OBJECT IDENTIFIER ::= { id-on 4 } */
oid.h:#define MBEDTLS_OID_PKCS                MBEDTLS_OID_RSA_COMPANY "\x01" /**< pkcs OBJECT IDENTIFIER ::= { iso(1) member-body(2) us(840) rsadsi(113549) 1 } */
oid.h:#define MBEDTLS_OID_PKCS1               MBEDTLS_OID_PKCS "\x01" /**< pkcs-1 OBJECT IDENTIFIER ::= { iso(1) member-body(2) us(840) rsadsi(113549) pkcs(1) 1 } */
oid.h:#define MBEDTLS_OID_PKCS5               MBEDTLS_OID_PKCS "\x05" /**< pkcs-5 OBJECT IDENTIFIER ::= { iso(1) member-body(2) us(840) rsadsi(113549) pkcs(1) 5 } */
oid.h:#define MBEDTLS_OID_PKCS7               MBEDTLS_OID_PKCS "\x07" /**< pkcs-7 OBJECT IDENTIFIER ::= { iso(1) member-body(2) us(840) rsadsi(113549) pkcs(1) 7 } */
oid.h:#define MBEDTLS_OID_PKCS9               MBEDTLS_OID_PKCS "\x09" /**< pkcs-9 OBJECT IDENTIFIER ::= { iso(1) member-body(2) us(840) rsadsi(113549) pkcs(1) 9 } */
oid.h:#define MBEDTLS_OID_PKCS12              MBEDTLS_OID_PKCS "\x0c" /**< pkcs-12 OBJECT IDENTIFIER ::= { iso(1) member-body(2) us(840) rsadsi(113549) pkcs(1) 12 } */
oid.h:#define MBEDTLS_OID_PKCS1_RSA           MBEDTLS_OID_PKCS1 "\x01" /**< rsaEncryption OBJECT IDENTIFIER ::= { pkcs-1 1 } */
oid.h:#define MBEDTLS_OID_PKCS1_MD5           MBEDTLS_OID_PKCS1 "\x04" /**< md5WithRSAEncryption ::= { pkcs-1 4 } */
oid.h:#define MBEDTLS_OID_PKCS1_SHA1          MBEDTLS_OID_PKCS1 "\x05" /**< sha1WithRSAEncryption ::= { pkcs-1 5 } */
oid.h:#define MBEDTLS_OID_PKCS1_SHA224        MBEDTLS_OID_PKCS1 "\x0e" /**< sha224WithRSAEncryption ::= { pkcs-1 14 } */
oid.h:#define MBEDTLS_OID_PKCS1_SHA256        MBEDTLS_OID_PKCS1 "\x0b" /**< sha256WithRSAEncryption ::= { pkcs-1 11 } */
oid.h:#define MBEDTLS_OID_PKCS1_SHA384        MBEDTLS_OID_PKCS1 "\x0c" /**< sha384WithRSAEncryption ::= { pkcs-1 12 } */
oid.h:#define MBEDTLS_OID_PKCS1_SHA512        MBEDTLS_OID_PKCS1 "\x0d" /**< sha512WithRSAEncryption ::= { pkcs-1 13 } */
oid.h:#define MBEDTLS_OID_PKCS9_EMAIL         MBEDTLS_OID_PKCS9 "\x01" /**< emailAddress AttributeType ::= { pkcs-9 1 } */
oid.h:#define MBEDTLS_OID_RSASSA_PSS          MBEDTLS_OID_PKCS1 "\x0a" /**< id-RSASSA-PSS ::= { pkcs-1 10 } */
oid.h:#define MBEDTLS_OID_MGF1                MBEDTLS_OID_PKCS1 "\x08" /**< id-mgf1 ::= { pkcs-1 8 } */
oid.h:#define MBEDTLS_OID_DIGEST_ALG_MD5              MBEDTLS_OID_RSA_COMPANY "\x02\x05" /**< id-mbedtls_md5 OBJECT IDENTIFIER ::= { iso(1) member-body(2) us(840) rsadsi(113549) digestAlgorithm(2) 5 } */
oid.h:        MBEDTLS_OID_OIW_SECSIG_SHA1                                                                        /**< id-mbedtls_sha1 OBJECT IDENTIFIER ::= { iso(1) identified-organization(3) oiw(14) secsig(3) algorithms(2) 26 } */
oid.h:#define MBEDTLS_OID_DIGEST_ALG_SHA224           MBEDTLS_OID_NIST_ALG "\x02\x04" /**< id-sha224 OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistalgorithm(4) hashalgs(2) 4 } */
oid.h:#define MBEDTLS_OID_DIGEST_ALG_SHA256           MBEDTLS_OID_NIST_ALG "\x02\x01" /**< id-mbedtls_sha256 OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistalgorithm(4) hashalgs(2) 1 } */
oid.h:#define MBEDTLS_OID_DIGEST_ALG_SHA384           MBEDTLS_OID_NIST_ALG "\x02\x02" /**< id-sha384 OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistalgorithm(4) hashalgs(2) 2 } */
oid.h:#define MBEDTLS_OID_DIGEST_ALG_SHA512           MBEDTLS_OID_NIST_ALG "\x02\x03" /**< id-mbedtls_sha512 OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistalgorithm(4) hashalgs(2) 3 } */
oid.h:#define MBEDTLS_OID_DIGEST_ALG_RIPEMD160        MBEDTLS_OID_TELETRUST "\x03\x02\x01" /**< id-ripemd160 OBJECT IDENTIFIER :: { iso(1) identified-organization(3) teletrust(36) algorithm(3) hashAlgorithm(2) ripemd160(1) } */
oid.h:#define MBEDTLS_OID_DIGEST_ALG_SHA3_224         MBEDTLS_OID_NIST_ALG "\x02\x07" /**< id-sha3-224 OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithms(4) hashalgs(2) sha3-224(7) } */
oid.h:#define MBEDTLS_OID_DIGEST_ALG_SHA3_256         MBEDTLS_OID_NIST_ALG "\x02\x08" /**< id-sha3-256 OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithms(4) hashalgs(2) sha3-256(8) } */
oid.h:#define MBEDTLS_OID_DIGEST_ALG_SHA3_384         MBEDTLS_OID_NIST_ALG "\x02\x09" /**< id-sha3-384 OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithms(4) hashalgs(2) sha3-384(9) } */
oid.h:#define MBEDTLS_OID_DIGEST_ALG_SHA3_512         MBEDTLS_OID_NIST_ALG "\x02\x0a" /**< id-sha3-512 OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithms(4) hashalgs(2) sha3-512(10) } */
oid.h:#define MBEDTLS_OID_HMAC_SHA1                   MBEDTLS_OID_RSA_COMPANY "\x02\x07" /**< id-hmacWithSHA1 OBJECT IDENTIFIER ::= { iso(1) member-body(2) us(840) rsadsi(113549) digestAlgorithm(2) 7 } */
oid.h:#define MBEDTLS_OID_HMAC_SHA224                 MBEDTLS_OID_RSA_COMPANY "\x02\x08" /**< id-hmacWithSHA224 OBJECT IDENTIFIER ::= { iso(1) member-body(2) us(840) rsadsi(113549) digestAlgorithm(2) 8 } */
oid.h:#define MBEDTLS_OID_HMAC_SHA256                 MBEDTLS_OID_RSA_COMPANY "\x02\x09" /**< id-hmacWithSHA256 OBJECT IDENTIFIER ::= { iso(1) member-body(2) us(840) rsadsi(113549) digestAlgorithm(2) 9 } */
oid.h:#define MBEDTLS_OID_HMAC_SHA384                 MBEDTLS_OID_RSA_COMPANY "\x02\x0A" /**< id-hmacWithSHA384 OBJECT IDENTIFIER ::= { iso(1) member-body(2) us(840) rsadsi(113549) digestAlgorithm(2) 10 } */
oid.h:#define MBEDTLS_OID_HMAC_SHA512                 MBEDTLS_OID_RSA_COMPANY "\x02\x0B" /**< id-hmacWithSHA512 OBJECT IDENTIFIER ::= { iso(1) member-body(2) us(840) rsadsi(113549) digestAlgorithm(2) 11 } */
oid.h:#define MBEDTLS_OID_HMAC_SHA3_224               MBEDTLS_OID_NIST_ALG "\x02\x0d" /**< id-hmacWithSHA3-512 OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithms(4) hashalgs(2) hmacWithSHA3-224(13) } */
oid.h:#define MBEDTLS_OID_HMAC_SHA3_256               MBEDTLS_OID_NIST_ALG "\x02\x0e" /**< id-hmacWithSHA3-512 OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithms(4) hashalgs(2) hmacWithSHA3-256(14) } */
oid.h:#define MBEDTLS_OID_HMAC_SHA3_384               MBEDTLS_OID_NIST_ALG "\x02\x0f" /**< id-hmacWithSHA3-512 OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithms(4) hashalgs(2) hmacWithSHA3-384(15) } */
oid.h:#define MBEDTLS_OID_HMAC_SHA3_512               MBEDTLS_OID_NIST_ALG "\x02\x10" /**< id-hmacWithSHA3-512 OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithms(4) hashalgs(2) hmacWithSHA3-512(16) } */
oid.h:#define MBEDTLS_OID_HMAC_RIPEMD160              MBEDTLS_OID_INTERNET "\x05\x05\x08\x01\x04" /**< id-hmacWithSHA1 OBJECT IDENTIFIER ::= {iso(1) iso-identified-organization(3) dod(6) internet(1) security(5) mechanisms(5) ipsec(8) isakmpOakley(1) hmacRIPEMD160(4)} */
oid.h: * https://datatracker.ietf.org/doc/html/rfc8018#appendix-C.
oid.h:        MBEDTLS_OID_OIW_SECSIG_ALG "\x07"                                                                        /**< desCBC OBJECT IDENTIFIER ::= { iso(1) identified-organization(3) oiw(14) secsig(3) algorithms(2) 7 } */
oid.h:#define MBEDTLS_OID_DES_EDE3_CBC                MBEDTLS_OID_RSA_COMPANY "\x03\x07" /**< des-ede3-cbc OBJECT IDENTIFIER ::= { iso(1) member-body(2) -- us(840) rsadsi(113549) encryptionAlgorithm(3) 7 } */
oid.h:#define MBEDTLS_OID_AES                         MBEDTLS_OID_NIST_ALG "\x01" /** aes OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithm(4) 1 } */
oid.h:#define MBEDTLS_OID_AES_128_CBC                 MBEDTLS_OID_AES "\x02" /** aes128-cbc-pad OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithms(4) aes(1) aes128-CBC-PAD(2) } */
oid.h:#define MBEDTLS_OID_AES_192_CBC                 MBEDTLS_OID_AES "\x16" /** aes192-cbc-pad OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithms(4) aes(1) aes192-CBC-PAD(22) } */
oid.h:#define MBEDTLS_OID_AES_256_CBC                 MBEDTLS_OID_AES "\x2a" /** aes256-cbc-pad OBJECT IDENTIFIER ::= { joint-iso-itu-t(2) country(16) us(840) organization(1) gov(101) csor(3) nistAlgorithms(4) aes(1) aes256-CBC-PAD(42) } */
oid.h:#define MBEDTLS_OID_AES128_KW                   MBEDTLS_OID_AES "\x05" /** id-aes128-wrap     OBJECT IDENTIFIER ::= { aes 5 } */
oid.h:#define MBEDTLS_OID_AES128_KWP                  MBEDTLS_OID_AES "\x08" /** id-aes128-wrap-pad OBJECT IDENTIFIER ::= { aes 8 } */
oid.h:#define MBEDTLS_OID_AES192_KW                   MBEDTLS_OID_AES "\x19" /** id-aes192-wrap     OBJECT IDENTIFIER ::= { aes 25 } */
oid.h:#define MBEDTLS_OID_AES192_KWP                  MBEDTLS_OID_AES "\x1c" /** id-aes192-wrap-pad OBJECT IDENTIFIER ::= { aes 28 } */
oid.h:#define MBEDTLS_OID_AES256_KW                   MBEDTLS_OID_AES "\x2d" /** id-aes256-wrap     OBJECT IDENTIFIER ::= { aes 45 } */
oid.h:#define MBEDTLS_OID_AES256_KWP                  MBEDTLS_OID_AES "\x30" /** id-aes256-wrap-pad OBJECT IDENTIFIER ::= { aes 48 } */
oid.h:#define MBEDTLS_OID_PKCS5_PBKDF2                MBEDTLS_OID_PKCS5 "\x0c" /**< id-PBKDF2 OBJECT IDENTIFIER ::= {pkcs-5 12} */
oid.h:#define MBEDTLS_OID_PKCS5_PBES2                 MBEDTLS_OID_PKCS5 "\x0d" /**< id-PBES2 OBJECT IDENTIFIER ::= {pkcs-5 13} */
oid.h:#define MBEDTLS_OID_PKCS5_PBMAC1                MBEDTLS_OID_PKCS5 "\x0e" /**< id-PBMAC1 OBJECT IDENTIFIER ::= {pkcs-5 14} */
oid.h:#define MBEDTLS_OID_PKCS5_PBE_MD5_DES_CBC       MBEDTLS_OID_PKCS5 "\x03" /**< pbeWithMD5AndDES-CBC OBJECT IDENTIFIER ::= {pkcs-5 3} */
oid.h:#define MBEDTLS_OID_PKCS5_PBE_MD5_RC2_CBC       MBEDTLS_OID_PKCS5 "\x06" /**< pbeWithMD5AndRC2-CBC OBJECT IDENTIFIER ::= {pkcs-5 6} */
oid.h:#define MBEDTLS_OID_PKCS5_PBE_SHA1_DES_CBC      MBEDTLS_OID_PKCS5 "\x0a" /**< pbeWithSHA1AndDES-CBC OBJECT IDENTIFIER ::= {pkcs-5 10} */
oid.h:#define MBEDTLS_OID_PKCS5_PBE_SHA1_RC2_CBC      MBEDTLS_OID_PKCS5 "\x0b" /**< pbeWithSHA1AndRC2-CBC OBJECT IDENTIFIER ::= {pkcs-5 11} */
oid.h:#define MBEDTLS_OID_PKCS7_DATA                        MBEDTLS_OID_PKCS7 "\x01" /**< Content type is Data OBJECT IDENTIFIER ::= {pkcs-7 1} */
oid.h:#define MBEDTLS_OID_PKCS7_SIGNED_DATA                 MBEDTLS_OID_PKCS7 "\x02" /**< Content type is Signed Data OBJECT IDENTIFIER ::= {pkcs-7 2} */
oid.h:#define MBEDTLS_OID_PKCS7_ENVELOPED_DATA              MBEDTLS_OID_PKCS7 "\x03" /**< Content type is Enveloped Data OBJECT IDENTIFIER ::= {pkcs-7 3} */
oid.h:#define MBEDTLS_OID_PKCS7_SIGNED_AND_ENVELOPED_DATA   MBEDTLS_OID_PKCS7 "\x04" /**< Content type is Signed and Enveloped Data OBJECT IDENTIFIER ::= {pkcs-7 4} */
oid.h:#define MBEDTLS_OID_PKCS7_DIGESTED_DATA               MBEDTLS_OID_PKCS7 "\x05" /**< Content type is Digested Data OBJECT IDENTIFIER ::= {pkcs-7 5} */
oid.h:#define MBEDTLS_OID_PKCS7_ENCRYPTED_DATA              MBEDTLS_OID_PKCS7 "\x06" /**< Content type is Encrypted Data OBJECT IDENTIFIER ::= {pkcs-7 6} */
oid.h:#define MBEDTLS_OID_PKCS9_CSR_EXT_REQ           MBEDTLS_OID_PKCS9 "\x0e" /**< extensionRequest OBJECT IDENTIFIER ::= {pkcs-9 14} */
oid.h:#define MBEDTLS_OID_PKCS12_PBE                      MBEDTLS_OID_PKCS12 "\x01" /**< pkcs-12PbeIds OBJECT IDENTIFIER ::= {pkcs-12 1} */
oid.h:#define MBEDTLS_OID_PKCS12_PBE_SHA1_DES3_EDE_CBC    MBEDTLS_OID_PKCS12_PBE "\x03" /**< pbeWithSHAAnd3-KeyTripleDES-CBC OBJECT IDENTIFIER ::= {pkcs-12PbeIds 3} */
oid.h:#define MBEDTLS_OID_PKCS12_PBE_SHA1_DES2_EDE_CBC    MBEDTLS_OID_PKCS12_PBE "\x04" /**< pbeWithSHAAnd2-KeyTripleDES-CBC OBJECT IDENTIFIER ::= {pkcs-12PbeIds 4} */
oid.h:#define MBEDTLS_OID_PKCS12_PBE_SHA1_RC2_128_CBC     MBEDTLS_OID_PKCS12_PBE "\x05" /**< pbeWithSHAAnd128BitRC2-CBC OBJECT IDENTIFIER ::= {pkcs-12PbeIds 5} */
oid.h:#define MBEDTLS_OID_PKCS12_PBE_SHA1_RC2_40_CBC      MBEDTLS_OID_PKCS12_PBE "\x06" /**< pbeWithSHAAnd40BitRC2-CBC OBJECT IDENTIFIER ::= {pkcs-12PbeIds 6} */
oid.h:/* id-ecPublicKey OBJECT IDENTIFIER ::= {
oid.h: *       iso(1) member-body(2) us(840) ansi-X9-62(10045) keyType(2) 1 } */
oid.h:/*   id-ecDH OBJECT IDENTIFIER ::= {
oid.h: *     iso(1) identified-organization(3) certicom(132)
oid.h: *   iso(1) member-body(2) us(840) ansi-X9-62(10045) curves(3) prime(1) 1 } */
oid.h: *   iso(1) identified-organization(3) certicom(132) curve(0) 33 } */
oid.h: *   iso(1) member-body(2) us(840) ansi-X9-62(10045) curves(3) prime(1) 7 } */
oid.h: *   iso(1) identified-organization(3) certicom(132) curve(0) 34 } */
oid.h: *   iso(1) identified-organization(3) certicom(132) curve(0) 35 } */
oid.h: *   iso(1) identified-organization(3) certicom(132) curve(0) 31 } */
oid.h: *   iso(1) identified-organization(3) certicom(132) curve(0) 32 } */
oid.h: *   iso(1) identified-organization(3) certicom(132) curve(0) 10 } */
oid.h: * identified-organization(3) teletrust(36) algorithm(3) signature-
oid.h: * prime-field OBJECT IDENTIFIER ::= { id-fieldType 1 }
oid.h: * id-fieldType OBJECT IDENTIFIER ::= { ansi-X9-62 fieldType(1)}
oid.h:#define MBEDTLS_OID_ANSI_X9_62_SIG_SHA2     MBEDTLS_OID_ANSI_X9_62_SIG "\x03" /* ecdsa-with-SHA2(3) */
oid.h:/* ecdsa-with-SHA1 OBJECT IDENTIFIER ::= {
oid.h: *   iso(1) member-body(2) us(840) ansi-X9-62(10045) signatures(4) 1 } */
oid.h:/* ecdsa-with-SHA224 OBJECT IDENTIFIER ::= {
oid.h: *   iso(1) member-body(2) us(840) ansi-X9-62(10045) signatures(4)
oid.h: *   ecdsa-with-SHA2(3) 1 } */
oid.h:/* ecdsa-with-SHA256 OBJECT IDENTIFIER ::= {
oid.h: *   iso(1) member-body(2) us(840) ansi-X9-62(10045) signatures(4)
oid.h: *   ecdsa-with-SHA2(3) 2 } */
oid.h:/* ecdsa-with-SHA384 OBJECT IDENTIFIER ::= {
oid.h: *   iso(1) member-body(2) us(840) ansi-X9-62(10045) signatures(4)
oid.h: *   ecdsa-with-SHA2(3) 3 } */
oid.h:/* ecdsa-with-SHA512 OBJECT IDENTIFIER ::= {
oid.h: *   iso(1) member-body(2) us(840) ansi-X9-62(10045) signatures(4)
oid.h: *   ecdsa-with-SHA2(3) 4 } */
oid.h:#define MBEDTLS_OID_X25519                  MBEDTLS_OID_THAWTE "\x6e" /**< id-X25519    OBJECT IDENTIFIER ::= { 1 3 101 110 } */
oid.h:#define MBEDTLS_OID_X448                    MBEDTLS_OID_THAWTE "\x6f" /**< id-X448      OBJECT IDENTIFIER ::= { 1 3 101 111 } */
oid.h:#define MBEDTLS_OID_ED25519                 MBEDTLS_OID_THAWTE "\x70" /**< id-Ed25519   OBJECT IDENTIFIER ::= { 1 3 101 112 } */
oid.h:#define MBEDTLS_OID_ED448                   MBEDTLS_OID_THAWTE "\x71" /**< id-Ed448     OBJECT IDENTIFIER ::= { 1 3 101 113 } */
oid.h: * \brief           Translate a string containing a dotted-decimal
oid.h: *                  On success, this function allocates oid->buf from the
oid.h: * \param oid       #mbedtls_asn1_buf to populate with the DER-encoded OID
oid.h: *                  allocate oid->buf
dhm.h: * \brief   This file contains Diffie-Hellman-Merkle (DHM) key exchange
dhm.h: * Diffie-Hellman-Merkle (DHM) key exchange is defined in
dhm.h: * <em>RFC-2631: Diffie-Hellman Key Agreement Method</em> and
dhm.h: * <em>Public-Key Cryptography Standards (PKCS) #3: Diffie
dhm.h: * <em>RFC-3526: More Modular Exponential (MODP) Diffie-Hellman groups for
dhm.h: * Diffie-Hellman groups for IKE.
dhm.h: * <em>RFC-5114: Additional Diffie-Hellman Groups for Use with IETF
dhm.h: * Standards</em> defines a number of standardized Diffie-Hellman
dhm.h: *           of prime modulus - optimally, it should be a safe prime. The usage
dhm.h: *           of non-safe primes both decreases the difficulty of the underlying
dhm.h: *           while the criticality of small-subgroup attacks is lower for
dhm.h: *           subgroups. Moreover, even if these were performed, non-standardized
dhm.h: * \warning  Diffie-Hellman-Merkle is therefore a security risk when not using
dhm.h: *           Elliptic Curve Diffie-Hellman (ECDH), which has better performance,
dhm.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
dhm.h:#define MBEDTLS_ERR_DHM_BAD_INPUT_DATA                    -0x3080
dhm.h:#define MBEDTLS_ERR_DHM_READ_PARAMS_FAILED                -0x3100
dhm.h:#define MBEDTLS_ERR_DHM_MAKE_PARAMS_FAILED                -0x3180
dhm.h:#define MBEDTLS_ERR_DHM_READ_PUBLIC_FAILED                -0x3200
dhm.h:#define MBEDTLS_ERR_DHM_MAKE_PUBLIC_FAILED                -0x3280
dhm.h:#define MBEDTLS_ERR_DHM_CALC_SECRET_FAILED                -0x3300
dhm.h:#define MBEDTLS_ERR_DHM_INVALID_FORMAT                    -0x3380
dhm.h:#define MBEDTLS_ERR_DHM_ALLOC_FAILED                      -0x3400
dhm.h:#define MBEDTLS_ERR_DHM_FILE_IO_ERROR                     -0x3480
dhm.h:#define MBEDTLS_ERR_DHM_SET_GROUP_FAILED                  -0x3580
dhm.h: * \note           This function assumes that the DHM parameters \c ctx->P
dhm.h: *                 and \c ctx->G have already been properly set. For that, use
dhm.h: *                 with a 2-byte length field. It is the responsibility of the
dhm.h: *                 mbedtls_mpi_size() to computing the byte-size of an MPI.
dhm.h: * \note           This function can be used to set \c ctx->P, \c ctx->G
dhm.h: *                 the raw public key in big-endian format.
dhm.h: *                 so as to contain a big-endian representation of G^X mod P.
dhm.h: *                 If it is larger than \c ctx->len, it is padded accordingly
dhm.h: *                 with zero-bytes at the beginning.
dhm.h: *                 equal to `ctx->len` (the size of \c P).
dhm.h: *                 only if our private key \c X is re-used, and not used
dhm.h: *                 otherwise. We recommend always passing a non-NULL
dhm.h: *                      least the size of \c ctx->len (the size of \c P).
dhm.h: *                 i.e. the number n such that 2^(n-1) <= P < 2^n.
dhm.h: *                 i.e. the number n such that 2^(8*(n-1)) <= P < 2^(8*n).
dhm.h: *                 in which case this function is a no-op. If it is not \c NULL,
dhm.h: * Diffie-Hellman groups, some of which are included here
dhm.h: * when configuring the Diffie-Hellman parameters by hand
dhm.h: * - RFC 5114 section 2.2:  2048-bit MODP Group with 224-bit Prime Order Subgroup
dhm.h: * - RFC 3526 section 3:    2048-bit MODP Group
dhm.h: * - RFC 3526 section 4:    3072-bit MODP Group
dhm.h: * - RFC 3526 section 5:    4096-bit MODP Group
dhm.h: * - RFC 7919 section A.1:  ffdhe2048
dhm.h: * - RFC 7919 section A.2:  ffdhe3072
dhm.h: * - RFC 7919 section A.3:  ffdhe4096
dhm.h: * - RFC 7919 section A.4:  ffdhe6144
dhm.h: * - RFC 7919 section A.5:  ffdhe8192
dhm.h: * while all other constants represent null-terminated strings holding the
dhm.h: * trust-worthy procedure:
dhm.h: * - Fix N in { 2048, 3072, 4096, 6144, 8192 } and consider the N-bit number
dhm.h: *   the first and last 64 bits are all 1, and the remaining N - 128 bits of
dhm.h: * - Add the smallest multiple of the first N - 129 bits of the binary expansion
dhm.h: *   of pi (for RFC 5236) or e (for RFC 7919) to this intermediate bit-string
dhm.h: *   such that the resulting integer is a safe-prime.
dhm.h: * - The result is the respective RFC 3526 / 7919 prime, and the corresponding
dhm.h: *   hence the corresponding subgroup has order (p-1)/2 and avoids leaking a
config_adjust_psa_superset_legacy.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
hkdf.h: *          The HMAC-based Extract-and-Expand Key Derivation Function (HKDF) is
hkdf.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
hkdf.h:#define MBEDTLS_ERR_HKDF_BAD_INPUT_DATA  -0x5F80
hkdf.h: *  \brief  This is the HMAC-based Extract-and-Expand Key Derivation Function
hkdf.h: *  \param  salt      An optional salt value (a non-secret random value);
hkdf.h: *                    string. This can be a zero-length string.
hkdf.h: *          fixed-length pseudorandom key \p prk.
hkdf.h: *  \param       salt      An optional salt value (a non-secret random value);
hkdf.h: *                    string. This can be a zero-length string.
sha1.h: * \brief This file contains SHA-1 definitions and functions.
sha1.h: * The Secure Hash Algorithm 1 (SHA-1) cryptographic hash function is defined in
sha1.h: * <em>FIPS 180-4: Secure Hash Standard (SHS)</em>.
sha1.h: * \warning   SHA-1 is considered a weak message digest and its use constitutes
sha1.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
sha1.h:/** SHA-1 input data was malformed. */
sha1.h:#define MBEDTLS_ERR_SHA1_BAD_INPUT_DATA                   -0x0073
sha1.h: * \brief          The SHA-1 context structure.
sha1.h: * \warning        SHA-1 is considered a weak message digest and its use
sha1.h: * \brief          This function initializes a SHA-1 context.
sha1.h: * \warning        SHA-1 is considered a weak message digest and its use
sha1.h: * \param ctx      The SHA-1 context to initialize.
sha1.h: * \brief          This function clears a SHA-1 context.
sha1.h: * \warning        SHA-1 is considered a weak message digest and its use
sha1.h: * \param ctx      The SHA-1 context to clear. This may be \c NULL,
sha1.h: *                 SHA-1 context.
sha1.h: * \brief          This function clones the state of a SHA-1 context.
sha1.h: * \warning        SHA-1 is considered a weak message digest and its use
sha1.h: * \param dst      The SHA-1 context to clone to. This must be initialized.
sha1.h: * \param src      The SHA-1 context to clone from. This must be initialized.
sha1.h: * \brief          This function starts a SHA-1 checksum calculation.
sha1.h: * \warning        SHA-1 is considered a weak message digest and its use
sha1.h: * \param ctx      The SHA-1 context to initialize. This must be initialized.
sha1.h: * \brief          This function feeds an input buffer into an ongoing SHA-1
sha1.h: * \warning        SHA-1 is considered a weak message digest and its use
sha1.h: * \param ctx      The SHA-1 context. This must be initialized
sha1.h: * \brief          This function finishes the SHA-1 operation, and writes
sha1.h: * \warning        SHA-1 is considered a weak message digest and its use
sha1.h: * \param ctx      The SHA-1 context to use. This must be initialized and
sha1.h: * \param output   The SHA-1 checksum result. This must be a writable
sha1.h: * \brief          SHA-1 process data block (internal use only).
sha1.h: * \warning        SHA-1 is considered a weak message digest and its use
sha1.h: * \param ctx      The SHA-1 context to use. This must be initialized.
sha1.h: * \brief          This function calculates the SHA-1 checksum of a buffer.
sha1.h: *                 The SHA-1 result is calculated as
sha1.h: *                 output = SHA-1(input buffer).
sha1.h: * \warning        SHA-1 is considered a weak message digest and its use
sha1.h: * \param output   The SHA-1 checksum result.
sha1.h: * \brief          The SHA-1 checkup routine.
sha1.h: * \warning        SHA-1 is considered a weak message digest and its use
config_psa.h: *  This set of compile-time options takes settings defined in
config_psa.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
config_psa.h:/* Require built-in implementations based on PSA requirements */
config_psa.h: * before we deduce what built-ins are required. */
config_psa.h: * required built-ins. Otherwise, PSA features will be provided by the server. */
entropy.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
entropy.h:#define MBEDTLS_ENTROPY_BLOCK_SIZE      64      /**< Block size of entropy accumulator (SHA-512) */
entropy.h:#define MBEDTLS_ENTROPY_BLOCK_SIZE      32      /**< Block size of entropy accumulator (SHA-256) */
entropy.h:#define MBEDTLS_ERR_ENTROPY_SOURCE_FAILED                 -0x003C
entropy.h:#define MBEDTLS_ERR_ENTROPY_MAX_SOURCES                   -0x003E
entropy.h:#define MBEDTLS_ERR_ENTROPY_NO_SOURCES_DEFINED            -0x0040
entropy.h:#define MBEDTLS_ERR_ENTROPY_NO_STRONG_SOURCE              -0x003D
entropy.h:#define MBEDTLS_ERR_ENTROPY_FILE_IO_ERROR                 -0x003F
entropy.h: * \param data      Callback-specific data pointer
entropy.h:                                               * -1 after free. */
entropy.h: * \brief           Platform-specific entropy poll callback
entropy.h: *                  (Thread-safe if MBEDTLS_THREADING_C is enabled)
entropy.h: *                  (Thread-safe if MBEDTLS_THREADING_C is enabled)
entropy.h: *                  (Thread-safe if MBEDTLS_THREADING_C is enabled)
entropy.h: *                  (Thread-safe if MBEDTLS_THREADING_C is enabled)
entropy.h: *                 This module self-test also calls the entropy self-test,
md5.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
nist_kw.h: *        padding (KWP) as defined in NIST SP 800-38F.
nist_kw.h: *        https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.800-38F.pdf
nist_kw.h: *        Key wrapping specifies a deterministic authenticated-encryption mode
nist_kw.h: *        of operation, according to <em>NIST SP 800-38F: Recommendation for
nist_kw.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
nist_kw.h: * \brief    The key wrapping context-type definition. The key wrapping context is passed
nist_kw.h: * \param cipher    The 128-bit block cipher to use. Only AES is supported.
nist_kw.h: * \return          \c MBEDTLS_ERR_CIPHER_FEATURE_UNAVAILABLE for 128-bit block ciphers
nist_kw.h: * \return          cipher-specific error code on failure of the underlying cipher.
nist_kw.h: *          and underlying cipher sub-context.
nist_kw.h: *                  <ul><li>For KW mode: a multiple of 8 bytes between 16 and 2^57-8 inclusive. </li>
nist_kw.h: *                  <li>For KWP mode: any length between 1 and 2^32-1 inclusive.</li></ul>
nist_kw.h: * \return          cipher-specific error code on failure of the underlying cipher.
nist_kw.h: * \return          cipher-specific error code on failure of the underlying cipher.
asn1write.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \brief           Write an arbitrary-precision number (#MBEDTLS_ASN1_INTEGER)
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: *                  It must be non-negative.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: *                  It must be non-negative.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
asn1write.h: * \param start     The start of the buffer which is used for bounds-checking.
asn1write.h: * \param start     The start of the buffer, for bounds-checking.
compat-2.x.h: * \file compat-2.x.h
compat-2.x.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
compat-2.x.h:#warning "Including compat-2.x.h is deprecated"
ecp.h: * <em>RFC-4492: Elliptic Curve Cryptography (ECC) Cipher Suites
ecp.h: * <em>RFC-2409: The Internet Key Exchange (IKE)</em> defines ECP
ecp.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
ecp.h:#define MBEDTLS_ERR_ECP_BAD_INPUT_DATA                    -0x4F80
ecp.h:#define MBEDTLS_ERR_ECP_BUFFER_TOO_SMALL                  -0x4F00
ecp.h:#define MBEDTLS_ERR_ECP_FEATURE_UNAVAILABLE               -0x4E80
ecp.h:#define MBEDTLS_ERR_ECP_VERIFY_FAILED                     -0x4E00
ecp.h:#define MBEDTLS_ERR_ECP_ALLOC_FAILED                      -0x4D80
ecp.h:#define MBEDTLS_ERR_ECP_RANDOM_FAILED                     -0x4D00
ecp.h:#define MBEDTLS_ERR_ECP_INVALID_KEY                       -0x4C80
ecp.h:#define MBEDTLS_ERR_ECP_SIG_LEN_MISMATCH                  -0x4C00
ecp.h:#define MBEDTLS_ERR_ECP_IN_PROGRESS                       -0x4B00
ecp.h: * Domain-parameter identifiers: curve, subgroup, and generator.
ecp.h: * - Add it at the end of this enum, otherwise you'll break the ABI by
ecp.h: * - Increment MBEDTLS_ECP_DP_MAX below if needed.
ecp.h: * - Update the calculation of MBEDTLS_ECP_MAX_BITS below.
ecp.h: * - Add the corresponding MBEDTLS_ECP_DP_xxx_ENABLED macro definition to
ecp.h: * - List the curve as a dependency of MBEDTLS_ECP_C and
ecp.h: * - Add the curve to the appropriate curve type macro
ecp.h: * - Add the necessary definitions to ecp_curves.c.
ecp.h: * - Add the curve to the ecp_supported_curves array in ecp.c.
ecp.h: * - Add the curve to applicable profiles in x509_crt.c.
ecp.h: * - Add the curve to applicable presets in ssl_tls.c.
ecp.h:    MBEDTLS_ECP_DP_SECP192R1,      /*!< Domain parameters for the 192-bit curve defined by FIPS 186-4 and SEC1. */
ecp.h:    MBEDTLS_ECP_DP_SECP224R1,      /*!< Domain parameters for the 224-bit curve defined by FIPS 186-4 and SEC1. */
ecp.h:    MBEDTLS_ECP_DP_SECP256R1,      /*!< Domain parameters for the 256-bit curve defined by FIPS 186-4 and SEC1. */
ecp.h:    MBEDTLS_ECP_DP_SECP384R1,      /*!< Domain parameters for the 384-bit curve defined by FIPS 186-4 and SEC1. */
ecp.h:    MBEDTLS_ECP_DP_SECP521R1,      /*!< Domain parameters for the 521-bit curve defined by FIPS 186-4 and SEC1. */
ecp.h:    MBEDTLS_ECP_DP_BP256R1,        /*!< Domain parameters for 256-bit Brainpool curve. */
ecp.h:    MBEDTLS_ECP_DP_BP384R1,        /*!< Domain parameters for 384-bit Brainpool curve. */
ecp.h:    MBEDTLS_ECP_DP_BP512R1,        /*!< Domain parameters for 512-bit Brainpool curve. */
ecp.h:    MBEDTLS_ECP_DP_SECP192K1,      /*!< Domain parameters for 192-bit "Koblitz" curve. */
ecp.h:    MBEDTLS_ECP_DP_SECP224K1,      /*!< Domain parameters for 224-bit "Koblitz" curve. */
ecp.h:    MBEDTLS_ECP_DP_SECP256K1,      /*!< Domain parameters for 256-bit "Koblitz" curve. */
ecp.h:    const char *name;               /*!< A human-friendly name. */
ecp.h: * (SEC1 + RFC-4492)</li>
ecp.h: * In both cases, the generator (\p G) for a prime-order subgroup is fixed.
ecp.h: * authentic value for curves with <code>A = -3</code>(SECP256R1, etc), in which
ecp.h: * range of <code>0..2^(2*pbits)-1</code>, and transforms it in-place to an integer
ecp.h: * in size, so that it may be efficiently brought in the 0..P-1 range by a few
ecp.h: * reduction. It must return 0 on success and non-zero on failure.
ecp.h: *                as in the built-in implementation.
ecp.h:    int(*MBEDTLS_PRIVATE(modp))(mbedtls_mpi *);  /*!< The function for fast pseudo-reduction
ecp.h:    mbedtls_ecp_point *MBEDTLS_PRIVATE(T);       /*!< Pre-computed points for ecp_mul_comb(). */
ecp.h:    size_t MBEDTLS_PRIVATE(T_size);              /*!< The number of dynamic allocated pre-computed points. */
ecp.h: * Result is an array of at most ( 1 << ( MBEDTLS_ECP_WINDOW_SIZE - 1 ) )
ecp.h: *      w-size:     6       5       4       3       2
ecp.h: * Trade code size for speed on fixed-point multiplication.
ecp.h: * For each n-bit Short Weierstrass curve that is enabled, this adds 4n bytes
ecp.h:#define MBEDTLS_ECP_FIXED_POINT_OPTIM  1   /**< Enable fixed-point speed-up. */
ecp.h: * defines an MBEDTLS_ECP_MAX_BYTES-sized array unconditionally. */
ecp.h:    unsigned MBEDTLS_PRIVATE(depth);                     /*!<  call depth (0 = top-level)    */
ecp.h:    mbedtls_ecp_restart_mul_ctx *MBEDTLS_PRIVATE(rsm);   /*!<  ecp_mul_comb() sub-context    */
ecp.h:    mbedtls_ecp_restart_muladd_ctx *MBEDTLS_PRIVATE(ma); /*!<  ecp_muladd() sub-context      */
ecp.h:#define MBEDTLS_ECP_BUDGET(ops)     /* no-op; for compatibility */
ecp.h: * \brief    The ECP key-pair structure.
ecp.h: * A generic key-pair that may be used for ECDSA and fixed ECDH, for example.
ecp.h: *                  (currently only for clients and ECDHE-ECDSA).
ecp.h: *                  Lower (non-zero) values mean ECC functions will block for
ecp.h: *                  multiplication in GF(p) for the NIST P-256 curve.
ecp.h: *                  - about 3300 basic operations for P-256
ecp.h: *                  - about 9400 basic operations for P-384
ecp.h: *                  P-256   208     208     160     136     124
ecp.h: *                  P-384   682     416     320     272     248
ecp.h: *                  P-521  1364     832     640     544     496
ecp.h: *                  human-readable name.
ecp.h: * \param name      The human-readable name.
ecp.h: * \return          #MBEDTLS_ERR_MPI_ALLOC_FAILED on memory-allocation failure.
ecp.h: * \return          #MBEDTLS_ERR_MPI_ALLOC_FAILED on memory-allocation failure.
ecp.h: * \return          #MBEDTLS_ERR_MPI_ALLOC_FAILED on memory-allocation failure.
ecp.h: * \return          \c 0 if the point is non-zero.
ecp.h: * \brief           This function imports a non-zero point from two ASCII
ecp.h: * \param x         The first affine coordinate, as a null-terminated string.
ecp.h: * \param y         The second affine coordinate, as a null-terminated string.
ecp.h: * \return          #MBEDTLS_ERR_MPI_ALLOC_FAILED on memory-allocation failure.
ecp.h: *                  as defined in <em>RFC-4492: Elliptic Curve Cryptography
ecp.h: *                  It is not thread-safe to use same group in multiple threads.
ecp.h: *                  executes the exact same sequence of base-field
ecp.h: *                  operations for any valid \p m. It avoids any if-branch or
ecp.h: * \return          #MBEDTLS_ERR_MPI_ALLOC_FAILED on memory-allocation failure.
ecp.h: * \return          #MBEDTLS_ERR_MPI_ALLOC_FAILED on memory-allocation failure.
ecp.h: *                  \c -3.
ecp.h: * \return          \c 1 if <code>A = -3</code>.
ecp.h:    return grp->A.MBEDTLS_PRIVATE(p) == NULL;
ecp.h: *                  It is not thread-safe to use same group in multiple threads.
ecp.h: * \return          #MBEDTLS_ERR_MPI_ALLOC_FAILED on memory-allocation failure.
ecp.h: * \return          #MBEDTLS_ERR_MPI_ALLOC_FAILED on memory-allocation failure.
ecp.h: *                  It only checks that the point is non-zero, has
ecp.h: *                  point \c grp->G used by mbedtls_ecp_gen_keypair().
ecp.h: * \return          #MBEDTLS_ERR_MPI_ALLOC_FAILED on memory-allocation failure.
ecp.h: *                  For Weierstrass curves, this is the big-endian
ecp.h: *                  representation (which is little-endian), padded with
ecp.h: *                  (`grp->nbits` + 7) / 8 bytes
ecp.h: *                  where `grp->nbits` is the private key size in bits.
ecp.h: *                  This is always (`grp->nbits` + 7) / 8 bytes
ecp.h: *                  where `grp->nbits` is the private key size in bits.
ecp.h: * \brief           This function exports generic key-pair parameters.
ecp.h: * \return          #MBEDTLS_ERR_MPI_ALLOC_FAILED on memory-allocation failure.
platform.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
platform.h:/* The older Microsoft Windows common runtime provides non-conforming
platform.h: * \brief               This function dynamically sets the memory-management
platform.h: * - it *must* always correctly zero-terminate the buffer
platform.h: * - however it is acceptable to return -1 instead of the required length when
platform.h: * - it *must* always correctly zero-terminate the buffer
platform.h: * - however it is acceptable to return -1 instead of the required length when
platform.h: * Non-Volatile storage (NV) in a platform-independent way
platform.h: * \note    This structure may be used to assist platform-specific
platform.h: * \brief   This function performs any platform-specific initialization
platform.h: *          Its implementation is platform-specific, and unless
platform.h: *          platform-specific code is provided, it does nothing.
platform.h: *          Its implementation is platform-specific, and unless
platform.h: *          platform-specific code is provided, it does nothing.
ecjpake.h: * \brief Elliptic curve J-PAKE
ecjpake.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
ecjpake.h: * J-PAKE is a password-authenticated key exchange that allows deriving a
ecjpake.h: * strong shared secret from a (potentially low entropy) pre-shared
ecjpake.h: * This file implements the Elliptic Curve variant of J-PAKE,
ecjpake.h: * As the J-PAKE algorithm is inherently symmetric, so is our API.
ecjpake.h: * Roles in the EC J-PAKE exchange
ecjpake.h: * EC J-PAKE context structure.
ecjpake.h: * J-PAKE is a symmetric protocol, except for the identifiers used in
ecjpake.h: * Zero-Knowledge Proofs, and the serialization of the second message
ecjpake.h:    mbedtls_mpi MBEDTLS_PRIVATE(s);                      /**< Pre-shared secret (passphrase) */
ecjpake.h: * \param secret    The pre-shared secret (passphrase). This must be
ecjpake.h: * \param len       The length of the pre-shared secret \p secret.
ecjpake.h: *                  (TLS: Pre-Master Secret).
base64.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
base64.h:#define MBEDTLS_ERR_BASE64_BUFFER_TOO_SMALL               -0x002A
base64.h:#define MBEDTLS_ERR_BASE64_INVALID_CHARACTER              -0x002C
base64.h: * \brief          Decode a base64-formatted buffer
check_config.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
check_config.h:/* *INDENT-OFF* */
check_config.h:#error "Mbed TLS requires a platform with 8-bit chars"
check_config.h:/* See auto-enabling SNPRINTF_ALT and VSNPRINTF_ALT
check_config.h:#error "The NET module is not available for mbed OS - please use the network functions provided by Mbed OS"
check_config.h:#error "Unsupported partial support for ECC key type acceleration, see docs/driver-only-builds.md"
check_config.h:#error "Unsupported partial support for ECC curves acceleration, see docs/driver-only-builds.md"
check_config.h:/* Use of EC J-PAKE in TLS requires SHA-256. */
check_config.h:#error "!MBEDTLS_SSL_KEEP_PEER_CERTIFICATE requires SHA-512, SHA-256 or SHA-1".
check_config.h:#error "MBEDTLS_SHA512_USE_A64_CRYPTO_ONLY defined on non-Aarch64 system"
check_config.h:#error "MBEDTLS_SHA256_USE_ARMV8_A_CRYPTO_ONLY defined on non-Armv8-A system"
check_config.h: * and at least one ciphersuite, so at least SHA-256 or SHA-384
check_config.h:/* TLS 1.2 and 1.3 require SHA-256 or SHA-384 (running handshake hash) */
check_config.h:#if defined(MBEDTLS_HAVEGE_C) //no-check-names
check_config.h:#error "MBEDTLS_HAVEGE_C was removed in Mbed TLS 3.0. See https://github.com/Mbed-TLS/mbedtls/issues/2599"
check_config.h:#if defined(MBEDTLS_SSL_HW_RECORD_ACCEL) //no-check-names
check_config.h:#error "MBEDTLS_SSL_HW_RECORD_ACCEL was removed in Mbed TLS 3.0. See https://github.com/Mbed-TLS/mbedtls/issues/4031"
check_config.h:#if defined(MBEDTLS_SSL_PROTO_SSL3) //no-check-names
check_config.h:#error "MBEDTLS_SSL_PROTO_SSL3 (SSL v3.0 support) was removed in Mbed TLS 3.0. See https://github.com/Mbed-TLS/mbedtls/issues/4031"
check_config.h:#if defined(MBEDTLS_SSL_SRV_SUPPORT_SSLV2_CLIENT_HELLO) //no-check-names
check_config.h:#error "MBEDTLS_SSL_SRV_SUPPORT_SSLV2_CLIENT_HELLO (SSL v2 ClientHello support) was removed in Mbed TLS 3.0. See https://github.com/Mbed-TLS/mbedtls/issues/4031"
check_config.h:#if defined(MBEDTLS_SSL_TRUNCATED_HMAC_COMPAT) //no-check-names
check_config.h:#error "MBEDTLS_SSL_TRUNCATED_HMAC_COMPAT (compatibility with the buggy implementation of truncated HMAC in Mbed TLS up to 2.7) was removed in Mbed TLS 3.0. See https://github.com/Mbed-TLS/mbedtls/issues/4031"
check_config.h:#if defined(MBEDTLS_TLS_DEFAULT_ALLOW_SHA1_IN_CERTIFICATES) //no-check-names
check_config.h:#error "MBEDTLS_TLS_DEFAULT_ALLOW_SHA1_IN_CERTIFICATES was removed in Mbed TLS 3.0. See the ChangeLog entry if you really need SHA-1-signed certificates."
check_config.h:#if defined(MBEDTLS_ZLIB_SUPPORT) //no-check-names
check_config.h:#error "MBEDTLS_ZLIB_SUPPORT was removed in Mbed TLS 3.0. See https://github.com/Mbed-TLS/mbedtls/issues/4031"
check_config.h:#if defined(MBEDTLS_CHECK_PARAMS) //no-check-names
check_config.h:#error "MBEDTLS_CHECK_PARAMS was removed in Mbed TLS 3.0. See https://github.com/Mbed-TLS/mbedtls/issues/4313"
check_config.h:#if defined(MBEDTLS_SSL_CID_PADDING_GRANULARITY) //no-check-names
check_config.h:#error "MBEDTLS_SSL_CID_PADDING_GRANULARITY was removed in Mbed TLS 3.0. See https://github.com/Mbed-TLS/mbedtls/issues/4335"
check_config.h:#if defined(MBEDTLS_SSL_TLS1_3_PADDING_GRANULARITY) //no-check-names
check_config.h:#error "MBEDTLS_SSL_TLS1_3_PADDING_GRANULARITY was removed in Mbed TLS 3.0. See https://github.com/Mbed-TLS/mbedtls/issues/4335"
check_config.h:#if defined(MBEDTLS_SSL_TRUNCATED_HMAC) //no-check-names
check_config.h:#error "MBEDTLS_SSL_TRUNCATED_HMAC was removed in Mbed TLS 3.0. See https://github.com/Mbed-TLS/mbedtls/issues/4341"
check_config.h: * Avoid warning from -pedantic. This is a convenient place for this
check_config.h:/* *INDENT-ON* */
config_adjust_psa_from_legacy.h: * that high-level modules that can use either cryptographic API have the
config_adjust_psa_from_legacy.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
config_adjust_psa_from_legacy.h:/* SECP224K1 is buggy via the PSA API (https://github.com/Mbed-TLS/mbedtls/issues/3541) */
aes.h: *          The Advanced Encryption Standard (AES) specifies a FIPS-approved
aes.h: *          <em>ISO/IEC 18033-2:2006: Information technology -- Security
aes.h: *          techniques -- Encryption algorithms -- Part 2: Asymmetric
aes.h: *          The AES-XTS block mode is standardized by NIST SP 800-38E
aes.h: *          <https://nvlpubs.nist.gov/nistpubs/legacy/sp/nistspecialpublication800-38e.pdf>
aes.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
aes.h:/* Error codes in range 0x0020-0x0022 */
aes.h:#define MBEDTLS_ERR_AES_INVALID_KEY_LENGTH                -0x0020
aes.h:#define MBEDTLS_ERR_AES_INVALID_INPUT_LENGTH              -0x0022
aes.h:/* Error codes in range 0x0021-0x0025 */
aes.h:#define MBEDTLS_ERR_AES_BAD_INPUT_DATA                    -0x0021
aes.h: * \brief The AES context-type definition.
aes.h:                                                    10 round keys for 128-bit case. */
aes.h:                                                    <li>Simplifying key expansion in the 256-bit
aes.h: * \brief The AES XTS context-type definition.
aes.h: *                 <ul><li>256 bits (each of key1 and key2 is a 128-bit key)</li>
aes.h: *                 <li>512 bits (each of key1 and key2 is a 256-bit key)</li></ul>
aes.h: *                 <ul><li>256 bits (each of key1 and key2 is a 128-bit key)</li>
aes.h: *                 <li>512 bits (each of key1 and key2 is a 256-bit key)</li></ul>
aes.h: * \brief          This function performs an AES single-block encryption or
aes.h: * \brief  This function performs an AES-CBC encryption or decryption operation
aes.h: * \brief      This function performs an AES-XTS encryption or decryption
aes.h: *             AES-XTS encrypts or decrypts blocks based on their location as
aes.h: *             NIST SP 800-38E limits the maximum size of a data unit to 2^20
aes.h: *                     bytes in little-endian format. For disk encryption, this
aes.h: * \brief This function performs an AES-CFB128 encryption or decryption
aes.h: * \brief This function performs an AES-CFB8 encryption or decryption
aes.h: * \brief       This function performs an AES-OFB (Output Feedback Mode)
aes.h: *              For non-streaming use, the iv should be initialised on each call
aes.h: * \brief      This function performs an AES-CTR encryption or decryption
aes.h: *             per-message nonce, handled by yourself, and the second one
aes.h: *             per-message nonce, and the last 4 bytes for internal use. In that
aes.h: *             The per-message nonce (or information sufficient to reconstruct
aes.h: *             for example, with 96-bit random nonces, you should not encrypt
aes.h: * \param nonce_counter    The 128-bit nonce and counter.
aes.h: *                         It must be a readable-writeable buffer of \c 16 Bytes.
aes.h: *                         It must be a readable-writeable buffer of \c 16 Bytes.
timing.h: *  SPDX-License-Identifier: Apache-2.0 OR GPL-2.0-or-later
timing.h: * \return         -1 if cancelled (fin_ms = 0),
